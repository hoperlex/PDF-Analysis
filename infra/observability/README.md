# Observability

Define diagnostic logs, durable audit and metrics separately. Metric labels remain low-cardinality; redaction uses allowlist. Exact metric/event names freeze with the capability that emits them.
