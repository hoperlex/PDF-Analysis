# Local stack

Implemented in S01. It must start isolated PostgreSQL and private S3-compatible storage with documented health checks. Local credentials are disposable and never reused as production credentials.
