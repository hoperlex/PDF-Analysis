{
  "lock_version": 1,
  "purpose": "Everything a later regeneration needs to prove the committed API client matches its input: the toolchain pins, the exact generator invocation, and the exact OpenAPI bytes the client was generated from. Verified by web/tests/guards/frontend-lock.guard.test.ts, which recomputes every digest here and fails if one has moved.",
  "owner": "Gate A session A5 (P3-WEB-00 + P3-API-01)",
  "toolchain": {
    "node": "22.23.1",
    "npm": "10.9.8",
    "packageManager": "npm@10.9.8",
    "nvmrc": "web/.nvmrc",
    "manifest": "web/package.json",
    "lockfile": "web/package-lock.json",
    "lockfileVersion": 3,
    "lockfile_sha256": "98691bc83549225d6d94ec53c1fc810a58b12cf74487786638fd007d57fd3b77",
    "install_command": "npm --prefix web ci",
    "no_floating_versions": "Every dependency, devDependency, engines entry and packageManager value is an exact version. `save-exact=true` and `engine-strict=true` in web/.npmrc keep a later `npm install` from reintroducing a range. Enforced by web/tests/guards/pinned-versions.guard.test.ts."
  },
  "openapi": {
    "path": "contracts/api/v1/openapi.json",
    "sha256": "00b16114e176238636fa0d62c475ad15483c008625ed1b3c5af3f882d6717ab1",
    "content_commit": "a49d047",
    "content_commit_subject": "feat(W45-BLOCKS): getVersionBlocks — the block index for a version, absent vs not-produced",
    "dispatch_named_commit": "a49d047",
    "commit_note": "Resealed 2026-09-25 by W45-BLOCKS. One operation is added -- getVersionBlocks, GET /versions/{version_uid}/blocks -- the first half of R-23's block markup: page_geometry_extraction writes a real bbox per extracted text line (contracts/analysis/v1/stage-registry.json declares this stage's execution.model_routed:false, so D-70's blocked provider key does not gate it) and this operation reads that geometry back. Keyed by version_uid rather than by run_id, because the geometry is a deterministic property of the version and not of any one run of it. An unknown version answers 404 not_found. A version that exists but whose most recently successful run never reached page_geometry_extraction -- or has no run at all -- answers 200 with status:\"not_produced\" and blocks:[]; a version whose geometry was produced and genuinely has none answers 200 with status:\"produced\" and blocks:[] -- the same bytes for blocks, different bytes for status, which is the field a caller reads to tell the two apart. It carries no crops: page_geometry_extraction's second artifact, the page-crop manifest, is unconditionally empty in this pipeline (crop_policy:\"none\") and is a separate artifact this operation does not expose at all, rather than an always-[] field shipped unexplained. Two component schemas were added, VersionBlockIndex and BlockGeometry; BlockGeometry's bbox is inline (an object with x0/y0/x1/y1), not a third schema, so a nested Pydantic model would not mint one FastAPI's generated document and the frozen contract would then disagree about. No error code was added: the only refusal this operation needs, not_found, is already in the twenty-two-code catalog. Paths, operations and component schemas move from 15 / 18 / 51 to 16 / 19 / 53.\n\nThis reseal lands all four documents in one commit, following W42-SEAL's discipline rather than the two-commit shape the reseals before it used. The Python implementation -- the port, the adapter, the router, the two view types, the two Pydantic models, the composition wiring, and the composition guards this reseal's new parented GET requires updating (the ten-addressed-GET sweep becomes eleven, the eighteen-route composition-root literal becomes nineteen) -- landed one commit earlier, a49d047, which content_commit names for the reason W42-SEAL's own note gives: a commit cannot name itself, so the field names the nearest commit that made the surface this document now describes real. THE AUTHORITY IS THE DIGEST, not this field: sha256 above is computed over the contract's own bytes and is what the drift guard compares.\n\nResealed 2026-09-18 by W18-SEAL under owner ruling R-5 (docs/program/OWNER_RULINGS_2026-09-17.md section 3.5), closing DEBT_REGISTER.md D-16 and D-21 together. Three list operations were added -- listDocuments (GET /projects/{project_uid}/documents), listVersions (GET /documents/{document_uid}/versions) and listRuns (GET /versions/{version_uid}/runs) -- because before them every read below the project list needed an identifier the client was already holding, so a reloaded screen could reach nothing that survived in the database and the store. Each is the GET of the collection its parent creates into, takes cursor and limit and returns the same {items, page} envelope as listProjects. RunStatus gained three optional properties -- cost_micros, cost_basis and model_call_count -- so what a run spent at the provider is readable by a user for the first time; the figure is summed from the model_call rows and never from stage_result.metrics, whose cost_usd/cost_basis pair is the D-15 ambiguity. Paths, operations and component schemas move from 10 / 12 / 43 to 12 / 15 / 46. No error code was added: an unknown parent is not_found and a malformed cursor is validation_failed, both already in the catalog.\n\nResealed 2026-09-21 by W25-SEAL under owner ruling R-13 (docs/program/OWNER_RULINGS_2026-09-17.md section 3.8), which reinstates R-8 and explicitly authorises this file to move. R-8 adds one error code, staged_upload_lost, because TemporaryBlobLostError and BlobAttributeConflictError both answered conflict with aggregate_type Blob and produced envelopes byte-identical apart from correlation_id, while demanding opposite operator responses -- stop, the instance was restored wrong, against retry the upload. retryable is read from the catalog for the reported code, so a widened safe_detail_keys could not have carried two values; only a separate code can. contracts/api/v1/openapi.json therefore gains that one ErrorCode enum member, plus two prose corrections forced by guards: info.description's twenty-one-code catalog becomes twenty-two (tests/contract/api_v1/test_surface_counts_in_prose.py reads that block), and the 503 response description, which named one code for a status that now carries two. Paths, operations and component schemas do NOT move: still 12 / 15 / 46. The six digests below were re-derived with sha256sum against this tree, not copied from the W20-CODE record; all six agree with it.\n\nResealed 2026-09-22 by W34-CONTRACT, which adds one operation -- issueToken, POST /auth/token -- so that a caller holding no credential has a way through the R-3 seam. Login and password in, an opaque credential and its lifetime out. It is the only operation in the document whose own `security` is the empty requirement, overriding the root for it and for nothing else; every other operation still carries bearerAuth. T-6's ban is what shaped it: no issuer, no discovery URL, no flow, no bearerFormat and no role, subject or capability vocabulary appear, and `token` is a string the document states no structure for. Two component schemas were added, IssueTokenRequest and IssueTokenResponse. No error code was added: an unaccepted login/password pair is the catalog's authentication_required and a malformed body is validation_failed; permission_denied is deliberately not declared on this one operation, because it means an authenticated subject refused a resource and this is the operation that produces one. Paths, operations and component schemas move from 12 / 15 / 46 to 13 / 16 / 48. The six digests below were recomputed with sha256sum against this tree after `npm --prefix web run api:generate`, not copied forward.\n\nResealed 2026-09-22 by W38-KB under owner ruling R-24 (docs/program/OWNER_RULINGS_2026-09-17.md section 3.11), which explicitly authorises a second reseal after wave 34's and took the expensive option deliberately: a client-side walk over runs and findings is cheaper and lives only as long as the alpha, a listing operation outlives it. One operation is added -- listDecisions, GET /decisions -- because listDecisionHistory reads the history of ONE finding and no operation in the document enumerated decisions at all, so the knowledge base R-23 requires had no source. It is a rebuildable projection in the sense ADR-0012 fixes and creates nothing. Two component schemas were added, DecisionRecord and DecisionRecordPage; DecisionRecord restates DecisionEvent rather than composing it with allOf, for the reason FindingDetail already states about closed schemas under JSON Schema 2020-12. No parameter component was added: category and verdict already exist and already mean the finding's category and the finding's current projected verdict, which is what they mean here. No error code was added, and the operation declares no 404 at all -- its path addresses no parent identity, so an empty journal is an empty page. Paths, operations and component schemas move from 13 / 16 / 48 to 14 / 17 / 50. The six digests below were recomputed with sha256sum against this tree after `npm --prefix web run api:generate`, not copied forward.\n\nResealed 2026-09-23 by W39-REVOKE under owner ruling R-26 (docs/program/OWNER_RULINGS_2026-09-17.md), which rules four account guards into the alpha. This is the one of the four that CANNOT be added later: the other three make an account weak now and are fixed by adding them, while a credential already issued stays valid until it expires and no later work reaches one already sitting in a browser. One operation is added -- changePassword, POST /auth/password -- and it carries revocation with it, because the two share one mechanism: migration 0007_credential_epoch adds app_user.token_epoch, every minted credential carries the epoch it was minted under, the seam compares the two on every guarded request, and changePassword raises the column in the same UPDATE that writes the new digest. Raising it invalidates every credential ever minted for that account with no denylist kept anywhere. Revocation gets NO operation: taking credentials away from accounts whose passwords nobody is changing is an operator's action (python -m auditmanager.access.revoke) and publishing it would require deciding who may revoke whom, which is the role vocabulary T-6 forbids inventing at this seam. ONE component schema was added, ChangePasswordRequest; NO response schema was, because changePassword answers IssueTokenResponse -- a credential and its lifetime is one shape and the document already had a name for it. No error code was added: a wrong current password is authentication_required and a new password equal to the current one is validation_failed. Paths, operations and component schemas move from 14 / 17 / 50 to 15 / 18 / 51. Every credential minted before this reseal is refused from the moment it lands, because it carries no epoch; everyone signs in again once, which is the intended effect of a change whose purpose is that credentials can be taken away. The six digests below were recomputed with sha256sum against this tree after `npm --prefix web run api:generate`, not copied forward, and the openapi digest was verified against the contract's own bytes before the gate ran.\n\nResealed 2026-09-23 by W42-SEAL under owner rulings R-37 and R-29. **It is the first reseal in this programme that adds no path and no operation, and the counts below do not move: 15 / 18 / 51, exactly as W39-REVOKE left them.** That is the whole reason it had to be a reseal rather than an edit, and D-18 is the reason: the conformance gate drops description, summary and title as annotation under N4, so a corrected sentence is invisible to it and reaches a reader only through these four documents moving together -- contracts/api/v1/openapi.json, the regenerated client, the mirror, and the sha in this file written by hand.\n\nR-37, closing DEBT_REGISTER.md D-86: DecisionEvent.author_label and DecisionRecord.author_label described themselves as 'one configured local reviewer label ... a label, not a subject identity'. Wave 41 (D-78) made the server write the authenticated reviewer instead of a constant, which made that sentence false, and the owner has now ruled that what it writes is the reviewer's DISPLAY NAME rather than their login -- this is the field another reviewer reads on a decision somebody else took, and a login is an address. The schema does not move: same type, same minLength 1, same maxLength 128, so nothing frozen changed in the sense the freeze means. The sentence was carried TWICE and both copies are corrected; DecisionRecord restates DecisionEvent's properties rather than composing them with allOf, so a reseal that corrected one would have left listDecisions describing the field falsely. An account that has chosen no display name records its login, which is server-held, unique and 1..100 characters -- inside author_label's bounds by construction and not by luck. Migration 0009_reviewer_display_name adds app_user.display_name, nullable so that 'has not chosen a name' stays distinguishable from 'chose their login'.\n\nR-29, closing DEBT_REGISTER.md D-46 by that row's own cheaper option: RunStatus gains ONE OPTIONAL PROPERTY, terminal_detail. A failed run could say a dependency was unavailable and could not say WHICH, because the domain catalog's safe_detail_keys live on the ErrorEnvelope and a 200 run reading is not an error envelope. It is the same shape and the same rule as ErrorEnvelope.details -- safe scalar classifiers, maxProperties 16, propertyNames ^[a-z][a-z0-9_]{0,63}$, scalar values bounded at 256 -- restricted to the keys the catalog declares for the code in terminal_reason, and present only beside one, because the allowlist that bounds it is a property of the reported code. NO component schema was added: the shape is declared inline exactly as ErrorEnvelope.details is, which is why component_schemas stays 51. NO error code was added: a distinct code for a replay miss was the row's option 2 and is a second reseal, which the owner did not take, so contracts/domain/v1/error-codes.json is untouched and stays at twenty-two. The sentence this makes sayable is 'your document is fine, the provider is fine, this deployment has no recording for it', which R-30 made the ordinary case by putting the alpha stand in recorded mode. Migration 0010_run_terminal_detail adds audit_run.terminal_detail, a separate revision from 0009 so that reverting this reseal does not also un-name every reviewer.\n\nThe six digests below were recomputed with sha256sum against this tree after `npm --prefix web run api:generate`, not copied forward, and the contract digest was verified equal to the mirror's before the gate ran.\n\nAbout content_commit, because this reseal spends it differently from the four before it. Those landed the contract in one commit and this file in a later one (W38-KB's 2cb537b, W39-REVOKE's 05fe141), so the field could name the commit carrying the contract bytes. This reseal lands all four documents in ONE commit, because four documents moving together is the property D-18 exists to protect and a lock that trails its own contract by a commit is a window in which the frontend reads a digest the backend does not serve. A commit cannot name itself, so the field names 667a49c instead -- feat(R-37), the commit that made the corrected author_label description TRUE, and the nearest commit this document can honestly point at. THE AUTHORITY IS THE DIGEST, not this field: sha256 above is computed over the contract's own bytes and is what the drift guard compares.",
    "openapi_version": "3.1.0",
    "info_version": "1.0.0-draft.1",
    "paths": 16,
    "operations": 19,
    "component_schemas": 53,
    "snapshot": "web/openapi/openapi.json",
    "snapshot_sha256": "00b16114e176238636fa0d62c475ad15483c008625ed1b3c5af3f882d6717ab1",
    "read_only": "A5 generates from contracts/api/v1/** and never edits it: a client and a contract reconciled by the same hand prove nothing. The seam is repaired only by the session that owns it - the FindingDetail repair was made by A7-FIX in contracts/api/v1/**, and this client regenerated from the result."
  },
  "generator": {
    "script": "web/scripts/generate-api-client.mjs",
    "script_sha256": "787c744d0c97420b0c1c1bd33bcc530d7ce803a21db07a5ef9557de4a02ee123",
    "generator_version": "1.0.0",
    "generate_command": "npm --prefix web run api:generate",
    "verify_command": "npm --prefix web run api:verify",
    "runtime": "node 22.23.1, no dependencies",
    "determinism": "Output is a pure function of the input bytes. No timestamp, hostname, absolute path or environment value is emitted; every iteration order is an explicit locale-independent sort, because localeCompare is locale-dependent. Two runs on the same document produce byte-identical files."
  },
  "generated": {
    "directory": "web/src/shared/api/generated",
    "files": {
      "client.gen.ts": "54727cba44458ea1c074098af2384775e31c97a326d6f54b95f7e9256256e2bd",
      "index.ts": "3179df6c050b85535f07aa1e22507990965ae39b12e0d4e96ed189dc60bca7ef",
      "operations.gen.ts": "9422315782156ccfe43016c5d70abbebc35948ac53282a69666eba71af592238",
      "types.gen.ts": "dbbffe4a4ec41c0e1ccdfed6360a4b04e4d77b14d602f678cea874a79313980f"
    },
    "hand_editing": "forbidden; every file carries a do-not-edit header and the drift guard compares bytes"
  },
  "commands": {
    "install": "npm --prefix web ci",
    "build": "npm --prefix web run build",
    "typecheck": "npm --prefix web run typecheck",
    "lint": "npm --prefix web run lint",
    "guards": "npm --prefix web run test:guards",
    "contract": "npm --prefix web run test:contract",
    "makefile": "none. OD-16 forbids adding a Makefile target for the frontend, so every command is `npm --prefix web run ...`."
  },
  "reserved_scripts": {
    "csv:verify": "the CSV export lane"
  }
}
