{
  "openapi": "3.1.0",
  "info": {
    "title": "AuditManager PC-01 API",
    "version": "1.0.0-draft.1",
    "summary": "The exact PC-01 journey: one project, one AR PDF, one text-consistency run, one CSV.",
    "description": "Frozen at Gate A so `A5` can generate a typed client and `B6` can implement routers against a fixed shape, without either waiting on the other.\n\nResealed 2026-09-18 under owner ruling `R-3`: every operation here requires a bearer credential, declared once at the document root under `components.securitySchemes.bearerAuth`. The scheme names the seam and never its implementation. The operational liveness and readiness plane is outside `/api/v1`, carries no product meaning and needs no credential.\n\nWhat this surface deliberately does not have: tenancy, roles, rate limiting, WebSocket or server-sent events; an export resource, export identity or export polling, because a synchronous read creates nothing; and any endpoint outside the eleven capabilities `P2-API-01` enumerates, other than the credential exchange the authorization seam needs and `W34-CONTRACT` adds below.\n\nThree rules hold everywhere and are not repeated per operation:\n\n1. **Opaque identity.** Every identifier is `<prefix>_<ULID>` from `contracts/domain/v1/identifiers.json`, and every path parameter carries the pattern for its entity. No response body, header or error detail ever contains a bucket name, object key, filesystem path, URL to internal storage, credential, prompt or model payload.\n2. **One failure shape.** Every non-2xx response is the `ErrorEnvelope` of `contracts/domain/v1/error-envelope.schema.json`, its `error_code` drawn from the twenty-two-code catalog and its `retryable` pinned to the catalog value for that code. A caller never infers retryability from the HTTP status.\n3. **Correlation.** Every response carries `X-Correlation-Id`. A caller may supply one on the request; the edge assigns one when the caller does not.\n\nRun state uses the contract vocabulary. The success terminal of an `AuditRun` is `published`. `succeeded` is a `StageResult` status on a different aggregate and never appears as a run state.\n\nResealed 2026-09-18 under owner ruling `R-5`, which closes `DEBT_REGISTER.md` `D-16` and `D-21` together. Three list operations -- `listDocuments`, `listVersions` and `listRuns` -- make published work reachable after a page reload: before them every read below the project list needed an identifier the client was already holding, so a reloaded screen could reach nothing that survived in the database and the store. And `RunStatus` gained `cost_micros`, `cost_basis` and `model_call_count`, so what a run spent is readable by a user for the first time. Each listing is a `GET` of the collection its parent creates into, pages with the same `cursor` and `limit` and returns the same `{items, page}` envelope as `listProjects`. No error code was added: an unknown parent is the catalog's `not_found` and a malformed cursor is `validation_failed`.\n\nAmended 2026-09-18 under owner ruling `R-8`, which closes `DEBT_REGISTER.md` `D-18`. The domain catalog gains one code, `staged_upload_lost`, and the `ErrorCode` enum below carries it: twenty-one becomes twenty-two. Two blob-store failures demanding opposite operator responses -- *stop, the instance was restored wrong* against *retry the upload* -- both answered `conflict` with `aggregate_type` `Blob`, so their envelopes were byte-identical apart from `correlation_id`. A widened `safe_detail_keys` was offered and rejected, because a detail key cannot fix `retryable`: the envelope reads that flag from the catalog for the reported code, so only a separate code carries a separate value. **No path, operation or schema changed** -- that amendment moved none of the three counts -- and the new code travels on the `503` every staging write already declares.\n\nResealed 2026-09-22 by `W34-CONTRACT`. `R-3` declared the seam and left no way to get through it: the document required a bearer credential on every operation and described no operation that hands one out, so the only caller this surface admitted was one already holding a credential from somewhere the document does not name. One operation closes that -- `issueToken`, `POST /auth/token` -- and it is a credential *exchange* and nothing more: a login and a password in, an opaque credential and its lifetime out. It is the single operation whose `security` is the empty requirement, because requiring a credential to obtain one is a closed door with a handle on the inside.\n\n**What this reseal deliberately does not add, because `T-6` forbids it.** No issuer, no discovery URL, no flow, no `bearerFormat`, and no role, subject or capability vocabulary. The returned `token` is a string this document states no structure for; the deployment decides what it is, and the operations keep working when that decision changes. No error code was added either: an unaccepted login and password pair is the catalog's `authentication_required`, a malformed body is `validation_failed`. `permission_denied` is **not** declared here, because it means an authenticated subject refused a resource and this operation has no authenticated subject in it -- it is the operation that produces one. That reseal added one path, one operation and two schemas, and the sentence that stated the totals it produced has been rewritten as this delta: a reseal's own arithmetic stops being true at the next reseal, and `tests/contract/api_v1/test_surface_counts_in_prose.py` reads every such claim in this block against the live document. The totals are stated once, below.\n\nResealed 2026-09-22 by `W38-KB` under owner ruling `R-24`, which was ruled against the recommendation the owner was given: a client-side walk over runs and findings is cheaper and lives only as long as the alpha, and a listing operation costs a reseal and outlives it. One operation closes it -- `listDecisions`, `GET /decisions` -- because `listDecisionHistory` reads the history of **one** finding and nothing in this document enumerated decisions at all, so the knowledge base `R-23` requires had no source but a walk over every run. It is a projection in the sense `ADR-0012` fixes and creates nothing. Two component schemas were added, `DecisionRecord` and `DecisionRecordPage`; no parameter was, because `category` and `verdict` already exist and already mean the finding's category and the finding's current verdict. No error code was added: a malformed cursor is the catalog's `validation_failed`, an absent or refused credential is `authentication_required`, and there is no `404` on it at all -- the path addresses no parent identity, so an empty journal is an empty page. That reseal added one path, one operation and two schemas.\n\nResealed 2026-09-23 by `W39-REVOKE` under owner ruling `R-26`, which rules four account guards into the alpha. This is the one of them that **cannot be added later**: the other three make an account weak now and are fixed by adding them, while a credential already issued is permanent until it expires, and no later work reaches one sitting in somebody's browser. One operation closes it -- `changePassword`, `POST /auth/password` -- and it carries revocation with it, because the two share a mechanism: the account's credential generation is raised by the same write that stores the new digest, which is what makes every credential minted under the old password stop being accepted.\n\n**What this does not add.** No response schema: a credential and its lifetime is one shape and `IssueTokenResponse` already names it. No error code: a wrong current password is the catalog's `authentication_required` and a new password that is the current one is `validation_failed`. No revocation operation: taking credentials away from accounts whose passwords nobody is changing is an operator's action on the deployment, and publishing it would require deciding who may revoke whom -- the role vocabulary `T-6` forbids inventing here. One path, one operation and one schema. The surface moved to 15 / 18 / 51.\n\nResealed 2026-09-23 by `W42-SEAL` under owner rulings `R-37` and `R-29`, and it is the first reseal here that adds no path and no operation. **No path, operation or schema moved: still 15 / 18 / 51, exactly as before.** Three things change and each is a *description* or a *property of an existing schema*, which is precisely why it still costs a reseal -- the conformance gate drops descriptions as annotation under `N4`, so nothing but this document carries them to a reader, and `D-18`'s four documents move together or the frontend reads a contract the backend does not serve.\n\n`R-37`, and `DEBT_REGISTER.md` `D-86`: `DecisionEvent.author_label` and `DecisionRecord.author_label` described themselves as *\"one configured local reviewer label ... a label, not a subject identity\"*. Since wave 41 that was false -- the server writes the authenticated reviewer, not a constant -- and the owner has now ruled that what it writes is the reviewer's **display name** rather than their login, because this is the field another reviewer reads on a decision somebody else took. **The schema does not move**: same type, same `minLength: 1`, same `maxLength: 128`. The sentence was wrong twice over and is corrected in both copies; `DecisionRecord` restates `DecisionEvent`'s properties rather than composing them with `allOf`, so a reseal that corrected one would have left `listDecisions` still describing the field falsely. An account that has chosen no display name records its login, which is a server-held string the reviewer typed and is 1..100 characters, inside this field's bounds by construction.\n\n`R-29`, and `D-46`: `RunStatus` gains one optional property, `terminal_detail`. A `failed` run could say that a dependency was unavailable and could not say **which**, because the catalog's `safe_detail_keys` live on the `ErrorEnvelope` and a 200 run reading is not an error envelope. It is the same shape and the same rule as `ErrorEnvelope.details` -- safe scalar classifiers, restricted to the keys the domain catalog declares for the reported code -- applied to the code in `terminal_reason` instead of to the one in `error_code`, and it is present only beside a `terminal_reason` because the allowlist that bounds it is a property of that code. **No component schema was added**, because the shape is declared inline exactly as `ErrorEnvelope.details` is, and **no error code was added**: a distinct code for a replay miss was the row's other option and is a second reseal, which the owner did not take. The domain catalog stays at twenty-two codes and stays frozen. The true sentence this makes sayable is *your document is fine, the provider is fine, this deployment has no recording for it*, which `R-30` made the ordinary case by putting the alpha stand in `recorded` mode. Two migrations carry the storage: `0009_reviewer_display_name` and `0010_run_terminal_detail`.\n\nResealed 2026-09-25 by `W45-BLOCKS`. One operation is added -- `getVersionBlocks`, `GET /versions/{version_uid}/blocks` -- the first half of `R-23`'s block markup: `page_geometry_extraction` writes a real `bbox` per extracted text line and this operation reads it back, keyed by `version_uid` rather than by `run_id` because the stage carries no model and no provider reference and its output is a deterministic property of the version, identical across every run that reaches it. It answers `404` for an unknown version and, for a known one, `status: \"produced\"` or `status: \"not_produced\"` -- absent is not empty, and a version whose geometry has not yet been derived answers the same `blocks: []` as one that genuinely has none, distinguished only by `status`. It carries no crops: the stage's second artifact, the page-crop manifest, is unconditionally empty in this pipeline and is not exposed here at all. Two component schemas are added, `VersionBlockIndex` and `BlockGeometry`; no error code was added, because the only refusal this operation needs, `not_found`, is already in the twenty-two-code catalog. The surface moves to sixteen paths, nineteen operations and fifty-three schemas.",
    "contact": {
      "name": "docs/program/P02_SEAMS.md - seam S8"
    },
    "license": {
      "name": "Proprietary",
      "identifier": "LicenseRef-Proprietary"
    }
  },
  "servers": [
    {
      "url": "/api/v1",
      "description": "Version-prefixed base path. A breaking change is a new contract version, never an edit here."
    }
  ],
  "security": [
    {
      "bearerAuth": []
    }
  ],
  "tags": [
    {
      "name": "auth",
      "description": "Exchange a login and a password for a bearer credential."
    },
    {
      "name": "projects",
      "description": "Create and list projects."
    },
    {
      "name": "documents",
      "description": "Upload one PDF and read the published version."
    },
    {
      "name": "runs",
      "description": "Start a run and read its per-stage status."
    },
    {
      "name": "findings",
      "description": "Published findings, their evidence and detail."
    },
    {
      "name": "decisions",
      "description": "The append-only expert decision ledger, and the journal projected over it."
    },
    {
      "name": "export",
      "description": "Synchronous CSV export of one run."
    }
  ],
  "paths": {
    "/auth/token": {
      "post": {
        "operationId": "issueToken",
        "tags": [
          "auth"
        ],
        "summary": "Exchange a login and a password for a bearer credential.",
        "description": "The only operation on this surface that is reachable without a credential, because it is the one that hands one out: its `security` is the empty requirement, which overrides the document root for this operation and for no other.\n\n**What this operation says about the credential it returns: nothing.** The response carries an opaque string and how long it stays valid. Its structure, what it encodes, and where the deployment got it are the deployment's own, are not described anywhere in this document, and are never parsed by a caller -- the same rule that keeps `bearerFormat` off `components.securitySchemes.bearerAuth`. A deployment that replaces a static string with something else changes no operation here.\n\nNothing is created and nothing is changed, so there is no idempotency key: a repeat of the same exchange is a second exchange, not a replay of the first.",
        "security": [],
        "parameters": [
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/IssueTokenRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The credentials were accepted. The body carries the credential to present on every other operation and how long it stays valid.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IssueTokenResponse"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/auth/password": {
      "post": {
        "operationId": "changePassword",
        "tags": [
          "auth"
        ],
        "summary": "Change the signed-in account's password and revoke its old credentials.",
        "description": "Two things happen here and they are one write. The account's password becomes the one supplied, and every credential this deployment ever minted for that account stops being accepted -- **including the credential this request presented**. A password change that leaves the old password's credentials working is a password change in name only, so the two are not offered separately and cannot land separately.\n\n**Whose password changes is decided by the credential, never by the body.** There is no login property and there will not be one: the caller may change their own password and no other, and a login in the body would be an operation one caller could aim at another.\n\n**The response is the replacement credential**, minted after the change, and the only one this account now accepts. It is answered rather than left to a second exchange because the caller's own credential was revoked by this very request: a `204` would leave them holding something already dead with no way to tell that from a failure.\n\nThe `403` is declared because every operation behind the seam declares it: `permission_denied` means an authenticated subject was refused, a generated client needs a typed shape for it on every authorized operation, and an operation that omitted it would be claiming a property about a role model this document does not describe. This surface raises it nowhere, here included.\n\nNothing is created, so there is no idempotency key. A repeat of the same request is refused by its own `current_password`, which is no longer current -- it is not replayed and there is no `409` here.\n\nRevocation has no operation of its own on this surface. Ending a pilot -- taking credentials away from accounts whose passwords nobody is changing -- is an operator's action taken on the deployment, and publishing it would require deciding who may revoke whom, which is the role vocabulary this document deliberately does not have.",
        "parameters": [
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChangePasswordRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The password was changed. Every credential minted for this account before this response -- including the one this request presented -- is refused from now on. The body carries the replacement.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IssueTokenResponse"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/projects": {
      "post": {
        "operationId": "createProject",
        "tags": [
          "projects"
        ],
        "summary": "Create a project.",
        "parameters": [
          {
            "$ref": "#/components/parameters/IdempotencyKey"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateProjectRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The project was created, or the recorded outcome of an identical earlier request was replayed.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Project"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "409": {
            "$ref": "#/components/responses/IdempotencyConflict"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      },
      "get": {
        "operationId": "listProjects",
        "tags": [
          "projects"
        ],
        "summary": "List projects, newest first.",
        "parameters": [
          {
            "$ref": "#/components/parameters/Cursor"
          },
          {
            "$ref": "#/components/parameters/Limit"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of projects.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectPage"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/projects/{project_uid}/documents": {
      "parameters": [
        {
          "$ref": "#/components/parameters/ProjectUid"
        }
      ],
      "post": {
        "operationId": "uploadDocument",
        "tags": [
          "documents"
        ],
        "summary": "Upload one PDF and publish an immutable document version.",
        "description": "The whole PC-01 ingest path. The accepted envelope is one unencrypted PDF, at most 25 MiB and at most 30 pages, every page carrying extractable embedded text. A scanned or image-only file, a password-protected file, a non-PDF, an oversize file, an over-page-count file and a companion or archive upload are each refused with `validation_failed` and a `constraint` detail. OCR is never silently substituted.\n\nOn success the version and its input manifest are immutable: there is no update or delete endpoint for either, and the database refuses both.",
        "parameters": [
          {
            "$ref": "#/components/parameters/IdempotencyKey"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "$ref": "#/components/schemas/UploadDocumentRequest"
              },
              "encoding": {
                "file": {
                  "contentType": "application/pdf"
                }
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "A published, immutable document version.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DocumentVersion"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/IdempotencyConflict"
          },
          "422": {
            "$ref": "#/components/responses/UploadRejected"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      },
      "get": {
        "operationId": "listDocuments",
        "tags": [
          "documents"
        ],
        "summary": "List the documents of one project, newest first.",
        "description": "One item per document, carrying the version the document currently points at -- the one a reader would open, stream or start a run against. A document with no published version is not listed, because there is nothing to address.\n\nThe item is a `DocumentVersion` because that is what `uploadDocument` publishes into this same collection: the `GET` of a collection returns a page of exactly what its `POST` returns, which is the rule `listProjects` follows against `createProject`. This surface has no separate `Document` resource and does not acquire one here.\n\nAn unknown project is `404`, never an empty page: \"this project has nothing in it yet\" is a different answer from \"there is no such project\".",
        "parameters": [
          {
            "$ref": "#/components/parameters/Cursor"
          },
          {
            "$ref": "#/components/parameters/Limit"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of this project's documents.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DocumentVersionPage"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/versions/{version_uid}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/VersionUid"
        }
      ],
      "get": {
        "operationId": "getDocumentVersion",
        "tags": [
          "documents"
        ],
        "summary": "Read one published document version.",
        "parameters": [
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "The version and its input manifest.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DocumentVersion"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/versions/{version_uid}/content": {
      "parameters": [
        {
          "$ref": "#/components/parameters/VersionUid"
        }
      ],
      "get": {
        "operationId": "streamDocumentVersionContent",
        "tags": [
          "documents"
        ],
        "summary": "Stream the PDF bytes for the viewer.",
        "description": "The server streams the bytes itself. It does not redirect and does not return a presigned link: a URL into object storage is exactly the internal address the contract forbids putting in a response, and it would also outlive the request that authorized it.",
        "parameters": [
          {
            "$ref": "#/components/parameters/CorrelationId"
          },
          {
            "name": "Range",
            "in": "header",
            "required": false,
            "description": "Byte range, so the viewer can page a large document without fetching all of it.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The complete PDF.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              },
              "Content-Length": {
                "description": "Byte size of the published version.",
                "schema": {
                  "type": "integer",
                  "minimum": 0
                }
              }
            },
            "content": {
              "application/pdf": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          },
          "206": {
            "description": "The requested byte range.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/pdf": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "422": {
            "$ref": "#/components/responses/StorageIntegrityError"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/versions/{version_uid}/runs": {
      "parameters": [
        {
          "$ref": "#/components/parameters/VersionUid"
        }
      ],
      "get": {
        "operationId": "listRuns",
        "tags": [
          "runs"
        ],
        "summary": "List the runs of one published version, newest first.",
        "description": "Runs hang off the version because `startRun` takes a `version_uid` and nothing else that identifies anything -- the project is derived from it. This is the inverse of that direction and not a second way of addressing a run.\n\nEach item is the whole `RunStatus`, identical to what `getRunStatus` answers for that run. There is no lighter list shape: a second shape of one resource is a second thing to drift.\n\nAn unknown version is `404`, never an empty page.",
        "parameters": [
          {
            "$ref": "#/components/parameters/Cursor"
          },
          {
            "$ref": "#/components/parameters/Limit"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of this version's runs.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunStatusPage"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/versions/{version_uid}/blocks": {
      "parameters": [
        {
          "$ref": "#/components/parameters/VersionUid"
        }
      ],
      "get": {
        "operationId": "getVersionBlocks",
        "tags": [
          "documents"
        ],
        "summary": "Read the page-by-page block index derived for one published version.",
        "description": "Keyed by `version_uid`, not by `run_id`. `page_geometry_extraction` carries no model and no provider reference, so its output is a deterministic re-derivation of the source bytes and the published text layer -- identical across every run of this version that reaches the stage successfully. Addressing it by run would ask a caller to already hold a run identity to read a fact about the document.\n\nAn unknown `version_uid` is `404`. A version that exists but whose most recent successful run never reached `page_geometry_extraction` -- or has no run at all -- answers `200` with `status: \"not_produced\"` and `blocks: []`; that is a different fact from a version whose geometry was produced and genuinely has no blocks, which answers `200` with `status: \"produced\"` and `blocks: []`. The two are the same bytes for `blocks` and different bytes for `status`, and a caller reads `status` to tell them apart -- never the length of `blocks`.\n\nThis operation does not carry crops. `page_geometry_extraction` also publishes a page-crop manifest, but this pipeline renders no visual detection and that manifest is unconditionally empty (`crop_policy: \"none\"`, `crops: []`); it is a separate artifact and is not exposed on this operation at all, rather than being shipped here as a field that is always `[]`.",
        "parameters": [
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "The block index for this version, or its not-yet-produced status.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VersionBlockIndex"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/documents/{document_uid}/versions": {
      "parameters": [
        {
          "$ref": "#/components/parameters/DocumentUid"
        }
      ],
      "get": {
        "operationId": "listVersions",
        "tags": [
          "documents"
        ],
        "summary": "List the published versions of one document, newest first.",
        "description": "Every immutable version of one document, ordered by `version_ordinal` descending. The ordinal is a display and ordering value and is never an identity; the continuation token carries the opaque `version_uid` and nothing else.\n\nAn unknown document is `404`, never an empty page.",
        "parameters": [
          {
            "$ref": "#/components/parameters/Cursor"
          },
          {
            "$ref": "#/components/parameters/Limit"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of this document's published versions.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DocumentVersionPage"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/runs": {
      "post": {
        "operationId": "startRun",
        "tags": [
          "runs"
        ],
        "summary": "Start a text-consistency run over one published version.",
        "description": "PD-03, in the subset PC-01 exercises. A new idempotency key creates a run. The same key with the same payload returns the existing run and creates nothing, whatever state that run is in, terminal included. The same key with a different payload returns `idempotency_key_reuse` and creates nothing. A new key over a terminal run creates a new run and leaves the terminal one exactly as it was: a terminal run is never reopened.",
        "parameters": [
          {
            "$ref": "#/components/parameters/IdempotencyKey"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StartRunRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "The run exists. A replay under the same key and payload returns the same run and this same status.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunStatus"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/IdempotencyConflict"
          },
          "422": {
            "$ref": "#/components/responses/RunInputInvalid"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/runs/{run_id}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/RunId"
        }
      ],
      "get": {
        "operationId": "getRunStatus",
        "tags": [
          "runs"
        ],
        "summary": "Read run state and per-stage state.",
        "description": "The polling target. `state` is the contract run state; `stages` carries one entry per stage the run scheduled, with the `StageResult` status vocabulary. A client stops polling on any terminal state.",
        "parameters": [
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "Current run state.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunStatus"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/runs/{run_id}/findings": {
      "parameters": [
        {
          "$ref": "#/components/parameters/RunId"
        }
      ],
      "get": {
        "operationId": "listRunFindings",
        "tags": [
          "findings"
        ],
        "summary": "List the published findings of one run, with their evidence.",
        "description": "Published means grounded: every listed finding's quotations were verified present at their declared anchors. An ungrounded model item is not a finding, does not appear here and is retained only as run diagnostics.",
        "parameters": [
          {
            "$ref": "#/components/parameters/Cursor"
          },
          {
            "$ref": "#/components/parameters/Limit"
          },
          {
            "$ref": "#/components/parameters/CategoryFilter"
          },
          {
            "$ref": "#/components/parameters/VerdictFilter"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of findings.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FindingPage"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/findings/{finding_uid}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/FindingUid"
        }
      ],
      "get": {
        "operationId": "getFinding",
        "tags": [
          "findings"
        ],
        "summary": "Read one finding with its observation, evidence and provenance.",
        "parameters": [
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "The finding detail.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FindingDetail"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/findings/{finding_uid}/decisions": {
      "parameters": [
        {
          "$ref": "#/components/parameters/FindingUid"
        }
      ],
      "post": {
        "operationId": "appendDecision",
        "tags": [
          "decisions"
        ],
        "summary": "Append one expert decision event.",
        "description": "Append-only. Nothing is updated: an accept after a reject is a third event, and the current verdict is a projection over the stream. Replaying under one idempotency key appends exactly one event, which the database enforces rather than the handler promising it.\n\nPC-01 emits `accept`, `reject` and `comment`. `revoke` is declared so the PD-01 revocation semantics stay implementable without a schema change; no PC-01 client offers it.",
        "parameters": [
          {
            "$ref": "#/components/parameters/IdempotencyKey"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AppendDecisionRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The appended event, and the projection that now follows from the stream.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AppendDecisionResponse"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/IdempotencyConflict"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      },
      "get": {
        "operationId": "listDecisionHistory",
        "tags": [
          "decisions"
        ],
        "summary": "Read the decision history of one finding, oldest first.",
        "description": "Ordered by `(recorded_at, decision_id)`, which is a total order and is stable across pages. The server's row sequence is never exposed, in a field or inside a cursor.",
        "parameters": [
          {
            "$ref": "#/components/parameters/Cursor"
          },
          {
            "$ref": "#/components/parameters/Limit"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of decision events.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DecisionEventPage"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/decisions": {
      "get": {
        "operationId": "listDecisions",
        "tags": [
          "decisions"
        ],
        "summary": "Read the decision journal across findings, newest first.",
        "description": "Every expert decision this deployment has recorded, in one listing, with the finding context each event was recorded against. `listDecisionHistory` answers *what was decided about this finding*; this answers *what has been decided*, which no operation could answer before and which a client could otherwise reach only by walking every run and every finding.\n\n**It is a projection and creates nothing.** `ADR-0012` holds: the current verdict, the reason aggregates and the knowledge-base views are rebuildable projections over the append-only ledger. Every property of `DecisionRecord` is rebuildable from `expert_decision_event`, `finding`, `finding_observation` and the `finding_current_verdict` projection, and this operation is the only place they are read together.\n\nOrdered by `(recorded_at, decision_id)` **descending** - the newest decision first, as `listProjects` orders projects - and that is a total order, stable across pages. The server's row sequence is never exposed, in a field or inside a cursor.\n\n`category` and `verdict` filter on the **finding**, exactly as they do on `listRunFindings`: the category of the finding the event was recorded against, and the verdict that now stands for it. Neither filters on the event. There is no parent identity in the path, so an empty journal is an empty page and never `not_found`.",
        "parameters": [
          {
            "$ref": "#/components/parameters/Cursor"
          },
          {
            "$ref": "#/components/parameters/Limit"
          },
          {
            "$ref": "#/components/parameters/CategoryFilter"
          },
          {
            "$ref": "#/components/parameters/VerdictFilter"
          },
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "One page of the decision journal.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DecisionRecordPage"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "422": {
            "$ref": "#/components/responses/ValidationFailed"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    },
    "/runs/{run_id}/export.csv": {
      "parameters": [
        {
          "$ref": "#/components/parameters/RunId"
        }
      ],
      "get": {
        "operationId": "exportRunCsv",
        "tags": [
          "export"
        ],
        "summary": "Download the CSV for one run.",
        "description": "Synchronous. Nothing is created, so there is no export resource, no export identity and nothing to poll. Repeating the request returns byte-identical bytes.\n\nThe discriminator is the frozen `terminal_semantics.publishes_result` flag, not a hand-written state list (`OD-11`). A run whose terminal declares `publishes_result: true` - `published` or `partial` - is exported, with the degraded state carried explicitly in the `run_state` column rather than as a silent empty file. Every other state is refused with `state_transition_not_allowed`: a non-terminal run, and the terminal `failed`. `cancelled` is likewise not exportable and is unreachable in PC-01. `partial_result_not_publishable` is never emitted by this surface.\n\nBytes: UTF-8 with a byte-order mark, comma delimiter, CRLF line ending, RFC 4180 quoting. The column list and its order are frozen in `docs/program/P02_SEAMS.md` section 6.",
        "parameters": [
          {
            "$ref": "#/components/parameters/CorrelationId"
          }
        ],
        "responses": {
          "200": {
            "description": "The CSV for this exact project, version and run.",
            "headers": {
              "X-Correlation-Id": {
                "$ref": "#/components/headers/CorrelationId"
              },
              "Content-Disposition": {
                "description": "Attachment with a display file name built from opaque identifiers. The file name is presentation only and is never an identity.",
                "schema": {
                  "type": "string"
                }
              }
            },
            "content": {
              "text/csv": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/AuthenticationRequired"
          },
          "403": {
            "$ref": "#/components/responses/PermissionDenied"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/RunNotExportable"
          },
          "500": {
            "$ref": "#/components/responses/InternalError"
          },
          "503": {
            "$ref": "#/components/responses/DependencyUnavailable"
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "description": "The authorization seam. Every operation in this document requires a bearer credential presented as `Authorization: Bearer <token>`, except `issueToken`, the credential exchange, whose own `security` is the empty requirement because it is what a caller with no credential uses to obtain one.\n\n**This declares the seam, not its implementation.** No issuer, no discovery URL, no flow, no token format, no role, subject or capability vocabulary appears here, and none may be added: the deployment decides all of them and the same operations must keep working when it does. `bearerFormat` is deliberately absent for that reason -- an opaque deployment token and an OIDC-issued JWT are both bearer credentials, and naming the format would pin the document to whichever one is current.\n\nA request with no credential, or one the deployment does not accept, is `401 authentication_required`; so is an exchange whose login and password the deployment does not accept, which is the same refusal and not a second one. A credential the deployment accepts, for a subject not permitted this operation on this resource, is `403 permission_denied`; authorization is decided server-side and the response never reveals the existence of a resource the caller may not see, so absence and refusal are reported as this surface's `404 not_found` where revealing them would leak.\n\nNothing outside `/api/v1` is covered. The operational liveness and readiness plane answers on its own port, carries no product meaning and needs no credential."
      }
    },
    "parameters": {
      "IdempotencyKey": {
        "name": "Idempotency-Key",
        "in": "header",
        "required": true,
        "description": "Client-supplied command key. Passed through to the owning command handler unchanged and never re-derived in the router. Never returned in a response body and never placed in an error envelope: it is a forbidden detail key.",
        "schema": {
          "$ref": "#/components/schemas/IdempotencyKey"
        }
      },
      "CorrelationId": {
        "name": "X-Correlation-Id",
        "in": "header",
        "required": false,
        "description": "Correlates this request with its diagnostic record. The edge assigns one when the caller does not supply it.",
        "schema": {
          "$ref": "#/components/schemas/CorrelationId"
        }
      },
      "Cursor": {
        "name": "cursor",
        "in": "query",
        "required": false,
        "description": "Opaque continuation token from the previous page's `next_cursor`. Never parsed by a client and never constructed by one.",
        "schema": {
          "$ref": "#/components/schemas/Cursor"
        }
      },
      "Limit": {
        "name": "limit",
        "in": "query",
        "required": false,
        "description": "Page size.",
        "schema": {
          "type": "integer",
          "minimum": 1,
          "maximum": 200,
          "default": 50
        }
      },
      "CategoryFilter": {
        "name": "category",
        "in": "query",
        "required": false,
        "description": "Restrict the page to one finding category.",
        "schema": {
          "$ref": "#/components/schemas/FindingCategory"
        }
      },
      "VerdictFilter": {
        "name": "verdict",
        "in": "query",
        "required": false,
        "description": "Restrict the page to findings whose current projected verdict is this value.",
        "schema": {
          "$ref": "#/components/schemas/Verdict"
        }
      },
      "ProjectUid": {
        "name": "project_uid",
        "in": "path",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/ProjectUid"
        }
      },
      "DocumentUid": {
        "name": "document_uid",
        "in": "path",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/DocumentUid"
        }
      },
      "VersionUid": {
        "name": "version_uid",
        "in": "path",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/VersionUid"
        }
      },
      "RunId": {
        "name": "run_id",
        "in": "path",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/RunId"
        }
      },
      "FindingUid": {
        "name": "finding_uid",
        "in": "path",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/FindingUid"
        }
      }
    },
    "headers": {
      "CorrelationId": {
        "description": "Present on every response, success or failure, so an operator can find the diagnostic record without the response carrying unsafe content.",
        "required": true,
        "schema": {
          "$ref": "#/components/schemas/CorrelationId"
        }
      }
    },
    "responses": {
      "AuthenticationRequired": {
        "description": "`authentication_required`. No valid authenticated subject was presented: the request carried no bearer credential, or one this deployment does not accept, or -- on the credential exchange, which presents no credential by design -- a login and password pair this deployment does not accept. The response carries no hint about the addressed resource, so it is the same answer whether or not the resource exists.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "PermissionDenied": {
        "description": "`permission_denied`. The authenticated subject is not permitted to perform this operation on this resource. Authorization is decided server-side. This is the caller's rights and nothing else: a dependency that refuses the *application's* own credential is `dependency_credential_refused` on the 500 response, which is a server fault and not this one.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "NotFound": {
        "description": "The addressed aggregate does not exist or is not visible. The response never reveals the existence of a resource the caller may not see.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "ValidationFailed": {
        "description": "The request or payload violates a declared schema, enum, format or invariant. Nothing was created or changed.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "UploadRejected": {
        "description": "The upload failed the PC-01 input envelope (`validation_failed`) or its bytes did not match their declared checksum or size (`storage_integrity_error`). In both cases nothing was published: no version row, no manifest and no readable object.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "StorageIntegrityError": {
        "description": "Stored bytes do not match their declared identity.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "RunInputInvalid": {
        "description": "The declared analysis inputs are not structurally acceptable (`analysis_input_invalid`) or the request itself is invalid (`validation_failed`).",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "IdempotencyConflict": {
        "description": "One of: `idempotency_key_reuse`, the same key with a different payload, which creates and changes nothing; `idempotency_key_in_progress`, the same key and payload still executing, which the caller retries under the same key and never re-issues under a new one; `idempotency_key_stale`, the recorded outcome is no longer available and is not guessed; or `conflict` for a uniqueness invariant with no more specific code.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "RunNotExportable": {
        "description": "`state_transition_not_allowed`. The run's terminal does not publish a result - a non-terminal run, or the terminal `failed` - so there is no download. A `partial` run is exported and is never refused here.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "InternalError": {
        "description": "An unclassified server fault, a declared run failure, or `dependency_credential_refused` -- a dependency refusing the application's own credential, which is a server-side configuration fault with no authenticated subject in it. The original internal code stays in protected diagnostics and never reaches this envelope.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      },
      "DependencyUnavailable": {
        "description": "`dependency_unavailable`, or since `R-8` `staged_upload_lost` on a write that stages bytes. The caller retries; nothing was partially applied. `idempotency_key_in_progress` is also retryable and is reachable on every write, so a caller reads `retryable` from the envelope, where the catalog pins it per code, and never infers it from the status or from a description like this one.",
        "headers": {
          "X-Correlation-Id": {
            "$ref": "#/components/headers/CorrelationId"
          }
        },
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      }
    },
    "schemas": {
      "ProjectUid": {
        "type": "string",
        "description": "Opaque project identity.",
        "pattern": "^prj_[0-9A-HJKMNP-TV-Z]{26}$",
        "examples": [
          "prj_01M2545JSD15ETSNNV904X991F"
        ]
      },
      "DocumentUid": {
        "type": "string",
        "pattern": "^doc_[0-9A-HJKMNP-TV-Z]{26}$",
        "examples": [
          "doc_01M2545JSD15ETSNNV904X991G"
        ]
      },
      "VersionUid": {
        "type": "string",
        "pattern": "^ver_[0-9A-HJKMNP-TV-Z]{26}$",
        "examples": [
          "ver_01M2545JSD15ETSNNV904X991J"
        ]
      },
      "RunId": {
        "type": "string",
        "pattern": "^run_[0-9A-HJKMNP-TV-Z]{26}$",
        "examples": [
          "run_01M2545JSD15ETSNNV904X991K"
        ]
      },
      "FindingUid": {
        "type": "string",
        "description": "Durable semantic issue identity, carrying expert history across reruns.",
        "pattern": "^fnd_[0-9A-HJKMNP-TV-Z]{26}$",
        "examples": [
          "fnd_01M2545JSD15ETSNNV904X991M"
        ]
      },
      "FindingObservationId": {
        "type": "string",
        "description": "Immutable evidence emitted by exactly one run.",
        "pattern": "^fobs_[0-9A-HJKMNP-TV-Z]{26}$",
        "examples": [
          "fobs_01M2545JSD15ETSNNV904X991N"
        ]
      },
      "DecisionId": {
        "type": "string",
        "pattern": "^dec_[0-9A-HJKMNP-TV-Z]{26}$",
        "examples": [
          "dec_01M2545JSD15ETSNNV904X991P"
        ]
      },
      "AnalysisProfileId": {
        "type": "string",
        "pattern": "^ap_[0-9A-HJKMNP-TV-Z]{26}$"
      },
      "PromptBundleId": {
        "type": "string",
        "pattern": "^pb_[0-9A-HJKMNP-TV-Z]{26}$"
      },
      "ModelCallId": {
        "type": "string",
        "pattern": "^mc_[0-9A-HJKMNP-TV-Z]{26}$"
      },
      "CorrelationId": {
        "type": "string",
        "description": "Not an entity identity. Never a foreign key and authorizes nothing.",
        "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$",
        "minLength": 1,
        "maxLength": 128
      },
      "CostBasis": {
        "type": "string",
        "description": "How well a published cost figure is known. `measured` means every provider call the figure sums reported its own cost; `estimated` means at least one did not, so the total is derived. The value is an aggregate over **every** call the figure spans and is never the last call's basis: a run whose first attempt replayed and whose second reported a cost is `estimated`.",
        "enum": [
          "measured",
          "estimated"
        ]
      },
      "IdempotencyKey": {
        "type": "string",
        "description": "Not an entity identity. Never interpreted as, converted into or stored as a system identifier.",
        "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$",
        "minLength": 1,
        "maxLength": 128
      },
      "Sha256": {
        "type": "string",
        "description": "A verification value, never an identity.",
        "pattern": "^[0-9a-f]{64}$"
      },
      "Cursor": {
        "type": "string",
        "description": "Opaque pagination token. It encodes the sort key of the last item on the page and nothing else; it is never a database row number or sequence value.",
        "minLength": 1,
        "maxLength": 512
      },
      "PageInfo": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "next_cursor"
        ],
        "properties": {
          "next_cursor": {
            "description": "Token for the next page, or null when this is the last page.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/Cursor"
              },
              {
                "type": "null"
              }
            ]
          }
        }
      },
      "ErrorCode": {
        "type": "string",
        "description": "Exactly the key set of contracts/domain/v1/error-codes.json.",
        "enum": [
          "validation_failed",
          "not_found",
          "authentication_required",
          "permission_denied",
          "conflict",
          "state_transition_not_allowed",
          "idempotency_key_reuse",
          "idempotency_key_in_progress",
          "idempotency_key_stale",
          "unsupported_contract_version",
          "storage_integrity_error",
          "dependency_unavailable",
          "dependency_credential_refused",
          "staged_upload_lost",
          "required_norm_unavailable",
          "analysis_input_invalid",
          "analysis_failed",
          "partial_result_not_publishable",
          "cost_budget_exceeded",
          "stale_attempt",
          "execution_token_invalid",
          "internal_error"
        ]
      },
      "ErrorEnvelope": {
        "type": "object",
        "description": "The single externally visible failure shape, mirroring urn:auditmanager:domain:error-envelope:v1. `retryable` is pinned to the catalog value for the reported code; a caller never infers it from the HTTP status or the message.",
        "additionalProperties": false,
        "required": [
          "contract_version",
          "error_code",
          "message",
          "correlation_id",
          "retryable"
        ],
        "properties": {
          "contract_version": {
            "const": "1.0.0-draft.1"
          },
          "error_code": {
            "$ref": "#/components/schemas/ErrorCode"
          },
          "message": {
            "type": "string",
            "description": "Stable caller-safe sentence. No path, object key, URL, credential, token, prompt, payload, query or stack content.",
            "minLength": 1,
            "maxLength": 512
          },
          "correlation_id": {
            "$ref": "#/components/schemas/CorrelationId"
          },
          "retryable": {
            "type": "boolean"
          },
          "details": {
            "type": "object",
            "description": "Safe scalar classifiers only, restricted to the safe_detail_keys the catalog declares for the reported code. The catalog's forbidden key set is rejected.",
            "maxProperties": 16,
            "propertyNames": {
              "pattern": "^[a-z][a-z0-9_]{0,63}$"
            },
            "additionalProperties": {
              "type": [
                "string",
                "number",
                "integer",
                "boolean",
                "null"
              ],
              "maxLength": 256
            }
          }
        }
      },
      "RunState": {
        "type": "string",
        "description": "The audit_run states of contracts/domain/v1/state-machines.json. The success terminal is `published`; there is no `succeeded` run state. `created` is transient and rarely observed by a client. `cancelled` is declared by the contract and is unreachable in PC-01, which has no cancel command.",
        "enum": [
          "created",
          "queued",
          "running",
          "validating",
          "published",
          "partial",
          "failed",
          "cancelled"
        ]
      },
      "StageStatus": {
        "type": "string",
        "description": "StageResult statuses from contracts/analysis/v1. A `succeeded` stage carries no error; every other status carries one.",
        "enum": [
          "succeeded",
          "partial",
          "failed",
          "skipped"
        ]
      },
      "StageId": {
        "type": "string",
        "description": "A canonical stage identity from contracts/analysis/v1/stage-registry.json. PC-01 schedules the first four.",
        "enum": [
          "source_preparation",
          "page_geometry_extraction",
          "document_context_build",
          "text_analysis",
          "block_analysis",
          "finding_merge",
          "finding_review",
          "finding_correction",
          "norm_verification"
        ]
      },
      "ProviderMode": {
        "type": "string",
        "description": "Whether the model call was made live or replayed from a recording. Visible in the UI and in the CSV, because a recorded run is not evidence of a live one.",
        "enum": [
          "live",
          "recorded"
        ]
      },
      "FindingCategory": {
        "type": "string",
        "description": "The only two questions PC-01 answers.",
        "enum": [
          "internal_contradiction",
          "explicit_placeholder"
        ]
      },
      "Verdict": {
        "type": "string",
        "description": "The current projected verdict. `pending` when the ledger holds no verdict-bearing event, and after a revocation. `needs_manual_review` is declared for the closed union and has no PC-01 producer.",
        "enum": [
          "pending",
          "accepted",
          "rejected",
          "needs_manual_review"
        ]
      },
      "DecisionEventType": {
        "type": "string",
        "description": "PC-01 emits accept, reject and comment. `revoke` is declared so PD-01 revocation stays implementable without a schema change.",
        "enum": [
          "accept",
          "reject",
          "comment",
          "revoke"
        ]
      },
      "CreateProjectRequest": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Display label. Not unique and not an identity.",
            "minLength": 1,
            "maxLength": 200
          }
        }
      },
      "Project": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "project_uid",
          "name",
          "created_at"
        ],
        "properties": {
          "project_uid": {
            "$ref": "#/components/schemas/ProjectUid"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "document_count": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "ProjectPage": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "items",
          "page"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Project"
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "UploadDocumentRequest": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "file"
        ],
        "properties": {
          "file": {
            "type": "string",
            "format": "binary",
            "description": "One unencrypted PDF, at most 25 MiB and at most 30 pages, every page carrying extractable embedded text."
          },
          "display_title": {
            "type": "string",
            "description": "Optional display label. When absent the server derives one for presentation. Neither this nor the uploaded file name is ever an identity.",
            "minLength": 1,
            "maxLength": 400
          }
        }
      },
      "InputManifestEntry": {
        "type": "object",
        "description": "One role of the immutable input manifest. It carries a blob_id and a checksum and never an object key, bucket or URL.",
        "additionalProperties": false,
        "required": [
          "role",
          "sha256",
          "size_bytes",
          "media_type"
        ],
        "properties": {
          "role": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)*$"
          },
          "sha256": {
            "$ref": "#/components/schemas/Sha256"
          },
          "size_bytes": {
            "type": "integer",
            "minimum": 0
          },
          "media_type": {
            "type": "string"
          }
        }
      },
      "DocumentVersion": {
        "type": "object",
        "description": "A published, immutable input state. There is no endpoint that modifies one.",
        "additionalProperties": false,
        "required": [
          "version_uid",
          "document_uid",
          "project_uid",
          "version_ordinal",
          "media_type",
          "byte_size",
          "sha256",
          "page_count",
          "published_at",
          "input_manifest"
        ],
        "properties": {
          "version_uid": {
            "$ref": "#/components/schemas/VersionUid"
          },
          "document_uid": {
            "$ref": "#/components/schemas/DocumentUid"
          },
          "project_uid": {
            "$ref": "#/components/schemas/ProjectUid"
          },
          "version_ordinal": {
            "type": "integer",
            "minimum": 1,
            "description": "Display and ordering value only. Never an identity and never accepted as a path parameter."
          },
          "display_title": {
            "type": "string",
            "minLength": 1,
            "maxLength": 400
          },
          "source_filename": {
            "description": "The uploaded file name, for display. Never an identity.",
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          },
          "media_type": {
            "const": "application/pdf"
          },
          "byte_size": {
            "type": "integer",
            "minimum": 0
          },
          "sha256": {
            "$ref": "#/components/schemas/Sha256"
          },
          "page_count": {
            "type": "integer",
            "minimum": 1,
            "maximum": 30
          },
          "published_at": {
            "type": "string",
            "format": "date-time"
          },
          "input_manifest": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/InputManifestEntry"
            }
          }
        }
      },
      "DocumentVersionPage": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "items",
          "page"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DocumentVersion"
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "BlockGeometry": {
        "type": "object",
        "description": "One derived block, one extracted text line. `block_id` is an anchor inside this artifact -- it matches `Evidence.block_id`'s pattern -- and is never a contract identifier.",
        "additionalProperties": false,
        "required": [
          "block_id",
          "page_number",
          "block_ordinal",
          "bbox",
          "bbox_unit",
          "bbox_origin",
          "char_start",
          "char_end"
        ],
        "properties": {
          "block_id": {
            "type": "string",
            "pattern": "^b_[0-9]{6}$"
          },
          "page_number": {
            "type": "integer",
            "minimum": 1
          },
          "block_ordinal": {
            "type": "integer",
            "minimum": 0,
            "description": "Position of this block within its page, starting at 0."
          },
          "bbox": {
            "type": "object",
            "description": "Points, top-left origin, y increasing downward -- see `bbox_unit` and `bbox_origin` beside it.",
            "additionalProperties": false,
            "required": [
              "x0",
              "y0",
              "x1",
              "y1"
            ],
            "properties": {
              "x0": {
                "type": "number"
              },
              "y0": {
                "type": "number"
              },
              "x1": {
                "type": "number"
              },
              "y1": {
                "type": "number"
              }
            }
          },
          "bbox_unit": {
            "const": "pt"
          },
          "bbox_origin": {
            "const": "top_left"
          },
          "char_start": {
            "type": "integer",
            "minimum": 0,
            "description": "Document-global offset into the version's prepared text layer, counted in Unicode code points -- the same sequence `Evidence.char_start` indexes."
          },
          "char_end": {
            "type": "integer",
            "minimum": 1
          }
        }
      },
      "VersionBlockIndex": {
        "type": "object",
        "description": "The page-by-page block markup derived for one version. Carries no crops: `page_geometry_extraction` also publishes a page-crop manifest, and this pipeline renders no visual detection, so that manifest is unconditionally empty and is a separate artifact this operation does not expose.",
        "additionalProperties": false,
        "required": [
          "version_uid",
          "status",
          "produced_by_run_id",
          "text_layer_sha256",
          "block_count",
          "blocks"
        ],
        "properties": {
          "version_uid": {
            "$ref": "#/components/schemas/VersionUid"
          },
          "status": {
            "type": "string",
            "description": "`produced`: a run reached `page_geometry_extraction` and this is its output, `blocks` may still be empty for a version that genuinely has none. `not_produced`: no run of this version has published this artifact yet -- absent, not empty -- and `blocks` is `[]` for that reason instead.",
            "enum": [
              "produced",
              "not_produced"
            ]
          },
          "produced_by_run_id": {
            "description": "The run whose `page_geometry_extraction` result this is. Null exactly when `status` is `not_produced`.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/RunId"
              },
              {
                "type": "null"
              }
            ]
          },
          "text_layer_sha256": {
            "description": "Binds this block index to the exact prepared text layer its spans index. Null exactly when `status` is `not_produced`.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/Sha256"
              },
              {
                "type": "null"
              }
            ]
          },
          "block_count": {
            "type": "integer",
            "minimum": 0,
            "description": "`len(blocks)`, carried rather than left for a caller to recompute."
          },
          "blocks": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/BlockGeometry"
            }
          }
        }
      },
      "StartRunRequest": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "version_uid"
        ],
        "properties": {
          "version_uid": {
            "$ref": "#/components/schemas/VersionUid"
          },
          "provider_mode": {
            "$ref": "#/components/schemas/ProviderMode",
            "description": "Defaults to the server's configured mode. A client asking for `live` is asking to spend money against the OD-03 ceiling."
          }
        }
      },
      "StageState": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "stage_id",
          "status"
        ],
        "properties": {
          "stage_id": {
            "$ref": "#/components/schemas/StageId"
          },
          "stage_version": {
            "type": "string",
            "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
          },
          "status": {
            "$ref": "#/components/schemas/StageStatus"
          },
          "error_code": {
            "description": "The typed reason a non-succeeded stage carries. Null exactly when the status is `succeeded`.",
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          },
          "started_at": {
            "oneOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ]
          },
          "finished_at": {
            "oneOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ]
          }
        }
      },
      "RunStatus": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "run_id",
          "project_uid",
          "version_uid",
          "state",
          "provider_mode",
          "stages",
          "created_at"
        ],
        "properties": {
          "run_id": {
            "$ref": "#/components/schemas/RunId"
          },
          "project_uid": {
            "$ref": "#/components/schemas/ProjectUid"
          },
          "version_uid": {
            "$ref": "#/components/schemas/VersionUid"
          },
          "state": {
            "$ref": "#/components/schemas/RunState"
          },
          "provider_mode": {
            "$ref": "#/components/schemas/ProviderMode"
          },
          "analysis_profile_id": {
            "$ref": "#/components/schemas/AnalysisProfileId"
          },
          "prompt_bundle_id": {
            "$ref": "#/components/schemas/PromptBundleId"
          },
          "stages": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StageState"
            }
          },
          "degradation_set": {
            "type": "array",
            "description": "The explicitly recorded missing or degraded stage set. Non-empty exactly when the run is `partial`; a `published` run carries none.",
            "items": {
              "$ref": "#/components/schemas/StageId"
            }
          },
          "terminal_reason": {
            "description": "The catalog code a `failed` run terminated with. Null otherwise.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/ErrorCode"
              },
              {
                "type": "null"
              }
            ]
          },
          "terminal_detail": {
            "type": "object",
            "description": "D-46: safe scalar classifiers saying WHICH dependency a `failed` run terminated on, restricted to the `safe_detail_keys` the domain catalog declares for the code in `terminal_reason`. Present only beside a `terminal_reason`, because the allowlist that bounds these keys is a property of the reported code; absent when there is nothing to say, and never an empty object. The same shape and the same rule as `ErrorEnvelope.details`, applied to a 200 run reading rather than to a failure -- which is why it had to be added at all: a run reading is not an error envelope and carried no detail object. It classifies; it never identifies. No path, object key, URL, credential, token, prompt, payload or host appears here, and no error code was added to the catalog for it.",
            "maxProperties": 16,
            "propertyNames": {
              "pattern": "^[a-z][a-z0-9_]{0,63}$"
            },
            "additionalProperties": {
              "type": [
                "string",
                "number",
                "integer",
                "boolean",
                "null"
              ],
              "maxLength": 256
            }
          },
          "interrupted_reason": {
            "description": "OD-10: a run left `running` by a crash is reconciled to `failed` with an explicit interrupted reason. There is no `interrupted` state.",
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          },
          "published_finding_count": {
            "type": "integer",
            "minimum": 0
          },
          "diagnostic_observation_count": {
            "type": "integer",
            "minimum": 0,
            "description": "Ungrounded model items rejected by the grounding gate. They are not findings and appear in no finding list."
          },
          "cost_micros": {
            "type": "integer",
            "minimum": 0,
            "description": "What this run spent at the provider, in millionths of the provider currency unit, as an integer. Never a floating-point money value. It is the sum of `cost_micros` over **every** `model_call` row of this run, including every retry attempt, because the per-run budget ceiling is enforced over the same span. Absent when the run made no provider call at all, which is a different fact from a cost of zero."
          },
          "cost_basis": {
            "$ref": "#/components/schemas/CostBasis"
          },
          "model_call_count": {
            "type": "integer",
            "minimum": 0,
            "description": "How many provider calls `cost_micros` sums. Published rather than left to be inferred: a reader who cannot see it cannot tell a run answered first time from one that was retried, and the two spent differently. Present exactly when `cost_micros` is."
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "terminal_at": {
            "oneOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ]
          }
        }
      },
      "RunStatusPage": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "items",
          "page"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RunStatus"
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "Evidence": {
        "type": "object",
        "description": "One verified quotation. `char_start` and `char_end` index the document-global character sequence of the prepared text layer, counted in Unicode code points after the declared normalization - see docs/program/P02_SEAMS.md section 4.",
        "additionalProperties": false,
        "required": [
          "evidence_ordinal",
          "page_number",
          "quote",
          "char_start",
          "char_end"
        ],
        "properties": {
          "evidence_ordinal": {
            "type": "integer",
            "minimum": 0
          },
          "page_number": {
            "type": "integer",
            "minimum": 1
          },
          "quote": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000
          },
          "char_start": {
            "type": "integer",
            "minimum": 0
          },
          "char_end": {
            "type": "integer",
            "minimum": 1
          },
          "block_id": {
            "description": "Secondary anchor into this version's block index. Not a contract identifier.",
            "oneOf": [
              {
                "type": "string",
                "pattern": "^b_[0-9]{6}$"
              },
              {
                "type": "null"
              }
            ]
          }
        }
      },
      "ObservationProvenance": {
        "type": "object",
        "description": "What produced this observation. Checksums and identities only: never the prompt, the request or the response body.",
        "additionalProperties": false,
        "required": [
          "stage_id",
          "analysis_profile_id",
          "prompt_bundle_id",
          "provider_mode"
        ],
        "properties": {
          "stage_id": {
            "$ref": "#/components/schemas/StageId"
          },
          "analysis_profile_id": {
            "$ref": "#/components/schemas/AnalysisProfileId"
          },
          "prompt_bundle_id": {
            "$ref": "#/components/schemas/PromptBundleId"
          },
          "provider_mode": {
            "$ref": "#/components/schemas/ProviderMode"
          },
          "model_call_id": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/ModelCallId"
              },
              {
                "type": "null"
              }
            ]
          },
          "model_identity": {
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          }
        }
      },
      "FindingObservation": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "finding_observation_id",
          "run_id",
          "category",
          "finding_text",
          "recommendation_text",
          "evidence",
          "provenance"
        ],
        "properties": {
          "finding_observation_id": {
            "$ref": "#/components/schemas/FindingObservationId"
          },
          "run_id": {
            "$ref": "#/components/schemas/RunId"
          },
          "category": {
            "$ref": "#/components/schemas/FindingCategory"
          },
          "finding_text": {
            "type": "string",
            "minLength": 1,
            "maxLength": 4000
          },
          "recommendation_text": {
            "type": "string",
            "minLength": 1,
            "maxLength": 4000
          },
          "evidence": {
            "type": "array",
            "minItems": 1,
            "items": {
              "$ref": "#/components/schemas/Evidence"
            }
          },
          "provenance": {
            "$ref": "#/components/schemas/ObservationProvenance"
          }
        }
      },
      "Finding": {
        "type": "object",
        "description": "A published finding. Every listed finding is grounded: its quotations were verified present at their declared anchors.",
        "additionalProperties": false,
        "required": [
          "finding_uid",
          "project_uid",
          "version_uid",
          "run_id",
          "category",
          "observation",
          "current_verdict"
        ],
        "properties": {
          "finding_uid": {
            "$ref": "#/components/schemas/FindingUid"
          },
          "project_uid": {
            "$ref": "#/components/schemas/ProjectUid"
          },
          "version_uid": {
            "$ref": "#/components/schemas/VersionUid"
          },
          "run_id": {
            "$ref": "#/components/schemas/RunId"
          },
          "category": {
            "$ref": "#/components/schemas/FindingCategory"
          },
          "observation": {
            "$ref": "#/components/schemas/FindingObservation"
          },
          "current_verdict": {
            "$ref": "#/components/schemas/Verdict"
          },
          "latest_decision_id": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/DecisionId"
              },
              {
                "type": "null"
              }
            ]
          },
          "decision_recorded_at": {
            "oneOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ]
          }
        }
      },
      "FindingDetail": {
        "type": "object",
        "description": "One finding with its full current-verdict projection. Restates `Finding` rather than composing it with `allOf`: under JSON Schema 2020-12 an `additionalProperties: false` is evaluated against its own schema object's property annotations only, so an `allOf` branch over the closed `Finding` would reject the two properties the sibling branch adds. Both shapes stay closed, and tests/contract/api_v1 asserts this property set stays `Finding`'s plus exactly `latest_comment` and `decision_event_count`.",
        "additionalProperties": false,
        "required": [
          "finding_uid",
          "project_uid",
          "version_uid",
          "run_id",
          "category",
          "observation",
          "current_verdict"
        ],
        "properties": {
          "finding_uid": {
            "$ref": "#/components/schemas/FindingUid"
          },
          "project_uid": {
            "$ref": "#/components/schemas/ProjectUid"
          },
          "version_uid": {
            "$ref": "#/components/schemas/VersionUid"
          },
          "run_id": {
            "$ref": "#/components/schemas/RunId"
          },
          "category": {
            "$ref": "#/components/schemas/FindingCategory"
          },
          "observation": {
            "$ref": "#/components/schemas/FindingObservation"
          },
          "current_verdict": {
            "$ref": "#/components/schemas/Verdict"
          },
          "latest_decision_id": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/DecisionId"
              },
              {
                "type": "null"
              }
            ]
          },
          "decision_recorded_at": {
            "oneOf": [
              {
                "type": "string",
                "format": "date-time"
              },
              {
                "type": "null"
              }
            ]
          },
          "latest_comment": {
            "description": "The comment of the most recent event carrying one, whatever its type.",
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          },
          "decision_event_count": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "FindingPage": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "items",
          "page"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Finding"
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "AppendDecisionRequest": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "event_type",
          "finding_observation_id"
        ],
        "properties": {
          "event_type": {
            "$ref": "#/components/schemas/DecisionEventType"
          },
          "finding_observation_id": {
            "$ref": "#/components/schemas/FindingObservationId",
            "description": "The observation the expert actually reviewed. A verdict is recorded against the evidence in front of the reviewer, not against whatever the newest run later produced."
          },
          "comment": {
            "description": "Required for `comment`; optional on any other event type.",
            "oneOf": [
              {
                "type": "string",
                "minLength": 1,
                "maxLength": 4000
              },
              {
                "type": "null"
              }
            ]
          }
        }
      },
      "DecisionEvent": {
        "type": "object",
        "description": "One appended event. Never updated and never removed.",
        "additionalProperties": false,
        "required": [
          "decision_id",
          "finding_uid",
          "finding_observation_id",
          "event_type",
          "author_label",
          "recorded_at"
        ],
        "properties": {
          "decision_id": {
            "$ref": "#/components/schemas/DecisionId"
          },
          "finding_uid": {
            "$ref": "#/components/schemas/FindingUid"
          },
          "finding_observation_id": {
            "$ref": "#/components/schemas/FindingObservationId"
          },
          "event_type": {
            "$ref": "#/components/schemas/DecisionEventType"
          },
          "verdict": {
            "description": "The verdict this event carries, or null for a comment.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/Verdict"
              },
              {
                "type": "null"
              }
            ]
          },
          "comment": {
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          },
          "author_label": {
            "type": "string",
            "description": "OD-12, as `R-37` now defines it: the display name of the reviewer who recorded the event -- the name they chose, or their login when they have chosen none -- derived server-side from the authenticated credential and **never** taken from a request body. It IS a subject identity: it names which reviewer judged, which is what `P04` needs and what one configured label for every reviewer could not give it. It still authorizes nothing: it is what the ledger records about who judged, not a permission anybody holds, and no client can choose it. The ledger is append-only, so an event keeps the label it was written with even after that reviewer is renamed.",
            "minLength": 1,
            "maxLength": 128
          },
          "recorded_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "AppendDecisionResponse": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "event",
          "current_verdict"
        ],
        "properties": {
          "event": {
            "$ref": "#/components/schemas/DecisionEvent"
          },
          "current_verdict": {
            "$ref": "#/components/schemas/Verdict"
          }
        }
      },
      "DecisionEventPage": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "items",
          "page"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DecisionEvent"
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "DecisionRecord": {
        "type": "object",
        "description": "One event of the decision journal, with the finding context it was recorded against. A rebuildable projection and never a source of truth: every property here is derived from `expert_decision_event`, `finding`, `finding_observation` and the `finding_current_verdict` projection, and nothing is stored in this shape. It restates `DecisionEvent`'s properties rather than composing them with `allOf`, for the reason `FindingDetail` states: under JSON Schema 2020-12 an `additionalProperties: false` is evaluated against its own schema object's property annotations only, so an `allOf` branch over the closed `DecisionEvent` would reject the properties the sibling branch adds.",
        "additionalProperties": false,
        "required": [
          "decision_id",
          "finding_uid",
          "finding_observation_id",
          "event_type",
          "author_label",
          "recorded_at",
          "project_uid",
          "run_id",
          "category",
          "finding_text",
          "current_verdict",
          "decision_event_count"
        ],
        "properties": {
          "decision_id": {
            "$ref": "#/components/schemas/DecisionId"
          },
          "finding_uid": {
            "$ref": "#/components/schemas/FindingUid"
          },
          "finding_observation_id": {
            "$ref": "#/components/schemas/FindingObservationId"
          },
          "event_type": {
            "$ref": "#/components/schemas/DecisionEventType"
          },
          "verdict": {
            "description": "The verdict this event carries, or null for a comment.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/Verdict"
              },
              {
                "type": "null"
              }
            ]
          },
          "comment": {
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          },
          "author_label": {
            "type": "string",
            "description": "OD-12, as `R-37` now defines it: the display name of the reviewer who recorded the event -- the name they chose, or their login when they have chosen none -- derived server-side from the authenticated credential and **never** taken from a request body. It IS a subject identity: it names which reviewer judged, which is what `P04` needs and what one configured label for every reviewer could not give it. It still authorizes nothing: it is what the ledger records about who judged, not a permission anybody holds, and no client can choose it. The ledger is append-only, so an event keeps the label it was written with even after that reviewer is renamed.",
            "minLength": 1,
            "maxLength": 128
          },
          "recorded_at": {
            "type": "string",
            "format": "date-time"
          },
          "project_uid": {
            "$ref": "#/components/schemas/ProjectUid"
          },
          "run_id": {
            "$ref": "#/components/schemas/RunId"
          },
          "category": {
            "$ref": "#/components/schemas/FindingCategory"
          },
          "finding_text": {
            "type": "string",
            "description": "The observation's own words, as `FindingObservation.finding_text` carries them. It is the analysis's text and not this API's prose, so it is in whatever language the analysis produced.",
            "minLength": 1,
            "maxLength": 4000
          },
          "current_verdict": {
            "$ref": "#/components/schemas/Verdict"
          },
          "decision_event_count": {
            "type": "integer",
            "description": "How many events the ledger holds for this finding, including this one. A count greater than one means the expert returned to the finding.",
            "minimum": 0
          }
        }
      },
      "DecisionRecordPage": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "items",
          "page"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DecisionRecord"
            }
          },
          "page": {
            "$ref": "#/components/schemas/PageInfo"
          }
        }
      },
      "IssueTokenRequest": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "login",
          "password"
        ],
        "properties": {
          "login": {
            "type": "string",
            "description": "The caller's login, spelled the way the deployment spells it. Not an identity of this surface: it addresses nothing, appears in no path and is never a foreign key.",
            "minLength": 1,
            "maxLength": 320
          },
          "password": {
            "type": "string",
            "description": "The secret presented with the login. It appears in no response body, no error detail and no diagnostic record.",
            "minLength": 1,
            "maxLength": 1024
          }
        }
      },
      "IssueTokenResponse": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "token",
          "expires_in"
        ],
        "properties": {
          "token": {
            "type": "string",
            "description": "The credential, presented on the authorized operations as `Authorization: Bearer <token>`. Opaque: this document states no format for it, and a caller that reads anything out of it has taken a dependency this contract does not offer.",
            "minLength": 1
          },
          "expires_in": {
            "type": "integer",
            "description": "How long the credential stays valid, in seconds counted from this response. A lifetime and never a clock reading, so no clock has to agree between the two sides. A caller exchanges again before it elapses; an elapsed credential is `authentication_required` like any other the deployment does not accept.",
            "minimum": 1
          }
        }
      },
      "ChangePasswordRequest": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "current_password",
          "new_password"
        ],
        "properties": {
          "current_password": {
            "type": "string",
            "description": "The password the account holds now, proved before anything is changed. It appears in no response body, no error detail and no diagnostic record. A value that is not this account's is `authentication_required` -- the same refusal a missing credential gets, because 'this deployment does not accept this' is one fact.",
            "minLength": 1,
            "maxLength": 1024
          },
          "new_password": {
            "type": "string",
            "description": "The password the account will hold. The bounds are mechanical, not a policy: this surface declares no minimum length, no complexity rule, no history and no expiry. A value equal to `current_password` is `validation_failed`, because a change that changes nothing would report a password as changed when it was not.",
            "minLength": 1,
            "maxLength": 1024
          }
        }
      }
    }
  }
}
