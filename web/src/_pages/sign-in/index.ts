/**
 * Public API of the `sign-in` page slice.
 *
 * The route file at `web/src/app/login/page.tsx` delegates here, the same way every other
 * route delegates to its page slice.
 */

export type { SignInPageProps } from './ui/sign-in-page';
export { SignInPage } from './ui/sign-in-page';
