/**
 * Public API of the `change-password` page slice.
 *
 * The route file at `web/src/app/account/password/page.tsx` delegates here, the same way
 * every other route delegates to its page slice.
 */

export type { ChangePasswordPageProps } from './ui/change-password-page';
export { ChangePasswordPage } from './ui/change-password-page';
