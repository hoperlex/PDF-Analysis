/**
 * `/logs` — the route file for the execution-journal section.
 *
 * Delegation-only: the route decides nothing.
 */

import { LogsPage } from '@/_pages/logs';

export default function LogsRoute() {
  return <LogsPage />;
}
