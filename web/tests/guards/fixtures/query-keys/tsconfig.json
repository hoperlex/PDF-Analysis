{
  "//": "Compiles the `query-keys` fixtures, which `web/tsconfig.json` excludes along with every other guard fixture. `query-key-shape.guard.test.ts` runs `tsc -p` against this file and reads the diagnostics. `incremental` is off so that a run writes nothing into the tree: the battery's checkout guard fails a suite that leaves a file behind.",
  "extends": "../../../../tsconfig.json",
  "compilerOptions": {
    "incremental": false,
    "noEmit": true
  },
  "include": ["./*.ts"],
  "exclude": []
}
