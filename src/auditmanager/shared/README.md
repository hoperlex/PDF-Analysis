# `shared` boundary

Cross-cutting primitives only: typed IDs, checksum/version/error/clock/telemetry abstractions. No domain dumping ground.

Create subfolders/classes only when a real use case requires them. Keep internal APIs private by default.
