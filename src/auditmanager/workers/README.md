# `workers` boundary

Worker registry/capabilities/lease protocol adapters; remote execution never becomes metadata writer.

Create subfolders/classes only when a real use case requires them. Keep internal APIs private by default.
