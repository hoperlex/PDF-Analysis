# `api` boundary

FastAPI transport adapters only: validation, auth context, call application use case, map typed error. No domain logic.

Create subfolders/classes only when a real use case requires them. Keep internal APIs private by default.
