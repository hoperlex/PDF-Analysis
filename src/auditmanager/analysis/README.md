# `analysis` boundary

Execution-engine ports, stage registry/contracts, analysis profiles/prompt/norm/model-call provenance. Heavy execution may run outside API process.

Create subfolders/classes only when a real use case requires them. Keep internal APIs private by default.
