# `findings` boundary

Finding stable identity, run-specific FindingObservation, review read models and evidence links.

Create subfolders/classes only when a real use case requires them. Keep internal APIs private by default.
