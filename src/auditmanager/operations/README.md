# `operations` boundary

Durable audit, observability/redaction application concerns, retention/erasure jobs once policy is approved.

Create subfolders/classes only when a real use case requires them. Keep internal APIs private by default.
