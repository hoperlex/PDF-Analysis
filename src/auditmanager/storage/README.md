# `storage` boundary

Blob metadata/application port and S3 adapter. Hides object layout; temporary→verify→publish.

Create subfolders/classes only when a real use case requires them. Keep internal APIs private by default.
