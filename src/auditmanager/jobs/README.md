# `jobs` boundary

AuditRun/Job/Attempt, leases/fencing, outbox and orchestration state. Progress is projection.

Create subfolders/classes only when a real use case requires them. Keep internal APIs private by default.
