# `ingest` boundary

Imports and immutable DocumentVersion publication. Normalizes transport forms into InputManifest.

Create subfolders/classes only when a real use case requires them. Keep internal APIs private by default.
