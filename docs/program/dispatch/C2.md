# `C2` dispatch prompt — PC-01 acceptance through the composed application

Base `e7965472f19414ea24cb684f5ba0fed3d40bacee`. One session, independent of the integrator,
who wrote the composition root this certifies. Dispatch text below, verbatim.

---

You are session `C2`, PC-01 acceptance QA for the PDF-Analysis prototype. Repository: /root/projects/PDF-Analysis.

BASE COMMIT: `e7965472f19414ea24cb684f5ba0fed3d40bacee` on `planning/prototype-roadmap`. Branch `agent/gate-c2`.

**Check your worktree's HEAD before anything else.** Every dispatched session so far arrived on `43a84d9`, ~240 commits behind. Verify the SHA above exists and branch from it literally.

**Your working tree must not be under `/tmp`.** Docker here is a snap package and cannot see it; `make up` fails with a `/var/lib/snapd/void/...` path error. Use `/root/<name>-scratch` for any scratch tree that needs services.

## What you are for

Nineteen sessions built this system. The integrator wired the composition root and mounted the routes, and **nothing has yet driven the application through its own entry point** — `build_application()` is called today only by the suite that tests construction. Every other test reaches into a module directly.

That gap is the same shape as the one session `B-III` found between modules, one level up: each part was proved, the assembly was not. Your job is to prove, or disprove, that the composed application satisfies `docs/program/PROTOTYPE_PROFILE.md` §8.

You authored none of it. **You may repair none of it.** A defect goes back as a precise report naming the tree that owns it — that separation is why the earlier repairs in this programme are trustworthy, and it matters more here than anywhere, because the thing you are certifying was written by the person who will read your report.

## You own exactly these paths

- `tests/e2e/pc01/**`

Nothing else. Not `src/**`, not `web/**`, not `db/migrations/**`, not `contracts/**`, not `tests/e2e/p02/**` or `tests/integration/**` which belong to earlier sessions, not the `Makefile`, not root locks.

## Drive it through the front door

Build the application the way a process does — `auditmanager.api.app.create_app()` or `auditmanager.api.composition.build_application()` — and exercise the twelve operations through the `Router` it returns, request in, response out. Do not import `IngestService`, `execute_run`, `export_run_csv` or any other module directly to make a step work. If an operation cannot be reached through the router, **that is the finding**, and it is exactly the kind this session exists to produce.

## The ten criteria of `PROTOTYPE_PROFILE.md` §8

Read them there; they are the specification and this is a summary. From a clean local start: services and migration; a project created and the bounded synthetic AR PDF uploaded to an immutable version and a verified private object; the deterministic path and `text_analysis` executed with the UI-visible states distinguishable; at least two seeded issues found and every published quotation verified present on its declared page; the page opened from a finding with its exact quotation, one accepted, one rejected, a later comment appended without overwriting history; a UTF-8 CSV whose rows resolve to the exact project, version, run, finding, observation and current verdict; a restart preserving canonical state; the same commands repeated under the same idempotency keys creating no duplicates; and explicit unsupported-input, checksum, unavailable-provider and ungrounded-model failures with no filesystem fallback and no fake success.

**Criterion 4 and criterion 5 name a _live_ `text_analysis` run.** There is no `ANTHROPIC_API_KEY` on this host, and the owner has been asked to decide whether to supply one. Until that is answered:

- prove every criterion that does not require a live call, on the recorded adapter;
- write criterion 5 so it **runs against a live provider when a credential is present and is explicitly reported as not executed when it is not**. Not skipped silently, not passed on a recording — reported, by name, as the one criterion the run could not exercise;
- **never fabricate a live transcript.** A previous session in this programme declined to and said so plainly; that is the standard.

Say in your report exactly which criteria were proved, which were proved only on the recorded adapter, and which were not exercised at all.

## Figures measured before you, to reproduce rather than assume

Run state `published`, empty degradation set, four stages `succeeded`, **3 findings**, **5 CSV rows**, 17 columns, two exports byte-identical, the baseline PDF 58978 bytes with sha256 `6d53674f…`, and all twelve manifest quotations resolving at their declared pages.

## Known open items — report if they bite, do not rediscover

Documented in `GATE_B1_CLOSURE.md` and `GATE_B2_CLOSURE.md`. Among them: `ungrounded_reason` has no CHECK and is never written because the analysis stage drops unresolvable anchors before the gate sees them; `blob_id` is content-derived and the primary key, so a rejected blob permanently bans those bytes; `model_call.status` admits no `truncated`; no query surface accepts cursor or filter parameters the contract declares; `OD-03` records no owner-decided cost ceiling and a $1.00 stand-in is in place.

## Anti-vacuity is your central obligation

**Nine tests in this programme have passed or failed without exercising what they named**, and the pattern is identical in all nine: the test asserted a property of the fixture rather than of the code. An immutability test discarded the row it was testing and reported that a trigger had failed to fire. A timestamp comparison certified a store that was rewriting bytes. Four projects created in a loop landed in one millisecond, so asserting their order descended stayed green against a reversed `ORDER BY`. A mutation patched a name the conftest had already bound, so it reached nothing and came back green.

Therefore **every guard you write must be shown to fail**: mutate what it protects, confirm red, revert, confirm green, report both. A mutation producing a collection or import error rather than a guard failure proves nothing and must be redone.

Mutating correctly here has three traps, all of which have cost sessions time:

- `pyproject.toml` sets `pythonpath = ["src"]`, which **overrides an exported `PYTHONPATH`**. Use `.venv/bin/pytest -o pythonpath=<copy>/src`, and **print the loaded module's `__file__`** to prove the copy was imported before believing a result.
- A scratch copy of `src/` alone will not import: `shared/errors/catalog.py` reads `contracts/domain/v1/error-codes.json` and `analysis/text/lock.py` reads `docs/program/P02_LOCK.json`, both relative to the module. Symlink **both** `contracts/` and `docs/` into the copy.
- Use a session-unique scratch directory name. Two sessions had theirs overwritten mid-run by a peer.

## Gate — run these, report exact exit codes

- `tests/e2e/pc01` exits `0` against real PostgreSQL and MinIO, driven through the router
- `make foundation` still exits `0`
- `git diff --check` exits `0`
- `git diff --name-only e7965472..HEAD` lists only `tests/e2e/pc01/**`

`.venv/` is git-ignored and exists **only in the main checkout**, so a literal `.venv/bootstrap/bin/python …` exits 127 inside a dispatched worktree. Use `/root/projects/PDF-Analysis/.venv/bin/pytest` and `/root/projects/PDF-Analysis/.venv/bootstrap/bin/python` against your own tree's scripts, and say which interpreter you used.

Bootstrap with `make bootstrap FOUNDATION_PYTHON=/usr/bin/python3.12`; the bare form is correctly refused under an active virtualenv and that refusal is the guard working. Your instance: `FOUNDATION_INSTANCE=gate-c2`, `POSTGRES_PORT=55490`, `S3_API_PORT=59100`, `S3_CONSOLE_PORT=59101`, `POSTGRES_DB=audit_c2`, `S3_BUCKET=audit-c2`. Copy `.env.example` to `.env`; it is git-ignored and never committed.

The integration suites share one database and some assert global state; run only your own suite plus `make foundation`. `tests/contract` and `tests/checkpoint` as wholes are CP-00 historical evidence, red before you start, and are not your gate.

## Rules

1. **Commit after every meaningful step.** A dispatch here was killed mid-run and lost four sessions entirely because each held work uncommitted.
2. **Report defects, never repair.**
3. **Never add a dependency.** `docs/program/P02_LOCK.json` is the pinned set and contains no HTTP framework by design; the API runs on standard library primitives.
4. Do not measure while another session runs against the same services.

## Report

Changed paths. Every command with its exit code, and which interpreter. Criterion by criterion: proved, proved-on-recording, or not exercised. The figures you reproduced and any that differed. Each guard you mutated, what went red, what went green on revert. Every defect found, described precisely, left unrepaired, naming the owning tree. Whether any operation could not be reached through the router. And your elapsed wall-clock from start to last commit.
