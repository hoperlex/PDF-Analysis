# `B-0` dispatch prompt — P02 pins and the additive environment contract

Base `3cf98c251171d1d243da279df3e347385d6a41b2`. Serial: nothing else runs while this
session holds the root lock. Dispatch text below, verbatim.

> **Executed and merged 2026-09-10 at `51cb2a3`.** Result in `docs/program/P02_LOCK.json`.
>
> **One instruction in this prompt was wrong and was refused.** Deliverable 3 asks for
> additive `.env.example` names for provider credentials, cost ceiling and provider mode.
> `load_env` enforces a strict allowlist of exactly the fifteen names FF-01 §3 freezes, and
> its comment states the intent: `.env` does not carry credentials for other systems "or
> anything that selects what code runs" — which is precisely what `PROVIDER_MODE` is.
> Provider configuration is application-layer instead, read from the process environment at
> runtime, with the credential an injected secret. Widening `.env` would be an FF-01 §3
> freeze-break needing a break record, which a pin session may not issue. The prompt is left
> unedited below so the refusal stays legible; `P02_LOCK.json` carries the resolution.

---

You are session `B-0` for the PDF-Analysis prototype. Repository: /root/projects/PDF-Analysis.

BASE COMMIT: `3cf98c251171d1d243da279df3e347385d6a41b2` on `planning/prototype-roadmap`. Create and work on branch `agent/gate-b0`. **Verify your worktree is actually on that commit before you start** — a previous session in this programme received a worktree checked out at an unrelated older commit. If it is not, branch from the literal SHA above.

You are the sole writer of the root dependency lock for P02. Every Gate B session that follows consumes your pins and may not add or upgrade a dependency. If you get this wrong, six sessions inherit it.

READ FIRST: `docs/program/GATE_B_DISPATCH_PLAN.md` §1 and §3, `docs/program/tasks/P2-INT-00.md`, `docs/program/PROTOTYPE_FOUNDATION_FREEZE.md` §3 for the frozen environment names, and `docs/program/FOUNDATION_LOCK.json` for how P01's pins are recorded. Gate A is closed and green; read `docs/program/GATE_A_CLOSURE.md` §5 for what the sessions before you were held to.

## You own exactly these paths

- `pyproject.toml`, `uv.lock`
- `.env.example` — **additive only**, never edit or reorder an existing name
- `docs/program/P02_LOCK.json`
- `docs/program/tasks/P2-INT-00.md` status and handoff sections only

Nothing else. Not the `Makefile` and its nine frozen targets, not `src/**`, `db/**`, `infra/**`, `web/**`, `contracts/**`, `fixtures/**`, `tests/**`, not `artifacts/checkpoints/**`, not Git tags.

## Deliver

**1. The PDF text-extraction library, per owner decision `OD-01`.** Its recorded default: a permissively licensed extractor giving per-character boxes, plus a separate page-count and encryption probe. **A copyleft library is blocked** — this rules out PyMuPDF/fitz, which is AGPL. Check the actual licence of what you pick and record it; do not trust a summary. Session `B2` will use it for the text layer with stable character offsets and for the block index with page, bounding box and text-span anchors, so per-character geometry is a requirement, not a preference.

**2. The model provider SDK, per owner decision `OD-02`.** Its recorded default: the Anthropic first-party API with `claude-opus-5` (1M context; $5.00 per MTok input and $25.00 per MTok output on the 2026-06-24 cached rate table), with `claude-sonnet-5` ($2.00/$10.00) as the cheaper tier. Pin the SDK exactly. Record both model ids and their rates in `P02_LOCK.json` so `B3`'s cost ceiling and `P4-OPS-01`'s cost measurement read one source.

**3. Additive `.env.example` names** for provider credentials, the per-run cost ceiling (`OD-03`) and the provider mode (`live` or `recorded`). Disposable local examples only; never a real credential. Keep the service side and the application side coherent — `make` enforces this and will refuse an incoherent file.

**4. `docs/program/P02_LOCK.json`** recording every pin with its version, licence and purpose, the model ids and rates, and the new environment names. Machine-readable, in the spirit of `FOUNDATION_LOCK.json`.

## The acceptance test that matters most

**Prove the pinned extractor against the corpus oracle, at pin time.** Session `A4` generated `fixtures/synthetic/ar/ar_baseline.pdf` and `expected_issues.json`, which declares twelve quotations — five seeded evidence and seven controls — each with its page, character offset and exact text. That manifest is the acceptance oracle for the whole programme.

Write a throwaway probe (not a committed test — `tests/**` is not yours) that uses **your pinned extractor** to recover all twelve at their declared pages, and report the count. If your candidate cannot recover all twelve character-for-character, it is the wrong candidate: `B4`'s grounding gate will resolve quotations against exactly this text, and a mismatch there is invisible and poisons every later measurement. Also probe the negative fixtures under `fixtures/synthetic/ar/negative/`: the encrypted one must raise rather than return empty text, and the image-only one must yield no extractable text rather than silently returning nothing indistinguishable from a blank page.

Report which candidates you tried, what each scored, and why you chose the one you chose.

## Gate — run these and report exact exit codes

- `make bootstrap FOUNDATION_PYTHON=/usr/bin/python3.12` twice: exit `0` both times, and **no tracked file changes between the two runs**. Bare `make bootstrap` is correctly refused under an active virtualenv; that is not a defect.
- `make foundation` still exits `0` on the widened lock. You are adding dependencies to a green foundation and it must stay green. This needs the local services; use `FOUNDATION_INSTANCE=gate-b0`, `POSTGRES_PORT=55460`, `S3_API_PORT=59060`, `S3_CONSOLE_PORT=59061`, `POSTGRES_DB=audit_b0`, `S3_BUCKET=audit-b0`. Copy `.env.example` to `.env` and set exactly these; `.env` is git-ignored and is never committed.
- `.venv/bootstrap/bin/python scripts/validate_bootstrap.py` exits `0` and prints `PASS`.
- `git diff --check` exits `0`.
- your extractor probe: report the recovered count out of twelve, and the negative-fixture behaviour.

## Rules this programme learned the hard way

- **Commit after every meaningful step** to `agent/gate-b0`. An earlier dispatch here was killed by a session restart and lost four sessions entirely, because each held its work uncommitted.
- **Report defects, never repair outside your tree.** If you find a problem in a Gate A deliverable, describe it precisely and leave it. Gate A's contract defect was trustworthy exactly because the hand that found it was not the hand that fixed it.
- **`tests/contract` and `tests/checkpoint` are not your gate.** They are CP-00 historical evidence and are red before you touch anything. Run only the commands above.
- **No floating versions.** Exact pins only, on every dependency you add. The foundation freeze forbids floating references and the frontend lock already holds this line.

## Report at the end

Changed paths. Every command with its exit code. The extractor candidates you tried, their licences, their scores against the twelve quotations, and your reasoning. The model ids and rates you recorded. Anything you could not do and why. Any defect you found and left unrepaired. And your elapsed wall-clock from start to last commit — the programme's forecast is being rebuilt from measurement and this is one of its inputs.
