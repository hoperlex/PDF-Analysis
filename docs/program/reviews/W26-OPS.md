# W26-OPS — the deploy stops sending the operator to the wrong file, and the rehearsal stops counting a view

> **Opened before the first edit**, as the dispatch requires. Every figure below is
> measured on this host and carries the command that produced it. Sections are filled as
> the work happens rather than written at the end.

`D-38` and `D-39`, both found by `W24-CERT2` and both about what a screen tells an
operator. Neither is a wrong exit code: one script refuses with the wrong diagnosis, the
other over-reports a total. The repairs are in `infra/deploy/deploy.sh` and
`infra/deploy/reset.sh`.

## 1. `D-38` reproduced before it was repaired

_pending — the instance, the stopped service, `$?` and the last ten lines._

## 2. The repair to `deploy.sh`

_pending._

## 3. `D-38` after the repair

_pending._

## 4. `D-39` reproduced, repaired, and re-measured

_pending._

## 5. Which test reddened for each repair

_pending._

## 6. Marker counts

_pending._

## 7. What an operator sees on a successful run

_pending._

## 8. Limits, and anything false in the dispatch

_pending._
