# `W0-QA-04` — a final-state contour that can tell a ratified checkpoint from an open round

**Owner:** single QA/tooling owner of `W0-QA-04`.
**Base commit measured against:** `95f4335754209885c41e744d15d72934372fc4c4`
(`docs(program): record Gate 0 variant A and open the CP-00 recovery task graph`), the
commit that adds the recovery planning files on top of the ratification commit
`39a3a6430bd97c38cb20bafc793fc9d077d0df8e`.
**Tree:** a clean full clone at `/root/projects/PDF-Analysis-qa04`, branch
`task/W0-QA-04`. Not a linked worktree: `.git` is a directory there, so the sandbox
tests can copy the object database and the suite collects its real size.
**Refs:** nothing was committed, pushed or tagged. `v0.0.0-architecture` was read and
never written.

---

## 1. Verdict

The contour exists and works. On this tree it says:

> **terminal state: `terminal-ratified`** — and thirteen findings, in three of the five
> coverage areas, where the records do not describe that state.

Every one of the thirteen is owned by `W0-INT-02`. None is a value this task chose, and
none goes green by editing a file this task owns.

Two areas — terminal state and live-vs-historical — are **green, with their
non-vacuity measured rather than asserted**: three historical claims were re-derived at
the commit each record names, and one live `CP-00` status row was read and compared.

Three things in the task contract turned out not to hold. They are in §7, plainly, and
one of them — a second pinned literal in `test_cp00_candidate.py` that made this task's
own licensed deliverable unreachable — is the most important thing in this report.

---

## 2. Deliverables

| Path | What it is | State |
|---|---|---|
| `tests/checkpoint/cp00_final_state.py` | the contour: five checks over an injected tree and tag, plus a runnable `main()` | new |
| `tests/checkpoint/test_cp00_final_state_contour.py` | 44 tests: one green fixture, a negative case and a mutation per check | new |
| `tests/contract/test_cp00_final_state.py` | the verdict on **this** tree, in the blocking contour | new |
| `tests/contract/test_cp00_candidate.py` | two pinned literals made resolvable; see §6 | modified |
| `docs/program/reviews/W0-QA-04.md` | this report | new |

`tests/contract/test_cp00_candidate.py` carries **three** repairs, not two: the third,
`_declared_allowed_paths`, is the remediation in §10.

The contour takes a `Tree` and a `TagFacts` and returns `Finding` objects. It knows
nothing about this repository, so the same five checks run here, on a candidate, or on
any historical commit. `tests/contract/test_cp00_final_state.py` is only the binding of
that contour to this tree.

`artifacts/checkpoints/CP-00/check_state_records.py` was **not edited and not replaced**.
Its round accounting is correct for an open round and is kept as history.

---

## 3. Required tests, verbatim, with the exit code of the measured process

Every exit code below is `$?` of the named process, captured immediately, not of a
pipeline tail. Commands were run one at a time in the clone; nothing else was cloning.

### 3.1 `git rev-list -n1 v0.0.0-architecture`

```text
39a3a6430bd97c38cb20bafc793fc9d077d0df8e
exit=0
```

`git cat-file -t v0.0.0-architecture` → `tag`, `exit=0`. Annotated, unmoved, identical
to the base. **Expected result met.**

### 3.2 `git diff --check`

```text
exit=0
```

No output. **Expected result met.**

### 3.3 `git status --porcelain -uall`

```text
 M tests/contract/test_cp00_candidate.py
?? docs/program/reviews/W0-QA-04.md
?? tests/checkpoint/cp00_final_state.py
?? tests/checkpoint/test_cp00_final_state_contour.py
?? tests/contract/test_cp00_final_state.py
exit=0
```

Containment, as a path set rather than a count:

| Reported path | Allowed by |
|---|---|
| `tests/contract/test_cp00_candidate.py` | the extension the integrator granted mid-task |
| `docs/program/reviews/W0-QA-04.md` | `docs/program/reviews/W0-QA-04.md` |
| `tests/checkpoint/cp00_final_state.py` | `tests/checkpoint/**` |
| `tests/checkpoint/test_cp00_final_state_contour.py` | `tests/checkpoint/**` |
| `tests/contract/test_cp00_final_state.py` | `tests/contract/test_cp00_final_state.py` |

The set is a subset of the allowed paths, and it is the whole set: five paths, no more.
**Expected result met.** No forbidden hotspot is touched:
`contracts/`, `fixtures/`, `scripts/`, `artifacts/checkpoints/CP-00/`,
`docs/architecture/`, `docs/program/CURRENT_STATE.md`, `CHECKPOINT_REGISTRY.md`,
`docs/program/waves/` and every other task file are unchanged, and no lock, migration
head, composition root or global style file exists in the delta.

### 3.4 `.venv/bootstrap/bin/python scripts/validate_bootstrap.py`

```text
Bootstrap validation
  json_files=57
  markdown_files=164
  stages=11
  manual_runbooks=11
  adrs=18
PASS
exit=0
```

**Expected result met**, standalone `PASS`. The base tree measured `json_files=57, markdown_files=163`
before any edit; the one added Markdown file is this report. Note for a
reader comparing against the bundle: `automated-summary.txt` records `json_files=56,
markdown_files=155`, which is true of `2ea7b68` — the tree that record names — and not
of this one. That is the live-vs-historical distinction in §5.2 working as intended.

### 3.5 `.venv/bootstrap/bin/python -m unittest discover -s tests/checkpoint`

```text
Ran 46 tests in 1.018s

OK
exit=0
```

**Expected result met.** 46 is what the runner collects; there is no static count in
this report that disagrees with it. (44 before the remediation in §10, which adds two.)

### 3.6 `.venv/bootstrap/bin/python -m unittest discover -s tests/contract`

```text
Ran 340 tests in 57.716s

FAILED (failures=5)
exit=1
```

**The `exit 0` clause was withdrawn by the integrator** after the first independent
review agreed it could not hold; the replacement gate is *the failure set is exactly the
base commit's own failures, plus contour checks all of whose findings name `W0-INT-02`
as owner*. **That gate is met**, and it is now asserted rather than left to a reader:
`ContourHygieneTests.test_no_finding_is_this_task_s_own_defect` fails if any finding
carries `W0-QA-04` as owner, which is how the contour reports a check of its own that
has gone vacuous. The measurement is broken out in full below because the reasoning
behind the withdrawn clause is still the substance of the report.

The five failures are two classes:

| # | Test | Class |
|---|---|---|
| 1 | `RatificationRecordTests.test_ratification_requires_an_accepted_acceptance_round` | **pre-existing at the base commit** |
| 2 | `SandboxResetTests.test_unresettable_names_a_reset_that_did_not_finish` | **pre-existing at the base commit** |
| 3 | `CrossGateAgreementTests.test_the_two_ratification_gates_agree` | this task's contour, red on purpose |
| 4 | `FileAndHashAccountingTests.test_every_file_the_checkpoint_claims_exists_and_every_aggregate_is_enumerated` | this task's contour, red on purpose |
| 5 | `TagIntegrityTests.test_the_tag_has_not_moved_and_its_message_is_true_of_its_commit` | this task's contour, red on purpose |

**Failures 1 and 2 exist at the base commit, before any edit of mine.** Measured first,
on the clean clone at `95f4335`, working tree empty:

```text
Ran 324 tests in 46.137s

FAILED (failures=2)
exit=1
```

```text
"round 10 is void: these paths differ from the tree frozen at 2ea7b68b4c44 as
tested_candidate_digest, and nothing licenses them: ['docs/program/EXECUTION_PLAN.md',
'docs/program/tasks/W0-INT-02.md', 'docs/program/tasks/W0-INT-03.md',
'docs/program/tasks/W0-QA-04.md']. ... The manifest's own rule: if it must change, the
round is void and a new one begins."
```

The commit that opened the recovery task graph put four planning files into the tree
after the round-ten freeze, outside the round's declared acceptance evidence and outside
the ratification delta ceiling. By the manifest's own rule, **round ten is void at
`95f4335`**, and the suite says so. This is not a finding of mine and not mine to
dispose of; it is `W0-INT-02`'s, and it is stated here because the required test
"`discover -s tests/contract` → exit 0" was already unreachable on the tree this task
was dispatched against. My four uncommitted files extend failure 1's path list from
four entries to nine — measured, with the report in place:

```text
nothing licenses them: ['docs/program/EXECUTION_PLAN.md',
'docs/program/reviews/W0-QA-04.md', 'docs/program/tasks/W0-INT-02.md',
'docs/program/tasks/W0-INT-03.md', 'docs/program/tasks/W0-QA-04.md',
'tests/checkpoint/cp00_final_state.py',
'tests/checkpoint/test_cp00_final_state_contour.py',
'tests/contract/test_cp00_candidate.py', 'tests/contract/test_cp00_final_state.py']
```

They lengthen the list; they do not create the failure, and the first four entries are
there at the base commit with the tree clean.

**Failures 3 to 5 are the deliverable.** The task contract requires both that this
command exit 0 and that "a tree ... whose two ratification gates disagree turns at
least one named check red". On this tree the gates do disagree, so the two clauses
cannot both hold. §7.1 says what was done about it and why.

---

## 4. The contour's verdict on this tree

```text
.venv/bootstrap/bin/python tests/checkpoint/cp00_final_state.py
exit=1
```

```text
tree: /root/projects/PDF-Analysis-qa04
  234 tracked paths; 224 live records, 10 historical by their own declaration or by the manifest's binding
  3 historical claim(s) re-derived at the commit the record names; 1 live CP-00 status row(s) read

terminal state: terminal-ratified

terminal-state: 0 finding(s)

live-vs-historical: 0 finding(s)

tag-integrity: 2 finding(s)
  ! v0.0.0-architecture (message): the tag message states artifact_manifest_sha256
    39721aac0ebe1aa5c13d0ae3e01ee93380671daddc5d9f3f3b774997d6f75e08 and the recipe over
    its own commit gives f362647cc9b1d2201ac4663bf5d877346eeeccf046734219103c3ac822d2845d
  ! v0.0.0-architecture (message): the tag message claims the reviewed families are byte
    identical to 92e13fa496a723ed6e4c3adbf138c4f4e1d7c368, and 5 differ at the tag's own
    commit: ['docs/architecture/ADR_INDEX.md',
    'docs/architecture/ARCHITECTURE_LINT_RULES.md',
    'docs/architecture/CP00_ARCHITECTURE_REVIEW.json',
    'docs/architecture/CP00_ARCHITECTURE_REVIEW.md',
    'docs/architecture/CP00_OWNER_DECISIONS.md']

file-and-hash-accounting: 9 finding(s)
  ! artifacts/checkpoints/CP-00/checkpoint-report.md: claims the repository path
    'tests/checkpoint/test_cp00_mechanism.py', which this tree does not track
  ! artifacts/checkpoints/CP-00/checkpoint-report.md: claims the repository path
    'tests/contract/test_cp00_contracts.py', which this tree does not track
  ! artifacts/checkpoints/CP-00/known-risks.md: claims the repository path
    'tests/checkpoint/test_cp00_mechanism.py', which this tree does not track
  ! artifact_manifest_sha256: two live checkpoint records give this top-level field
    different values, and neither says which tree it describes:
    build-info.json = 39721aac…; contract-manifest.yaml = 39721aac…;
    manifest.json = f362647c…
  ! contract-manifest.yaml:families.contracts: declares the aggregate d9436aee… over 33
    files and lists 0 member hashes
  ! contract-manifest.yaml:families.docs/architecture: … 352f1204… over 33 files, 0 listed
  ! contract-manifest.yaml:families.fixtures: … ade9090a… over 32 files, 0 listed
  ! contract-manifest.yaml:families.scripts: … ce9e8861… over 2 files, 0 listed
  ! contract-manifest.yaml:artifact_manifest_sha256: declares an aggregate over 100 files
    and the record lists 9 per-file hashes across all of its sections

cross-gate-agreement: 2 finding(s)
  ! docs/architecture/CP00_ARCHITECTURE_REVIEW.json: two gates of one checkpoint disagree
    on 'review_status': docs/program/tasks/W0-INT-01.md requires 'ratified' and
    docs/architecture/CP00_ARCHITECTURE_REVIEW.json carries 'ratified_at_w0_3'.
  ! the same, discovered independently in docs/program/tasks/W0-INT-02.md
```

Every finding carries `owner: W0-INT-02`. Three of them reproduce recorded blockers from
`EXECUTION_PLAN.md` §3.1 without having been told about them — `CP00-B7` (tag message),
`CP00-B3` (claimed test artifacts), `CP00-B4` (mixed pre/post-ratification hashes) and
`CP00-B1` (the gate disagreement) — which is the evidence that the contour reads the
tree rather than a list.

Two numbers, recomputed here with their method, since the bundle's labels for them are
swapped. Command: `git ls-tree -r --name-only <rev> -- contracts fixtures
docs/architecture scripts`, sorted, path bytes then raw sha256 per file into one sha256,
run in this clone:

| Revision | Reviewed-family digest | Files |
|---|---|---|
| `92e13fa` reviewed candidate | `39721aac0ebe1aa5c13d0ae3e01ee93380671daddc5d9f3f3b774997d6f75e08` | 100 |
| `39a3a64` tag / ratification | `f362647cc9b1d2201ac4663bf5d877346eeeccf046734219103c3ac822d2845d` | 100 |
| `95f4335` base measured | `f362647cc9b1d2201ac4663bf5d877346eeeccf046734219103c3ac822d2845d` | 100 |

`contract-manifest.yaml` publishes the first as `artifact_manifest_sha256` and the second
as `reviewed_manifest_digest`; `manifest.json` publishes the second as
`artifact_manifest_sha256`. One recipe, two trees, one field name.

---

## 5. The five coverage areas, and how each is made structural

### 5.1 Terminal state

`classify()` derives the state from the manifest's data and never restates it:
`ratified`, `status`, `current_round`, the current round's own `status`/`verdict`, and
whether a `ratification` record exists. Exactly one of `open-round`,
`closed-accepted-round`, `terminal-ratified` may be satisfied; zero or two is itself the
finding.

The design requirement was that the check **must not go quiet at publication**. In
`terminal-ratified` it still holds five obligations, each of which catches a real
post-publication staleness:

1. the accepted round is the **highest-numbered** round — opening round eleven without
   moving `current_round` is red;
2. both of the accepted round's primary reports **declare a subject commit in their own
   bytes** — a report that binds to a tree only by somebody else's say-so is red;
3. the two declared subjects **agree** — the streams judged one tree or the round is
   void;
4. the manifest's published digests **are the accepted round's own**;
5. every live `CP-00` **status row** states the terminal state and names the accepted
   round — this is the one that goes red the day a later round is accepted and the
   registry is not brought forward.

Attribution for (5) is structural in three parts, all read from the document: the record
must be live, the table must have a **status column**, and the row's **first cell** must
be the checkpoint id. This was arrived at empirically. A prose sweep for
"a sentence mentioning CP-00 and a non-terminal word" was written first and measured on
this tree: **36 hits across 13 files**, of which the great majority were about `ADR-0014`
being deferred, a fixture inside a test module, or a review report quoting a stale claim
in order to report it. That sweep would have been the same defect this programme keeps
making, so it was discarded rather than tuned. The column rule has a recorded negative
case: `README.md:44` carries a `CP-00` row in a roadmap table whose third column is the
checkpoint's *main result*, and it is correctly not a status claim.

### 5.2 Live versus historical, structurally

A record is **historical** when it declares a subject commit *in its own bytes*
(`candidate_commit:`, `Candidate frozen at <sha>`, and the other shapes in
`SUBJECT_DECLARATION`, within the header region), **or** when `manifest.json` binds it to
an acceptance round and that round to a frozen commit. Both sources are data in the tree.
There is **no path list**. `docs/program/reviews/` — which the sweep beside this one
excludes by path, in a narrowing its own acceptance record warned against — is judged
here as live, and a test asserts it is not in the historical set.

The exemption is turned into an **obligation**: a historical record's machine-checkable
claims are **re-derived at the commit it names** rather than skipped. On this tree three
such claims were re-derived and all three hold — `automated-summary.txt` claims 221
tracked paths of `2ea7b68` and `2ea7b68` has 221; `manual-test-report.md` and
`manual-report-round-10.md` claim the same of the same commit. The count is reported in
the verdict header and asserted non-zero, because an exemption nobody pays for is a skip
list with a better docstring.

The two sources are kept **separate** in the API (`declared_subject` and `bound_subject`)
after a defect found during development: conflating them let a report whose header
declaration had been stripped be excused by the round the manifest binds it to, which is
exactly the proximity failure one level up. Obligation (2) in §5.1 asks the report itself.

### 5.3 Tag integrity

The records must agree on **which** tag the checkpoint has; the tag must exist, be an
**annotated tag object**, and peel to a commit.

"Has not moved" is checked against the **digest the manifest publishes**, recomputed at
the tag's own commit. This is not a preference: a recorded-target field is not available
to this checkpoint by construction, because the tag sits on the commit that carries the
manifest and a commit cannot name its own hash — the manifest says so itself about
`manual_candidate_commit`. The digest test has the property that matters: move the tag to
any commit whose reviewed families differ and it goes red.

The **tag message** is the one record whose subject needs no declaration, because an
annotated tag object binds its message to its own commit. Its machine-readable claims are
therefore live claims about the tagged tree, and two of them are false — see §4.

### 5.4 File and hash accounting

Three rules:

* **every file the checkpoint claims exists** — every path-shaped *string value* in the
  live structured bundle records (airtight, no prose), plus backticked **executable test
  artifacts** (`tests/…​.py`) in the live deliverables. The second is a narrowed claim
  shape, not a narrowed sweep: every live deliverable is read, and the narrowing can only
  *add* findings, which is the same reason `MUST_ACCOUNT` is safe in the sweep beside
  this one. It is what catches `CP00-B3`;
* **no aggregate whose members are unlisted** — an aggregate that names a file count must
  enumerate that many per-file hashes, counted by *filename-shaped key* so that an
  aggregate's own value cannot count as one of its members, and counted **at any depth**
  so that a manifest which does enumerate family by family is not read as enumerating
  nothing;
* **no two live bundle records give the same top-level field two values** — top-level
  only, because a value nested under `digest_model` is prose *about* a field qualified by
  its container. This is what catches `CP00-B4`.

### 5.5 Cross-gate agreement

Gates are **discovered**, not hardcoded: any task file whose required-test command reads
a JSON document and asserts on its fields is a gate, and its `assert r['k'] == v` /
`assert r['k'] is v` clauses are extracted and evaluated against that document. Two gates
were found on this tree — `W0-INT-01` and `W0-INT-02` state the same command — and both
disagree with the review on `review_status`.

**This contour does not choose the value.** The finding names both sides and says so in
its own text. Applying the canonical value to
`docs/architecture/CP00_ARCHITECTURE_REVIEW.json` is `W0-INT-02`'s disposition; when it
lands, this check goes green with no edit to any file this task owns.

Two facts were verified independently rather than taken on report, because the choice of
`"ratified"` depends on them:

* `git grep -F -c -- '`ratified`' 92e13fa496a723ed6e4c3adbf138c4f4e1d7c368` → **no
  output, `rc=1`**: the code span appears **zero** times in the reviewed candidate. The
  novelty guard in `_review_consistency_problems` compares the code span, so
  `review_status = "ratified"` is satisfiable and the deadlock that produced
  `ratified_at_w0_3` is gone;
* `test_cp00_candidate.py:5722` already patches the sandbox with
  `review_status="ratified"`, and `:6086` already asserts
  `assertEqual(review["review_status"], "ratified")`. Neither needs changing.

---

## 6. `tests/contract/test_cp00_candidate.py` — two pinned literals made resolvable

### 6.1 `CHECKPOINT_TAG`, the one the integrator named

`CHECKPOINT_TAG = "v0.0.0-architecture"` was pinned "because a value the integrator can
change is not a value a check can hold them to". The reasoning is right; the literal made
the recovery unreachable. Two guards consume it, no task in the recovery graph except
`W0-QA-04` may write this file, and the recovery ratifies as `v0.0.1-architecture` — so
`W0-INT-03`'s own required `discover -s tests/contract` would fail *at the moment of
ratification*.

Replacing one literal with another only moves the problem to `v0.0.2`, so what is pinned
now is the **series and the rule**:

* `CHECKPOINT_TAG_SERIES = ^v0\.0\.(\d+)-architecture$` — a rename to anything outside
  the series is refused. This is the half of the pin that was load-bearing;
* `CHECKPOINT_TAG_FLOOR = "v0.0.0-architecture"` — the **first** tag of the series. An
  immutable historical fact, not a current value: it was published on an annotated tag
  and cannot be un-published, so the line never needs editing again;
* the resolved value must **agree across all three claims in two records**
  (`manifest.json:tag`, `manifest.json:tag_planned`,
  `contract-manifest.yaml:tag`), match the series, and never move **backward** from the
  floor.

That restores the property structurally: one manifest edit no longer moves the value,
three claims have to move together and the module compares them, a rename is refused, and
a re-tag onto a spent revision is refused. Resolution failure falls back to the floor and
is reported as a **named test failure** rather than raised at import, because an
import-time exception takes the whole suite down and reports nothing about why.

The anti-gutting needle in `RATIFICATION_PUBLICATION_RECORDS` — which required the W0.3
wave plan to still contain the literal `` `v0.0.0-architecture` `` — became a **shape**,
`re:` + the series body. Its job is to refuse a retraction made by deleting what the
stale claim was about, and a plan naming `v0.0.1-architecture` is not gutted. The needle
is a *marked string* rather than a compiled pattern for a reason worth recording: a
compiled pattern is a call to an attribute, `_is_a_literal_expectation` refuses one, so a
table holding a compiled pattern **could not be pinned at all** and
`TableExpectationTests` would have gone quiet on the whole table to accommodate one entry.
The marked string keeps the shape and keeps the table pinned whole. That failure was
observed, not predicted: the first version of this change turned
`test_every_table_this_module_defines_carries_a_literal_expectation` red.

### 6.2 `WriteBoundaryTests`, a second one of the same shape, found by doing the work

`test_only_the_two_owned_paths_are_reported_under_the_owned_trees` pinned the owned set
to `W0-QA-01`'s two files and required nothing else under `tests/contract/` or
`docs/program/reviews/` to be dirty. It is a **task-local containment gate that outlived
its task**: committed into the shared suite, it turns red for every later task licensed
to write in either tree. `W0-QA-04`'s allowed paths name
`tests/contract/test_cp00_final_state.py` and `docs/program/reviews/W0-QA-04.md`, so with
the pin in place **performing this task honestly failed the suite that judges it** — the
same shape as `CHECKPOINT_TAG`, and the same shape as round nine's "performing the
ratification voids the round that authorises it". It was not predicted; it turned red the
moment the deliverable was written.

The repair keeps the property and drops the pin: a dirty path is contained when **some
task file's own `## Allowed paths` section declares it**, read out of
`docs/program/tasks/`. That is not the weakness the module warns about — a directory
listing grows when anybody adds a file, whereas this grows only when somebody writes a
task that claims the path, in a document that names an owner and goes through the same
review as the work it licenses. The scan finds declarations in all 20 task files;
`_path_is_declared` is asserted to **refuse** an undeclared path, so containment still
proves something.

---

## 7. What did not hold, plainly

### 7.1 The contract's two clauses are inconsistent on this tree, and the red was chosen

`Required tests` says `discover -s tests/contract` must exit `0`. `Integration contract`
says a tree "whose claimed artifacts do not exist, or whose two ratification gates
disagree, turns at least one named check red". **On this tree both cannot be true.** The
gates do disagree, the claimed test modules do not exist, and choosing `review_status` is
explicitly `W0-INT-02`'s and forbidden to me.

Three ways out were considered and two rejected:

* `unittest.expectedFailure` on the cross-gate test. Rejected: it makes the contour
  silent *while the gates disagree*, which is precisely backwards, and Python turns an
  unexpected success red — so it would go red the moment `W0-INT-02` fixed it;
* a recorded ledger of open findings that the test compares against. Rejected: it is red
  only for findings nobody wrote down, and `tests/**` is `W0-QA-04`-owned in phase A, so
  `W0-INT-02` could not remove an entry after fixing it. A check that is quiet about a
  contradiction because somebody wrote the contradiction down is the failure mode this
  checkpoint has spent ten rounds on;
* **chosen:** the check is red, named, and self-closing. It names both sides, names the
  owner, and goes green with no edit to any file this task owns.

The exit code is reported honestly in §3.6 rather than engineered to `0`. Note also that
the command already exited `1` at the base commit for an unrelated and unowned reason
(§3.6), so "exit 0" was not achievable on this tree before this task started either.

### 7.2 The tag's target commit is not recorded anywhere, and cannot be

"Has not moved relative to its recorded target" cannot be checked against a recorded
target for CP-00: the tag is on the commit that carries the manifest, and a commit cannot
name its own hash. The digest reproduction in §5.3 is what replaces it and it has the
property that matters, but it is a *weaker* statement — it proves the tag points at a
tree with the published reviewed families, not at one exact commit. A superseding
checkpoint can close this properly by recording the *previous* tag's commit in the *new*
manifest, which is `W0-INT-02`'s to consider and is not a finding raised against this tree.

### 7.3 Everything else the contour does not cover

Seven, recorded in `LIMITATIONS` in the module itself, printed by every run, and
counted by a test that names this section, so a limitation dropped from the module and
left standing in this prose is a failure rather than a discrepancy nobody sees:

1. a record that declares no subject commit and is nonetheless historical is read as
   live. The rule is a declaration rule, so an undeclared dated report is a finding
   against the record rather than a silent pass — but the contour cannot date it for
   itself;
2. historical verification reaches only the claims the contour can machine-check against
   a named tree: the tracked-path count and the reviewed-family digest. Prose findings
   inside a dated report are not re-derived;
3. round accounting in prose is left to `check_state_records.py`. This contour reads round
   claims only where they sit in a structural unit it can attribute;
4. the tag message's test-count and stream claims are prose and are not re-measured. The
   message says "the 324-test contract suite" and the suite now collects 338; that is a
   claim about a historical run and is left to `W0-INT-02`;
5. "the tag has not moved" is checked against a published digest, not a recorded target —
   §7.2;
6. `parse_block_yaml` covers the block-map subset the contract manifest actually uses. No
   YAML library exists in the hash-locked bootstrap environment and adding one would touch
   a frozen lock file. Sequences are dropped rather than misread, and an aggregate whose
   members cannot be enumerated fails, so the omission cannot pass as coverage;
7. the mutation evidence proves the check classes, not every site inside them — §8.1.

### 7.4 Findings that belong to `W0-INT-02` rather than to this task

All thirteen in §4, plus the two pre-existing suite failures in §3.6. In particular:
**round ten is void at `95f4335` by the manifest's own rule**, because the recovery
planning commit changed four tracked files after the freeze. Any candidate `W0-INT-02`
prepares has to open a new round or the acceptance record contradicts itself again.

---

## 8. Mutation table — green before, red after, for every check

A guard nobody has made fail is a guard nobody has tested. Two matrices, both run
**in memory**: each mutation replaces one function on the loaded module and the tests are
re-run. Nothing was written to the working tree, no clone was made, and no ref was read
that was not read by the baseline.

### 8.1 The contour (`tests/checkpoint`, 44 tests)

Green before: `Ran 44 tests … OK, exit=0`, and `test_the_fixture_is_a_terminal_ratified_checkpoint_with_no_findings`
asserts the synthetic fixture has **zero** findings on all five checks at once.

| Mutation | Tests | Red | First test that goes red |
|---|---:|---:|---|
| *baseline, no mutation* | 44 | **0** | — |
| `classify()` always returns terminal-ratified | 44 | **2** | `test_MUTATION_a_ratified_manifest_whose_round_is_still_open_classifies_nothing` |
| `terminal_state_problems()` returns nothing | 44 | **8** | `test_MUTATION_a_dated_report_that_declares_no_subject_is_read_as_live` |
| `live_vs_historical_report()` returns nothing | 44 | **6** | `test_MUTATION_a_historical_claim_that_is_false_of_its_own_tree` |
| `tag_integrity_problems()` returns nothing | 44 | **7** | `test_MUTATION_a_lightweight_tag_is_not_a_checkpoint_tag` |
| `accounting_problems()` returns nothing | 44 | **6** | `test_MUTATION_a_claimed_file_that_is_not_there` |
| `aggregate_problems()` returns nothing | 44 | **2** | `test_MUTATION_an_aggregate_over_an_unstated_number_of_files` |
| `cross_gate_problems()` returns nothing | 44 | **4** | `test_MUTATION_a_field_a_gate_requires_and_the_document_lacks` |
| `declared_subject()` falls back to the manifest binding | 44 | **33** | `test_MUTATION_a_claimed_file_that_is_not_there` |
| `checkpoint_status_cells()` reads no rows | 44 | **5** | `test_MUTATION_a_status_row_left_on_the_previous_round` |
| `status_column()` accepts any column | 44 | **3** | `test_MUTATION_a_status_row_left_on_the_previous_round` |
| `gate_assertions()` finds no gates | 44 | **5** | `test_MUTATION_a_field_a_gate_requires_and_the_document_lacks` |
| `SUBJECT_DECLARATION` never matches | 44 | **8** | `test_MUTATION_a_historical_claim_that_is_false_of_its_own_tree` |

Every one of the five named checks, and every helper that decides attribution, is proved
load-bearing. Reproduce by hand: import the module, `setattr` the function, run the
suite. Re-measured after the §10 remediation at **46 tests**, same shape, plus one row:
`_allowlisted_env()` returning `dict(os.environ)` → **1 red**,
`test_the_environment_is_built_and_never_inherited`.

**What this method proves, and what it does not.** It proves the check *classes*. It
does **not** prove every site inside a check, and an independent reviewer was right to
say so: a site-level narrowing within a check that is already red is invisible to it.
Measured, not conceded in the abstract — dropping a single real path from
`claimed_paths`:

| | contour findings | accounting findings | `tests/checkpoint` |
|---|---:|---:|---|
| no skip | 13 | 9 | 46 tests, 0 red |
| one real path skipped | 11 | **7** | 46 tests, 0 red |

Two true findings vanish and nothing anywhere changes status. Reading a narrowing of
this contour is therefore a **review obligation**, not something the contour can measure
about itself, and it is recorded as the seventh entry in `LIMITATIONS` so every run
prints it.

Named tree-mutation cases inside those 44, each green-before on the fixture and
red-after on the mutation, one per coverage area and several per area:

| Coverage area | Mutation of the tree | Test |
|---|---|---|
| terminal state | ratified over an open round | `…_a_ratified_manifest_whose_round_is_still_open_classifies_nothing` |
| terminal state | round eleven opened, `current_round` left at ten | `…_a_later_round_leaves_current_round_behind` |
| terminal state | registry row left on the previous round | `…_a_status_row_left_on_the_previous_round` |
| terminal state | registry row still says blocked | `…_a_status_row_still_calling_the_checkpoint_blocked` |
| terminal state | the two streams declare different subjects | `…_the_accepted_rounds_two_streams_judged_different_trees` |
| terminal state | published digest is not the accepted round's | `…_the_published_digest_is_not_the_accepted_rounds` |
| terminal state | the registry deleted altogether | `…_no_live_record_corroborates_the_terminal_state` |
| live vs historical | a dated report's claim is false of its own tree | `…_a_historical_claim_that_is_false_of_its_own_tree` |
| live vs historical | a dated report's declaration stripped | `…_a_dated_report_that_declares_no_subject_is_read_as_live` |
| live vs historical | the subject commit is unresolvable | `…_a_subject_commit_this_tree_cannot_resolve` |
| live vs historical | every record historical / none historical | `…_the_classifier_going_degenerate…`, `…_an_empty_historical_class…` |
| tag integrity | the tag moved to another commit | `…_the_tag_has_moved` |
| tag integrity | the message states a foreign digest | `…_the_tag_message_states_a_digest_that_is_not_its_own` |
| tag integrity | the message claims a byte identity it lacks | `…_the_tag_message_claims_a_byte_identity_it_does_not_have` |
| tag integrity | lightweight tag / missing tag / two names | `…_a_lightweight_tag…`, `…_does_not_exist`, `…_a_record_naming_a_different_tag` |
| file and hash accounting | a claimed deliverable is absent | `…_a_claimed_file_that_is_not_there` |
| file and hash accounting | a claimed test module is absent | `…_a_claimed_test_module_that_is_not_there` |
| file and hash accounting | one field, two values, two records | `…_one_field_two_values_across_two_live_records` |
| file and hash accounting | an aggregate's members removed / count removed | `…_an_aggregate_whose_members_are_unlisted`, `…_over_an_unstated_number_of_files` |
| cross-gate agreement | either side of the gate moved | `…_the_two_gates_disagree` |
| cross-gate agreement | the field a gate requires is absent | `…_a_field_a_gate_requires_and_the_document_lacks` |
| cross-gate agreement | no gate exists at all | `…_no_gate_at_all_is_a_finding_not_a_pass` |

### 8.2 The changes to `test_cp00_candidate.py`

Green before: the three affected classes collect **154 tests, 0 red** other than the
pre-existing `test_ratification_requires_an_accepted_acceptance_round` (§3.6), which is
excluded from the counts below.

| Mutation | Tests run | Red | Tests that go red |
|---|---:|---:|---|
| *baseline, no mutation* | 154 | **0** | — |
| `_checkpoint_tag_problems()` returns nothing | 29 | **4** | `test_the_checkpoint_tag_resolves_and_the_rule_can_fail` and its three subtests |
| `_checkpoint_tag_claims()` reads no record | 29 | **2** | `…_are_pinned`, `…_the_rule_can_fail` |
| `_resolved_checkpoint_tag()` always returns the floor | 29 | **1** | `…_the_rule_can_fail` |
| `CHECKPOINT_TAG_SERIES` matches anything | 29 | **2** | `…_are_pinned`, `…_the_rule_can_fail` |
| `_needle_present()` always true | 122 | **9** | `test_every_anti_gutting_needle_is_load_bearing_on_its_own` (7 subtests), `…_missing_its_statement_or_its_subject_is_named (missing='the tag it was about')` |
| `_needle_removed()` removes nothing | 122 | **8** | the same needle subtests |
| `_path_is_declared()` always true | 3 | **1** | `test_no_undeclared_path_is_dirty_under_the_owned_trees` |
| `_declared_allowed_paths()` reads no task file | 3 | **1** | `test_no_undeclared_path_is_dirty_under_the_owned_trees` |

The integrator's third requirement — that the tag guards must not go quiet on the change
— is discharged by rows 2 to 6: a record naming the wrong tag, a rename outside the
series, a re-tag onto a spent revision, no record naming a tag at all, and a wave plan
that stops naming any tag of the series each turn a named test red. "The tag has moved" is
covered by the contour instead, in §8.1, because a tag is a ref and not a tracked file and
is therefore outside every anchor in `test_cp00_candidate.py`.

---

## 9. Handoff

**Changed files:** the four in §3.3 plus this report. All inside allowed paths.

**For `W0-INT-02`:** run
`.venv/bootstrap/bin/python tests/checkpoint/cp00_final_state.py` on your candidate. Exit
0 is the acceptance criterion; every finding names you as owner and none requires editing
a file `W0-QA-04` owns. The thirteen open findings are in §4, and the two pre-existing
suite failures in §3.6 — including that **round ten is void at `95f4335`** — are the first
thing to dispose of.

**For `W0-INT-03`:** the tag identity in `tests/contract/test_cp00_candidate.py` now
resolves from the checkpoint records and accepts any tag in `v0.0.x-architecture` at or
above the published floor, so ratifying as `v0.0.1-architecture` no longer requires
editing that file. Move all three claims — `manifest.json:tag`,
`manifest.json:tag_planned`, `contract-manifest.yaml:tag` — together, or
`_checkpoint_tag_problems` will say which one was left behind.

**Not done, deliberately:** no reconciliation of live state records, no erratum, no
manifest rewrite, no ratification, no tag, no push, no edit to
`check_state_records.py`, and no decision on the canonical value of `review_status`.

---

## 10. Remediation after the first independent review returned `REJECT`

One blocking finding and two non-blocking items. All three are closed. Every figure below
was measured after the change, on the same clone, one command at a time.

### 10.1 Blocker — the licence harvest read prose

`_declared_allowed_paths` took every path-shaped code span **anywhere** in an
`## Allowed paths` section. A task file is prose, so a sentence naming a path granted it.
The reviewer's instance was the widened `W0-QA-04.md`, whose closing sentence named the
test tree as a code span and thereby licensed all of `tests/**` — making the containment
guard vacuous for the exact tree it was written to contain.

**It was already live on the tree I measured, and worse than the instance that exposed
it.** Three sections name a reviewed family in order to say it is *not* writable, and the
harvest read each of those three sentences as a licence for the family they deny:

```text
W0-QA-03 -> (…, 'contracts/**')          from "In particular `contracts/**` is not:…"
W0-DOM-02 -> (…, 'scripts/**')           from a closing sentence
W0-ARC-02 -> (…, 'docs/architecture/adr/**')  from a paragraph explaining why it is not writable
W0-INT-01 -> (…, 'docs/architecture/', 'artifacts/', 'docs/architecture/REPOSITORY_LAYOUT.md')
                                          from continuation prose inside list items
```

I did not know this and would not have found it by reading; the review did. Recorded as
such.

**The repair is not a rewording.** The integrator corrected the sentence and explicitly
said not to rely on that, and they are right: a guard whose behaviour depends on how a
sentence is punctuated is not a guard. **A glob is now taken only from a line that opens
a Markdown list item** (`^\s*[-*+]\s+\S`). The harvest never reads prose, so it does not
matter what prose says.

Two alternatives were measured and rejected:

* *stop the section at its first non-list paragraph* — the integrator's other suggestion.
  It loses real declarations: `W0-CLN-01`, `W0-DOM-02` and `W0-INT-03` each introduce
  their list with a sentence, and `W0-INT-03`'s section **opens** with two paragraphs;
* *admit indented continuation lines* — re-opens the hole, because every continuation in
  all twenty task files is prose.

Every real declaration in all twenty files sits on a list-item line. Measured before/after
across the whole task directory:

| | files with declarations | globs dropped | globs gained |
|---|---:|---|---:|
| before → after | 20 → 20 | 7, listed above, all from prose | **0** |

`W0-QA-01` keeps `('tests/contract/test_cp00_candidate.py', 'docs/program/reviews/W0-QA-01.md')`
and `W0-QA-04` keeps `('tests/checkpoint/**', 'tests/contract/test_cp00_final_state.py',
'docs/program/reviews/W0-QA-04.md')`.

**Verified against the shipping shape, not only a synthetic.** The widened section was
reconstructed with the integrator's original offending sentence — the entry added as a
list item, followed by *"This task is the only writer of `tests/**` in this batch."* —
and the harvest measured against it:

```text
W0-QA-04 globs: ('tests/checkpoint/**', 'tests/contract/test_cp00_final_state.py',
                 'docs/program/reviews/W0-QA-04.md', 'tests/contract/test_cp00_candidate.py')
  _path_is_declared('tests/contract/nobody-declared-this.py') = False
  _path_is_declared('tests/contract/test_cp00_candidate.py')  = True
```

All four real declarations read, the sentence contributes nothing, containment intact.

**New test:** `WriteBoundaryTests.test_the_licence_harvest_reads_list_items_and_not_prose`,
on a synthetic task tree, asserting **both directions** — a span in a closing sentence, in
a continuation line, and in another section is not a licence; **the same span moved onto a
list item is**. Without the second half the rule could be "reject everything" and still
pass. The fixture also carries `` `**` `` on a list item of its own: it is the one
non-path token whose rejection is not cosmetic, because translated as a glob it becomes
`.*` and licenses the whole repository.

**New assertion in the existing guard:** `contracts/**`, `scripts/**` and
`docs/architecture/adr/**` must appear in no task's harvested globs, so the specific
inversion above cannot come back.

**Mutation evidence for the repair** (baseline for these three classes: 155 tests, 0 red
excluding the pre-existing failure):

| Mutation | Tests run | Red | Tests that go red |
|---|---:|---:|---|
| `_LIST_ITEM` matches every line — *the prose harvest, restored* | 4 | **2** | `…_reads_list_items_and_not_prose`, `…_no_undeclared_path_is_dirty…` |
| `_PATH_GLOB` matches any backticked token | 4 | **1** | `…_reads_list_items_and_not_prose` |
| `_declared_allowed_paths()` reads no task file | 4 | **2** | both |
| `_path_is_declared()` always true | 4 | **4** | `…_no_undeclared_path_is_dirty…` and three subtests |

`_PATH_GLOB` was **not** load-bearing when the remediation was first written — the matrix
said 0 red — so the fixture was extended until it was. A filter with no measured effect is
the defect this checkpoint exists to end, and it does not get a pass for being mine.

### 10.2 Non-blocking — the test module sat outside its own spawn guard

`test_cp00_final_state_contour.py` spawned `subprocess.run(["git", …])` with the inherited
environment and an unqualified `git`, in the one file of the deliverable that did what
`E-06` describes. `SpawnChokepointTests` did not catch it because the scan read
`cp00_final_state.py` and nothing else — a scan scoped to one file is a convention with a
spot-check, which is the shape this checkpoint keeps re-learning.

Closed, both halves:

* the call now goes through `contour._git`, so it gets a built environment and a program
  resolved on the trusted path. The module no longer imports `subprocess` at all;
* `test_no_module_of_this_deliverable_spawns_git_directly` scans **every Python file this
  task delivers** — the contour, both test modules, and any `test_*.py` added beside them
  tomorrow. The permitted site is located by finding `_git`'s `FunctionDef` in the AST and
  taking its line range, not by an offset from the function's first line, because an
  offset is a second thing to keep in step and a docstring edit would silently move it;
* `SPAWN_APIS` widened from 4 names to 10;
* `test_the_scan_would_notice_a_direct_spawn` plants a spawn in a source string and
  requires the scan to find it, so the scan cannot pass by being blind.

Mutation: `_allowlisted_env()` returning `dict(os.environ)` → 1 red,
`test_the_environment_is_built_and_never_inherited`.

### 10.3 Non-blocking — the mutation method proves classes, not sites

Accepted, measured and recorded rather than argued: see the table in §8.1 and
`LIMITATIONS` entry 7. Dropping one real path from `claimed_paths` removes two true
findings, 9 → 7, while `tests/checkpoint` stays at 46 tests / 0 red and
`tests/contract` keeps its exact five failures. Nothing in the deliverable can detect
that; a reviewer reading a narrowing is the control, and now the run says so on every
invocation.

### 10.4 State after remediation

| Command | Result | Change |
|---|---|---|
| `discover -s tests/checkpoint` | `Ran 46 tests … OK`, `exit=0` | 44 → 46 |
| `discover -s tests/contract` | `Ran 340 tests … FAILED (failures=5)`, `exit=1` | 338 → 340, **same five failures** |
| `tests/checkpoint/cp00_final_state.py` | 13 findings, `exit=1` | unchanged |
| `validate_bootstrap.py` | `PASS`, `exit=0` | unchanged |
| `git diff --check` | no output, `exit=0` | unchanged |
| `git rev-list -n1 v0.0.0-architecture` | `39a3a6430bd97c38cb20bafc793fc9d077d0df8e` | unchanged, annotated |
| `git status --porcelain -uall` | the same five allowed paths | unchanged |

The failure set is unchanged by the remediation: the base commit's own two, plus three
contour checks every one of whose findings names `W0-INT-02`. That is the replacement
gate, and it holds.

---

## 11. Reopening — the pin moved one level down, and four more of it

`W0-INT-02`'s reviewer found `tests/contract/test_cp00_candidate.py:11342`:

```python
self.assertEqual(CHECKPOINT_TAG, "v0.0.0-architecture")
```

evaluated against the **live** `REPOSITORY_ROOT`. §6.1 removed the module-level literal
and this one survived one level down, inside the anti-vacuity test that proves the
resolution works. Performing the ratification delta — flipping `manifest.json:tag`,
`manifest.json:tag_planned` and `contract-manifest.yaml:tag` to the successor — resolves
`CHECKPOINT_TAG` to the new tag and turns that line red, and `W0-INT-03` may not write
`tests/**`. A final state no task is licensed to reach, for the sixth time in this phase
and the second time inside my own repair for it.

The integrator calls it their defect as much as mine. It is mine: I wrote the line, in
the same change whose whole purpose was to delete lines of that shape.

### 11.1 The fix

The assertion has to keep proving that resolution is *real* without asserting *which*
tag is current. It is now anchored to the records it reads and to the pinned rule, with
no instance anywhere:

```python
self.assertEqual(set(original.values()), {CHECKPOINT_TAG})   # the constant is what the records say
self.assertRegex(CHECKPOINT_TAG, CHECKPOINT_TAG_SERIES)      # and it is in the series
self.assertGreaterEqual(resolved_revision, floor_revision)   # and at or above the floor
```

The proof that a resolution is a resolution and not a constant stays where it cannot
rot: the fixture branch, which builds a tree whose records say `v0.0.1-architecture` and
requires `_resolved_checkpoint_tag` to return `v0.0.1-architecture`. A fixture carries
its own expected value, so it is not a pin on the repository.

### 11.2 Then I swept for a fourth, and found four

An AST sweep over every file this task delivers, for `assertEqual`/`assertIn`/
`assertRegex` where one side reads the repository and the other is a string literal, ran
to 24 candidate sites; triaged, four were the same defect and all four are mine.

| # | Site | Why it is the same shape | Fix |
|---|---|---|---|
| 1 | `test_cp00_candidate.py:11342` | the reviewer's finding | anchored to the records, the series and the floor |
| 2 | `test_cp00_final_state_contour.py` `RealRepositoryTests.setUpClass` — `TagFacts.gather(ROOT, "v0.0.0-architecture")` | a **silent** one: the superseded tag is never deleted, so after a supersession this class would have gone on measuring the old tag for ever with every assertion green | name resolved from the records via `contour.recorded_tag` |
| 3 | `test_cp00_final_state.py` `TagIntegrityTests.test_the_checkpoint_tag_exists_and_is_annotated` — `assertTrue(self.tag.exists)` | the records move to the successor *before* that tag exists — the manifest is written in the commit that gets tagged — so this fails on `W0-INT-02`'s own output tree | existence is no longer asserted here; it arrives as a `tag-integrity` finding owned by `W0-INT-03` |
| 4 | `cp00_final_state.py` `main()` — `--tag` defaulting to `"v0.0.0-architecture"` | on a tree whose records had moved, the contour would verify the **superseded** tag and then report the disagreement it had itself introduced | `--tag` defaults to `None`; the name comes from `recorded_tag(tree)` |

Sites 2 to 4 were not in the reviewer's finding and would each have bitten `W0-INT-02` or
`W0-INT-03`. Site 3 in particular: `_tag_name` also carried a literal *fallback*, which
answers "which tag?" with a guess at the very moment the records stop saying. It returns
`""` now, and the contour reports "no checkpoint record names a tag" — the truth, with
an owner.

There is one shared resolver, `contour.recorded_tag(tree)`, used by all three call sites.
`grep '"v0\.0\.0-architecture"'` over the three delivered files returns nothing. The only
remaining literal of that string in the whole deliverable is `CHECKPOINT_TAG_FLOOR`,
which is by design an immutable historical fact and not a current value.

Four sites survived triage as safe and are recorded so a reviewer need not re-derive
them: `bound_subject(...) == 'round-10'` twice, a **fixture** value set in the same file;
`_resolved_checkpoint_tag(green) == 'v0.0.1-architecture'`, the fixture that proves
resolution is real; and two `assertRegex` calls that assert a *shape*, not a value.

### 11.3 A fifth of the same family, in the containment guard

Not found by the reviewer, found by the same sweep question. §10.1's repair asserted

```python
for family in ("contracts/**", "scripts/**", "docs/architecture/adr/**"):
    self.assertNotIn(family, every_glob)
```

Those three literals were the *symptoms* of the prose harvest, and S01 reopens
`scripts/**` and `contracts/**` — the first W1 task file to declare one as a list item
would have turned that assertion red, in a file W1 may not write. The same trap, in the
fix for the trap.

Replaced with the structural property those literals were standing in for:
`_prose_only_path_spans` computes, independently of the harvest, the path-shaped spans
that appear in an `Allowed paths` section on **no** list item, and the test requires the
intersection with the licence to be empty. On this tree the prose-only set is non-empty
(7 spans across 5 files, asserted non-empty so the intersection is not vacuous) and the
intersection is `{}`. It needs no edit when a family legitimately reopens, and it fails
the moment anything reaches the licence from a sentence.

The same question applied to the owner check in §10: `test_no_finding_is_this_task_s_own_defect`
now resolves each finding's owner against `docs/program/tasks/<owner>.md` rather than
comparing it with a list of task ids.

### 11.4 Mutation — the new form still fails when resolution breaks

`TableExpectationTests`, 29 tests, run on **both** trees: this clone, whose records say
`v0.0.0-architecture`, and a copy with the three delta sites flipped to
`v0.0.1-architecture`.

| Mutation | base tree | flipped tree |
|---|---:|---:|
| *baseline* | **0** red | **0** red |
| `_resolved_checkpoint_tag()` always returns the floor | **1** | **1** |
| `CHECKPOINT_TAG` frozen to the floor — the constant stops tracking the records | **0** | **2** |
| `_checkpoint_tag_claims()` reads no record | **2** | **2** |
| `_checkpoint_tag_problems()` returns nothing | **4** | **4** |
| `CHECKPOINT_TAG_SERIES` matches anything | **2** | **2** |

Row three is the honest one and is stated rather than smoothed: on a tree whose records
still say the floor, freezing the constant *to* the floor is indistinguishable, because
there is nothing to distinguish — the fixture branch is what carries that property there,
and the live assertion picks it up the moment the records move. Every other mutation is
caught on both trees.

### 11.5 Flipping the three delta sites leaves the failure set unchanged

The tree was copied with `cp -a` — not cloned, and with no suite running — and
`manifest.json:tag`, `manifest.json:tag_planned` and `contract-manifest.yaml:tag` flipped
to `v0.0.1-architecture`. `CHECKPOINT_TAG` then resolves to `v0.0.1-architecture` and
`_checkpoint_tag_problems` returns `[]`.

| Tree | `discover -s tests/contract` | `discover -s tests/checkpoint` |
|---|---|---|
| this clone, records at `v0.0.0` | `Ran 340`, **5 failures**, `exit=1` | `Ran 46`, `OK`, `exit=0` |
| flipped, records at `v0.0.1`, **fix applied** | `Ran 340`, **5 failures**, `exit=1` | `Ran 46`, `OK`, `exit=0` |
| flipped, records at `v0.0.1`, **pin restored** | `Ran 340`, **6 failures**, `exit=1` | — |

The five are the same five by name in both fixed runs — the base commit's two, plus the
three contour checks. The single test, run alone on the flipped tree:

```text
before the fix:  AssertionError: 'v0.0.1-architecture' != 'v0.0.0-architecture'   exit=1
after the fix:   Ran 1 test ... OK                                                exit=0
```

The contour on the flipped tree reports 13 findings, owners `{W0-INT-02: 12,
W0-INT-03: 1}` — the one being *"the records name this tag and it does not exist in this
repository"*, which is the correct and expected reading of a checkpoint that has been
prepared and not yet published.

### 11.6 State after the reopening

| Command | Result | Change from §10.4 |
|---|---|---|
| `discover -s tests/checkpoint` | `Ran 46 tests … OK`, `exit=0` | unchanged |
| `discover -s tests/contract` | `Ran 340 tests … FAILED (failures=5)`, `exit=1` | unchanged, same five |
| `tests/checkpoint/cp00_final_state.py` | 13 findings, 7 limitations, `exit=1` | unchanged |
| `validate_bootstrap.py` | `exit=0` | unchanged |
| `git diff --check` | no output, `exit=0` | unchanged |
| `git rev-list -n1 v0.0.0-architecture` | `39a3a6430bd97c38cb20bafc793fc9d077d0df8e` | unchanged, annotated |
| `git status --porcelain -uall` | the same five allowed paths | unchanged |

**A note for whoever writes the acceptance gate.** Its wording — *"contour checks all of
whose findings name `W0-INT-02` as owner"* — needs one word widening. Ref-side findings
correctly name `W0-INT-03`, because a record naming an unpublished tag is closed by
publishing it, not by editing the record. The suite now asserts the property that
actually matters: no finding names `W0-QA-04`, and every owner is a task that exists.

---

## 12. Round three — the same defect, two constants further along

`docs/program/EXECUTION_PLAN.md` §3.7 is an independent audit of the **task graph**,
taken after round two's bytes were pinned. It found the CP-00 barrier unreachable on two
blockers, both inside this file, both the shape §3.6.4 and §11.1 already closed twice: a
literal pinning a superseded instance. This section and §13 close them, and §14 is the
measurement.

**Tree and base.** `762af0391bb199aaef0bb82f8ece5b11dae5f90b`, worked in
`/root/projects/PDF-Analysis-qa04r3`, a `cp -a` copy — not a clone and not a linked
worktree, because the sandbox tests copy the working tree and the object database. Left
uncommitted. Nothing was tagged, pushed or moved; `git rev-list -n1 v0.0.0-architecture`
returns `39a3a6430bd97c38cb20bafc793fc9d077d0df8e`, unchanged.

**The two trees every claim below is measured on.** A repair that agrees with the old
literal on today's tree and is never exercised on tomorrow's is the defect wearing a
function call, so every figure comes in a pair:

| Tree | What it is | How it was built |
|---|---|---|
| **A** | this deliverable, records still saying `W0-INT-01` and `v0.0.0-architecture` | the working tree |
| **T** | the tree the recovery actually produces: round eleven open and unratified, the ratification act reassigned to `W0-INT-03`, the anchor documents reconciled, the freeze taken on it | `cp -a`, then three scripted commits; `scratchpad/qa04-r3/make_tree_t.sh` |

Tree **T** is the honest successor tree, not a records-only edit: its wave plan assigns
the act to `W0-INT-03`, its index carries that task's status row, `W0-INT-03.md` carries
the banner denial, `W0-INT-01.md` stops making a statement that is no longer its to make,
and `docs/architecture/**` is restored from the reviewed candidate because an unratified
candidate licenses no delta there. On tree T, before this round's changes,
`_acceptance_problems` on the **live** tree returns three findings and the contract suite
fails 138 of 340; after them the live tree returns **none** and the suite fails 29, with
**no test red that was green before**.

A fourth tree, **R**, is used in §13: a *ratified* tree whose freeze commit is taken after
the ratification. It isolates the sandbox baseline with everything else held constant.

### 12.1 Blocker A — `RATIFYING_TASK` was pinned to `W0-INT-01`

Reproduced first, on an untouched copy of the base commit, one field changed and nothing
else:

```text
$ ratification.task = 'W0-INT-01'  -> []
$ ratification.task = 'W0-INT-03'  -> ["artifacts/checkpoints/CP-00/manifest.json:
    ratification.task is 'W0-INT-03'; only W0-INT-01 may ratify CP-00"]
```

`W0-INT-03`'s delta is required to write `ratification.task: W0-INT-03`, and `W0-INT-03`
may not write this file. That is §3.6.4's shape exactly: a final state no task is
licensed to reach.

Resolved the way `CHECKPOINT_TAG` is, at `tests/contract/test_cp00_candidate.py:316`.
What is pinned is the **series and the rule**; the instance is read out of the records:

* `RATIFYING_TASK_SERIES` — `^W0-INT-(\d{2})$`. CP-00 is ratified by an integration task
  of wave W0; a reassignment to anything else is refused.
* `RATIFYING_TASK_FLOOR` — `W0-INT-01`. An immutable historical fact: that task did take
  a ratification act on round ten and cannot un-take it, so the line never needs editing.
* `_ratifying_task_claims` reads the **wave plan's single assignment row** and, once the
  act has been taken, `manifest.json:ratification.task`. `_ratifying_task_problems`
  requires them to agree, to be in the series, to be at or above the floor, and to name a
  task that has a file.
* `RATIFYING_TASK = _resolved_ratifying_task(REPOSITORY_ROOT)`.

The claim set is **state-dependent and is asserted as such**: an unratified candidate —
which is what a freeze is taken on — carries no `ratification` object at all, so the plan
is the only record that speaks. Requiring both unconditionally would have made a
candidate unable to resolve its own ratifier, which is the trap one layer in.

It does not resolve the value out of the record it checks. `_ratification_record`
compares a *candidate tree's* `ratification.task` against the constant resolved from
`REPOSITORY_ROOT`, so `test_a_record_from_the_wrong_task_licenses_nothing` still fires,
and one record moving alone is still refused — measured on tree A with only the manifest
field changed:

```text
resolved: W0-INT-01
  - the records name more than one ratifying task, which is a reassignment declared in
    one record and not the others: …manifest.json:ratification.task = 'W0-INT-03';
    …W0.3_ratification_integration.md:CP-00 review ratification = 'W0-INT-01'
  - ratification.task is 'W0-INT-03'; only W0-INT-01 may ratify CP-00
```

On tree A the resolved value is `W0-INT-01` and on tree T it is `W0-INT-03`, and
`_ratifying_task_problems` is empty on both.

### 12.2 The sweep — four more sites of the same instance

Fixing the constant named in the audit and stopping there is the method §4.5 warns
against. Swept by shape, the same fact — *which task ratifies* — was written down four
more times:

| # | Site | Why it is the same defect | Fix |
|---|---|---|---|
| 1 | `RATIFYING_TASK_FILE = "docs/program/tasks/W0-INT-01.md"` | the resolver answers "who ratifies?" and then opens a file named after the superseded answer | derived: `f"docs/program/tasks/{RATIFYING_TASK}.md"` |
| 2 | `RATIFICATION_PUBLICATION_RECORDS[0]["denials"]` — `` `W0-INT-01` is blocked ``, `` \| `W0-INT-01` \| blocked \| `` | a reconciled plan correctly stops calling an accepted task blocked; the anchor then reported rot on a document that had been brought up to date | `` `{task}` `` placeholder, resolved by `_needle_resolved` |
| 3 | the same entry's `must_still_contain` `` `W0-INT-01` ``, and the index entry's `row: "program/tasks/W0-INT-01.md"` | same | same placeholder |
| 4 | `_task_banner_problems` iterating `COMPLETED_TASK_FILES` alone, and `assertIn(RATIFYING_TASK_FILE, COMPLETED_TASK_FILES)` | a task assigned the act after the reviewed candidate is not one of the seventeen, so the loop would never visit it — **silently**, because a branch nothing reaches reports nothing | the loop is over `COMPLETED_TASK_FILES \| {RATIFYING_TASK_FILE}`; the membership assertion becomes a series-and-existence assertion |
| 5 | `_post_freeze_delta_problems`'s licence | the ratifying task's banner **must** move at ratification; outside the licence, the one file the act is required to rewrite would void the round it authorises | `{RATIFYING_TASK_FILE}` added where the licence is computed |

Site 5 is added at the use site rather than to `POST_FREEZE_DELTA_CEILING` deliberately.
The ceiling is a table pinned to a literal in
`test_the_two_ratification_ceilings_are_pinned`; a table with a resolved member could not
be pinned at all, and the guard would go quiet on thirty-seven fixed paths to accommodate
one. The ceiling stays literal; the resolved member sits beside
`_declared_evidence_paths(manifest)`, which is already computed there for the same reason.

**The placeholder, and why it is not a shape.** `CHECKPOINT_TAG_NEEDLE` answers "which
revision?" with a series, because a plan naming any tag of the series has not been
gutted. A **denial** is the opposite kind of needle — it is the anti-vacuity anchor, the
exact claim ratification retracts — so `` re:`W0-INT-(\d{2})` is blocked `` would be
satisfied by a plan calling some *other* integration task blocked. `NEEDLE_TASK_PLACEHOLDER`
keeps both properties: the table entry is a literal a reviewer reads and a pin holds, and
what is matched against the document is the instance the records resolve to.

### 12.3 Item 3 — the contour's round-ten literal

`tests/checkpoint/test_cp00_final_state_contour.py:415`. Measured before repairing it:
the literal is **fixture-local**. `"**ratified** on acceptance round ten"` is a
replacement against the registry row the same module builds 230 lines above, and the four
`.replace` mutations in that file all fail closed — an unmutated tree is green and
`assert_fires` then reports that nothing fired. So the audit's stated reason, that no task
can write this path after the freeze, does not bite here: no repository change can move
the value. **Stated rather than smoothed**, because the audit asserts otherwise.

It is repaired anyway, because it is still one value said twice, and because the real
defect in the neighbourhood is bigger than the literal: `str.replace` is silent when its
needle is absent, so a mutation written against a fixture that has moved on quietly tests
the *pristine* tree. "It fails closed today" is a property of today's fixture, not of the
method.

* the fixture's round is `ROUND = 10` / `ROUND_WORD = "ten"`, written once and used by the
  manifest, both reports, the registry row and both mutations;
* every string replacement in the module goes through `_ContourCase.assert_replaced`,
  which says *the mutation never happened* instead of *the check did not fire*;
* `test_no_mutation_edits_the_fixture_without_proving_it_edited_something` parses the
  module and refuses a bare `str.replace` inside any function but the helper, and proves
  the helper itself can fail.

### 12.4 What stayed pinned, and why — `RATIFYING_TASK_BANNER_DENIAL`

One resolver, two derivations, one phrase left pinned; the coordinator asked which, so:
`RATIFYING_TASK` is resolved, `RATIFYING_TASK_FILE` is derived from it, and
`RATIFYING_TASK_BANNER_DENIAL = "ratification and publication blocked"` stays a literal.

It names no instance. It is the banner's standing claim about the *state*, in exactly the
position `STATE_DOCUMENT_DENIAL` occupies, and it is checked in both directions so it
cannot rot silently. Resolving it would mean reading the phrase out of the banner it is
used to check, which is the vacuity `_state_document_problems` refuses in as many words.

Its cost is real and is recorded rather than asserted away: a reassignment obliges
whoever writes the new ratifying task's file to use this phrase verbatim while CP-00 is
unratified. That is a requirement which can be met **before** the freeze — `W0-INT-02`
writes the body of `W0-INT-03.md` under §3.7.5's widened licence — so it is not a state no
task can reach, and tree T meets it. The finding's text now says so, naming the task to
write rather than only offering to re-anchor the module.

### 12.5 Mutation battery, on both trees

Ten named tests, run on tree **A** and tree **T**, once per source-level mutation of
`tests/contract/test_cp00_candidate.py`. Cells are the number of red tests.
`scratchpad/qa04-r3/battery.py`.

| Mutation | A | T |
|---|---:|---:|
| *baseline* | **0** | **0** |
| M1 `_resolved_ratifying_task` always returns the floor | 1 | 6 |
| M2 `RATIFYING_TASK` frozen to the floor | **0** | 5 |
| M3 `_ratifying_task_claims` reads no record | 2 | 6 |
| M4 `_ratifying_task_problems` returns nothing | 7 | 7 |
| M5 `RATIFYING_TASK_SERIES` matches anything | 2 | 2 |
| M6 `RATIFYING_TASK_FILE` written back as the literal | **0** | 4 |
| M7 `_pre_ratification_baseline` returns the freeze commit | 1 | 1 |
| M8 `_needle_resolved` never expands the placeholder | 5 | 5 |
| M9 the ratifying file dropped from the banner loop | **0** | 1 |

**Three rows do not move on tree A, and they are stated rather than smoothed.** M2, M6
and M9 are indistinguishable there for the same reason round two's §11.4 row three was:
on a tree whose records still say `W0-INT-01`, freezing the constant *to* `W0-INT-01`,
writing its file back as a literal, or dropping a file that is already one of the
seventeen from a loop, changes nothing that any assertion could see. There is nothing to
distinguish. What carries those three on tree A is the fixture branch of
`test_the_ratifying_task_resolves_and_the_rule_can_fail`, which builds its trees in a
temporary directory and therefore measures the same thing on both; and on tree T every one
of them is caught by name.

M4's 7 on both trees is one test failing seven sub-tests, not seven tests.

### 12.6 The rule proved on trees this module builds

`test_the_ratifying_task_resolves_and_the_rule_can_fail` is the anti-vacuity half and
runs identically on A and T, because every tree in it is built in a temporary directory:

* a tree whose records both say `W0-INT-03` is **accepted** with no edit to this module,
  and `_ratification_record` on it reports nothing about who may ratify — while the same
  bytes with the constant pinned to the floor reproduce the audit's finding verbatim.
  One mutation, two outcomes, on the same bytes;
* one record left behind → *"name more than one ratifying task"*;
* reassigned outside the series → *"not in the ratifying-task series"*;
* no record names a task → the fallback to the floor is reported, not taken silently;
* the plan assigns the act twice, or one row co-assigns it → both named;
* the assigned task has no file → *"is not a task in this program"*;
* with `RATIFYING_TASK_FLOOR` patched to `W0-INT-03`, reassigning back to `W0-INT-01` →
  *"moves forward or not at all"*.

---

## 13. Blocker B — the sandbox baseline

### 13.1 What the defect is, isolated

`_reset_to_the_frozen_tree` rebuilds every sandbox from `_freeze_commit`, and
`normalise_to_candidate` then writes `ratified: false` into the manifest. While the freeze
is taken on an unratified checkpoint those two agree — which is every freeze this
repository has taken, `2ea7b68` included. Once a freeze is taken *after* a ratification
they stop agreeing, and the sandbox builds a tree whose record denies a ratification its
own documents still describe.

Isolated on the integrated tree with one thing changed — `_freeze_commit` pointed at
`39a3a64`, the round-ten ratification commit — and nothing else:

```text
freeze 2ea7b68 (today's):   live 0 problems | sandbox  0 problems
freeze 39a3a64 (ratified):  live 0 problems | sandbox 10 problems
```

All ten are anchors the sandbox had itself destroyed: the `CURRENT_STATE.md` denial, the
two wave-plan rows, the S00 checklist, the index status, the `W0-INT-01` banner denial,
and four "degenerate requirement" reports for post-ratification text that is present
while the record says unratified. The live tree is perfectly consistent throughout.

The audit reported 21 on its own simulated tree and about 135 of 141 cascading contract
failures. My first simulation reproduced the shape but not the isolation — it flipped
`ratified: false` on a tree whose documents were post-ratification, so the **live** tree
reported the same eleven findings as its sandbox, and those eleven are reconciliation debt
owned by `W0-INT-02`, not a sandbox defect. Tree **R** above is the clean isolation: a
ratified tree, self-consistent, whose freeze is re-taken after the ratification. There the
live tree reports two findings (an `acceptance.md` row my simulation did not write) and
the sandbox reports ten, and the contract suite fails **139 of 340**.

### 13.2 The option that was rejected, and what it would have cost

**Take the baseline from `REVIEWED_CANDIDATE_COMMIT`.** Measured, on the integrated tree,
by resolving the baseline to `92e13fa` and letting `normalise_to_candidate` run:

```text
subprocess.CalledProcessError: ['git','-C',<sandbox>,'rm','--cached','--quiet','--', …38 args]
   returncode: 128
   stderr:     fatal: pathspec '.vscode/settings.json' did not match any files
tracked strangers at REVIEWED_CANDIDATE_COMMIT: 30
including tests/contract/test_cp00_candidate.py: True
partially reset tree, _acceptance_problems: 17
candidate CURRENT_STATE.md carries STATE_DOCUMENT_DENIAL: False
```

Three separate costs, any one of them disqualifying. The reset **aborts**: `git rm
--cached` exits 128 and the exception propagates out of every sandbox construction, so
the whole `RatificationRecordTests` family errors rather than fails. The candidate
predates this module, the entire CP-00 evidence bundle and every acceptance report, so a
completed reset would unlink 30 tracked paths **including the module under test**. And it
does not fix the finding it targets: `CURRENT_STATE.md` at the candidate says "Nothing is
frozen, nothing is ratified", not the sentence `STATE_DOCUMENT_DENIAL` anchors, so the
anchor-rot report fires anyway.

**Normalise the state documents alongside the manifest.** Rejected on the ten findings
above: closing them means the sandbox writing the denial sentence, two wave-plan rows, an
index status, an unticked checklist and a banner denial — and *removing* `S01` and
"accepted and integrated" from three documents, since four of the ten are "degenerate
requirement" reports that need text taken away. Every one of those anchors would then be a
check on text the sandbox wrote thirty lines earlier. The two-directional anchors exist
precisely so that a reworded claim fails loudly while re-anchoring is still cheap; under
this option they could not fail for any repository reason at all. It also means carrying,
inside the test module, a second copy of what an unratified tree says — a model that rots
independently of the six documents it models.

### 13.3 What was taken

`_CheckpointSandbox._pre_ratification_baseline()`: the newest tree **at or before the
freeze commit** at which CP-00 was not ratified. Three conditions, and the third is what
keeps it from degrading into "any old commit" — the revision must say `ratified` is not
true, must carry a self-consistent freeze of its own, and that freeze must **reproduce**
over the commit's own tree, which is what `test_a_recovered_freeze_is_a_real_freeze` asks
of `_ensure_a_frozen_round` and is asked here for the same reason.

Nothing is invented: the tree comes out of the program's own history, from a commit at
which the records and the documents agreed. When the freeze is itself unratified — today,
and every freeze an honest recovery takes — the walk does not run at all and behaviour is
byte-for-byte what it was.

`_reset_to_the_frozen_tree` is unchanged in meaning and is now a one-line call into
`_reset_to(commit)`; `normalise_to_candidate` calls `_reset_to(self._pre_ratification_baseline())`.

Two probes, both running on A and T:

* `test_the_baseline_is_a_tree_that_denies_the_ratification` — with the freeze forced to a
  commit history says was ratified (asserted, not assumed), the resolved baseline must be
  a commit at which CP-00 was **not** ratified and whose `CURRENT_STATE.md` carries the
  denial, and the normalised sandbox's `_state_document_problems` must be empty;
* `test_MUTATION_taking_the_baseline_from_the_freeze_commit_goes_red` — the repair
  removed, the old behaviour restored, and the state-document anchor must report the
  contradiction by name with at least five findings in the cascade.

`test_a_recovered_freeze_is_a_real_freeze` is re-anchored from the freeze commit to the
baseline, and gains an assertion that the baseline is not a ratified tree. On A the two
are the same commit and nothing about it changes.

### 13.4 What it does not cover, recorded rather than asserted away

The walk can only reach trees this repository actually produced. On a tree that reassigned
the ratification act **after** its last unratified commit — tree T with its freeze forced
to `39a3a64`, which is not a state the recovery produces but is a state a test can
construct — the baseline is a tree naming the previous ratifier, and the wave-plan,
index and banner anchors then report the reassignment. That is a limit of history, not of
the resolver: there is no tree to find. It is why
`test_the_baseline_is_a_tree_that_denies_the_ratification` asserts
`_state_document_problems` is empty rather than the whole of `_acceptance_problems` — the
narrower claim is the one that holds on both trees, and the wider one would have been true
of tree A only.

Where history holds no unratified revision at all, the resolver returns the freeze commit
and the anchors report the contradiction by name. It does not guess.

---

## 14. Failure sets, before and after

Every run is `.venv/bootstrap/bin/python -m unittest discover -s tests/contract` in the
tree named, with no other process touching that tree. Exit codes are the runner's.

| Tree | Before this round | After this round |
|---|---|---|
| **A** — the integrated tree, `762af03` | `Ran 340`, **5 failures**, exit `1` | `Ran 343`, **5 failures**, exit `1` |
| **R** — ratified, freeze re-taken after the ratification | `Ran 340`, **139 failures**, exit `1` | `Ran 343`, **5 failures**, exit `1` |
| **T** — the recovery's own candidate, act reassigned to `W0-INT-03` | `Ran 340`, **138 failures**, exit `1` | `Ran 343`, **29 failures/errors**, exit `1` |

On tree **A** the five are the same five by name, before and after — the base commit's own
two, plus the three contour checks whose findings are `W0-INT-02`'s and `W0-INT-03`'s:

```text
FAIL: RatificationRecordTests.test_ratification_requires_an_accepted_acceptance_round
FAIL: SandboxResetTests.test_unresettable_names_a_reset_that_did_not_finish
FAIL: CrossGateAgreementTests.test_the_two_ratification_gates_agree
FAIL: FileAndHashAccountingTests.test_every_file_the_checkpoint_claims_exists_and_…
FAIL: TagIntegrityTests.test_the_tag_has_not_moved_and_its_message_is_true_of_its_commit
```

On tree **R** the five after are four of those plus
`TerminalStateTests.test_the_records_describe_the_state_the_manifest_derives`, which is my
simulation's own gap — its registry still says round ten while its manifest says eleven —
and `test_unresettable_…` is green there because that tree is committed and has no
untracked strangers. `_acceptance_problems` in the sandbox goes **10 → 0**.

On tree **T**, comparing failure sets by name rather than by count:

```text
red after but green before: (none)
red before and green after: 59 distinct test names
```

and the live tree's own verdict goes from three findings to none:

```text
before: anchor rot: …W0.3_ratification_integration.md no longer carries '`W0-INT-01` is blocked'
        anchor rot: …W0.3_ratification_integration.md no longer carries '| `W0-INT-01` | blocked |'
        anchor rot: the docs/program/tasks/W0-INT-01.md banner no longer says
                    'ratification and publication blocked'
after:  []                       and the sandbox, likewise: 0
```

Those three findings are the whole of blocker A's second half: a correctly reconciled plan
that stops calling an accepted task blocked was being reported as anchor rot, and the only
way to satisfy the check was to write something false.

The 29 remaining on tree T are the simulation's own incompleteness, not this deliverable's:
the evidence bundle is not rewritten for round eleven, `artifact_manifest_sha256` still
carries the post-ratification value against a candidate-restored architecture family
(`CP00-B4`), and the registry still names round ten. All of them are `W0-INT-02`'s
reconciliation, and all of them are red before this round as well.

**`tests/checkpoint`**: `Ran 47 tests … OK`, exit `0`, on A and on T. It was 46; the
forty-seventh is §12.3's no-op-mutation guard.

### 14.1 Contour mutations, both trees

`discover -s tests/checkpoint` under one source mutation of the contour test module:

| Mutation | A | T |
|---|---:|---:|
| *baseline* | **0** | **0** |
| C1 `assert_replaced` stops asserting | 1 | 1 |
| C2 the fixture's round word moves and the mutations do not | 2 | 2 |
| C3 the round said twice again, the pre-repair form | 1 | 1 |

C3 is the point of §12.3: with the literal restated, moving `ROUND_WORD` leaves a
mutation that mutates nothing, and the guard names it.

### 14.2 §11.3's replacement, measured rather than inherited

Round two's reviewer noted that §11.3's `_prose_only_path_spans` shipped under §10.1's
mutation row, measured against a different assertion. Measured here in its own right, over
`WriteBoundaryTests.test_no_undeclared_path_is_dirty_under_the_owned_trees` and
`…test_the_licence_harvest_reads_list_items_and_not_prose`:

| Mutation | A | T |
|---|---:|---:|
| *baseline* | **0** | **0** |
| P1 `_prose_only_path_spans` returns nothing | 1 | 1 |
| P2 the harvest reads prose as well as list items | 2 | 2 |

---

## 15. The seven observations from round two's review

Round two was accepted with no findings; these were non-blocking notes. Each is closed
here, in code where a code change was owed and in this report where a sentence was.

**1. `test_cp00_candidate.py:7433` — `assertIn("`v0.0.0-architecture`", <wave plan>)`.**
Recorded, as asked, so the next reviewer need not re-derive it. It is the same family and
it belongs in §11.2's table as a **fifth safe site**, not a defect: the needle is asserted
against `self.sandbox.root` after `_ratify_for_real`, and the sandbox is rebuilt from the
freeze commit's blobs, so the text it reads is the frozen wave plan, in which
`v0.0.0-architecture` is an immutable historical fact rather than a current value. The
reviewer measured it green on the recovery's real output tree; it is green on tree T here
too, where `RATIFYING_TASK` resolves to `W0-INT-03` and `CHECKPOINT_TAG` to the floor. My
round-two grep could not see it because the backticks sit inside the quotes — the sweep
now would, because §12.2 swept by the *fact* rather than by the spelling.

**2. `:11398–11404` `assertGreaterEqual(resolved_revision, floor_revision)`.** Removed.
It could not fail while the floor is revision 0 and the series body is `(\d+)`: no value
the records could carry makes it false, so it read as a third guarantee and was decoration
on two. The floor rule keeps its teeth in `_checkpoint_tag_problems`, asserted clean on
the live tree, and in the fixture branch with `CHECKPOINT_TAG_FLOOR` patched to
`v0.0.1-architecture` — the only place a revision *can* be below the floor. A comment at
the site says so, so it is not silently re-added.

**3. §11.2 says "over the three delivered files"; there are four.** The sentence was
inaccurate. Corrected here: the grep is over four delivered files, and over the fourth it
returns 7 hits, all triaged benign by the reviewer. The omission concealed nothing.

**4. §11.4 row two is method-dependent.** True, and re-measured at source level, which
round two's in-memory method could not do — replacing a function on the loaded module
cannot move a constant computed at import:

```text
_resolved_checkpoint_tag always returns the floor, source-level:
    tree A (records at v0.0.0): 1 red   tree with the three delta sites flipped: 2 red
```

Round two reported 1/1 for that row. The correct figure is **1/2**, and the in-memory
method's limit is now stated: it proves the class of defect, not the site. §12.5's battery
is source-level throughout for this reason.

**5. §11.3's replacement had no mutation row of its own.** Measured in §14.2 rather than
inherited from §10.1: two mutations, both firing on both trees.

**6. `assertTrue(prose_only, …)` is itself a live-tree dependency.** It is, and the
sentence that closes it is in the source at the site: the seven prose spans sit in task
files that acceptance has frozen outside their status banners — `_task_banner_problems`
compares every completed task file with the freeze commit byte for byte away from the
banner — so no single legitimate act removes all of them, and an act that did would be
reported by this assertion rather than pass silently. It is far weaker than the three
literals it replaced, and it is real.

**7. `test_no_finding_is_this_task_s_own_defect` is thin.** Confirmed by measurement, with
a slightly coarser mutation than the reviewer's — `run()` returning a verdict with no
findings at all:

```text
tests/contract/test_cp00_final_state.py:  ran 14, red 0
tests/checkpoint:                          ran 47, red 25
```

All fourteen tests in `test_cp00_final_state.py` are downstream of `Verdict.findings` and
stay green, which is the reviewer's point exactly. The reviewer's narrower mutation —
forcing the list empty while the appends still ran — was caught by 1 of 46; mine is caught
by 25 of 47. Either way it **is** caught, and by the contour rather than by the binding.
Recorded as a known thinness of `test_cp00_final_state.py`: it verifies what the contour
says about this tree, and the contour's own ability to say anything is proved next door.

---

## 16. Required tests, round three

All in `/root/projects/PDF-Analysis-qa04r3`, base `762af03`, tree uncommitted. Exit codes
are the measured process's, never a pipeline tail.

| Command | Result | Exit |
|---|---|---|
| `.venv/bootstrap/bin/python -m unittest discover -s tests/contract` | `Ran 343 tests`, `FAILED (failures=5)` — the base commit's five, by name, unchanged | `1` |
| `.venv/bootstrap/bin/python -m unittest discover -s tests/checkpoint` | `Ran 47 tests … OK` | `0` |
| `.venv/bootstrap/bin/python scripts/validate_bootstrap.py` | standalone `PASS` | `0` |
| `git status --porcelain -uall` | ` M tests/checkpoint/test_cp00_final_state_contour.py`, ` M tests/contract/test_cp00_candidate.py`, ` M docs/program/reviews/W0-QA-04.md` | — |
| `git diff --check` | no output | `0` |
| `git rev-list -n1 v0.0.0-architecture` | `39a3a6430bd97c38cb20bafc793fc9d077d0df8e` | `0` |

The path set is a subset of `Allowed paths` — asserted as containment, not as a count.
`tests/checkpoint/cp00_final_state.py` and `tests/contract/test_cp00_final_state.py` were
read this round and not changed.

Exit `1` on `tests/contract` is the contract's own withdrawn expectation, for the reasons
`docs/program/tasks/W0-QA-04.md` gives: the two ratification gates disagree on this tree
and choosing `review_status` is forbidden to this task, and four recovery-planning files
landed after the round-ten freeze outside the delta ceiling. Both are `W0-INT-02`'s. The
clause that is not relaxed — the check stays red while the gates disagree — holds.

---

## 17. Limitations recorded rather than asserted away

1. **Three battery rows do not move on tree A** (§12.5, M2/M6/M9). On a tree whose records
   still say `W0-INT-01` there is nothing for them to distinguish. They are carried there
   by the fixture branch, and on tree T each is caught by name.
2. **The baseline resolver cannot reach a tree that never existed** (§13.4). Where the act
   was reassigned after the last unratified commit, the sandbox reports the reassignment
   rather than smoothing it, and the positive probe asserts the state-document half only.
3. **`RATIFYING_TASK_BANNER_DENIAL` stays a pinned phrase** (§12.4). A reassignment
   obliges the new ratifying task's banner to carry it verbatim before the freeze.
4. **The `{task}` denial needle is only as strong as the plan's own agreement.** What
   stops a plan from calling an unrelated task blocked is `_ratifying_task_problems`
   requiring the assignment row and the ratification record to name the same task, and
   `_task_banner_problems` requiring *that* task's banner to carry the denial. There is no
   third, independent witness to who the ratifier is; the wave plan is the assignment and
   the manifest is the act, and on an unratified candidate only the first exists.
5. **Item 3's site is fixture-local** (§12.3). The audit's stated reason for it — no
   licensed writer after the freeze — does not apply; it was repaired for a different and
   smaller reason, and the no-op-mutation guard is what makes the repair non-vacuous.
6. **Trees R and T are simulations.** They are built by scripts in the scratchpad, from
   this repository's own history, and each carries reconciliation debt that belongs to
   `W0-INT-02`: 29 of tree T's failures and 5 of tree R's are that debt and are red before
   this round as well. What they establish is a *difference*, measured by name and not by
   count: on tree T no test is red after that was green before, and 59 are green after
   that were red before.
7. **`test_cp00_final_state.py` is entirely downstream of `Verdict.findings`** (§15.7).
   Its fourteen tests cannot detect a contour that stops reporting; the contour's own
   suite can, and does, by 25 of 47.

---

## 18. Round three, the bytes

Base `762af0391bb199aaef0bb82f8ece5b11dae5f90b`, tree
`/root/projects/PDF-Analysis-qa04r3`, uncommitted, three paths, all inside
`Allowed paths`. `git diff --stat` against the base: 847 / 131 lines of code and 537 lines
of report.

| Path | sha256 | lines |
|---|---|---:|
| `tests/contract/test_cp00_candidate.py` | `5672767d1e483f93445f7fd7ff6143f6c89eeb5403e8b34336bf02061b67050e` | 14 082 |
| `tests/checkpoint/test_cp00_final_state_contour.py` | `a1cb242f91ba537a3662a4d0802cc1b09713a6f3df022e28adb06e1b6158a0be` | 1 092 |

The report's own digest is stated in the handoff message rather than inside itself, for
the reason §3.6.8 gives about self-referential quantities.

`tests/checkpoint/cp00_final_state.py` and `tests/contract/test_cp00_final_state.py` were
read this round and not changed; their round-two digests stand.

**The integration tip moved while this round was in flight**, to
`7fa6d83c9a2f36c2d53e55089589976092305658` over three commits touching
`docs/program/EXECUTION_PLAN.md`, `docs/program/tasks/W0-QA-04.md` and
`docs/program/tasks/W1-GOV-00.md`. `git diff 762af03 7fa6d83` over the three paths
delivered here is **empty**, so this diff applies to the tip unchanged. The task-file
correction in that range — `:170` is now `:316`, and `RATIFYING_TASK_FILE` at `:597` and
`RATIFYING_TASK_BANNER_DENIAL` at `:598` derive from the same fact and must move with the
constant — is what §12.2 and §12.4 do and say.
