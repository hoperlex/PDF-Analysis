# `W21-E2E` — the browser journey moves into the repository

**Session** `W21-E2E`. **Branch** `agent/w21-e2e`, from `origin/dev`.
**HEAD on arrival** `0f9989a` *docs: R-11 reverts R-8, and the wave keeps everything that
did not depend on it* — the base the brief names, confirmed an ancestor of `origin/dev`.

**Base measurement, taken before any edit**, instance `auditmanager-gate-w21b`
(`POSTGRES_PORT=55880`, `S3_API_PORT=59480`, `POSTGRES_DB=audit_w21b`,
bucket `auditmanager-gate-w21b`):

| half | measured at `0f9989a` | brief's figure |
|---|---|---|
| foundation | **35 passed**, 29.58 s | 35 |
| battery | **1806 passed, 5 skipped, 168 subtests**, 216.80 s | 1806 / 5 / 168 |
| frontend | **681 passed, 47 files** | 681 (47 files) |
| `make gate` | **exit 0** (from `$?`), 269 s wall | exit 0 |

Every figure in the brief's base line is confirmed.

*(This section is written before the first code edit. The rest of the file is filled in as
the work lands — `scratchpad does not survive restart`, so conclusions are committed as
they are found.)*
