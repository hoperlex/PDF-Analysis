# W16-WEB — closing D-1.5, the named exception to PC-01's certification

**Session** `W16-WEB`. **Branch** `agent/w16-web`, from `origin/dev`.
**HEAD on arrival** `315de258306053b0b6ba73529a34fe0a50794a29` — `merge: the clean-lane
breaker, and the two infra lines W15-AUTH stopped at`.
**Started** `2026-09-18T04:03:03+05:00`.

Owns `web/**` and this file. Touches no `src/`, no `contracts/`, no `infra/`, no
`Makefile`. `web/FRONTEND_LOCK.json` unchanged — no dependency added, no generated file
touched.

## 1. The debt

`DEBT_REGISTER.md` D-1.5 (`W12CERT-DEF-3`): 18% of `web/src` reached by no test, ten
mutations inside that region all survivors, one of them *"`run-progress` stops rendering
`terminal_reason`"* — with PC-01 criterion 4 requiring the **UI** to distinguish run
states and provider mode.

Sections filled as the work lands.

## 2. Reachability, re-measured

_pending_

## 3. What was reached

_pending_

## 4. Mutations: killed and survived

_pending_

## 5. What was left, and why

_pending_

## 6. False premises in the dispatch

_pending_

## 7. The gate

_pending_

## 8. Elapsed

_pending_
