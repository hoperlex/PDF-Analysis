# W23-PARTIAL — can `partial` be induced?

**Session** `W23-PARTIAL` · base `origin/dev` = `5ed72cc` (contains `6e07b97`) · branch
`agent/w23-partial` · worktree `/root/w23partial` · lane instance `gate-w23b`.

`PA-01` criterion 4 holds with one named exception recorded by `W21-CERT`: *`partial` is not
inducible*. This document establishes what produces `partial`, what stands between an operator
and it, and what — if anything — closes the exception.

Opened before the first edit. Sections are appended as they are measured.
