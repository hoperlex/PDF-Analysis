# W41-BLIND — make the guards reach the state that carries the defect

**task_id:** `W41-BLIND` · **wave:** 41 · **lane:** `gate-w41b`
**worktree:** `/root/w41blind` · **branch:** `agent/w41-blind` · **base:** `295ff04`

> **Opened before the first measurement**, as the brief requires. Everything below it is
> written as it is measured, with the command and the commit that produced it.

## 0. A base the brief got wrong, recorded first because everything else is dated from it

The brief names its base as `6aeda82` (`alpha-w40`). The worktree it dispatched is at
`295ff04`, which is `6aeda82` plus the dispatch commit that carries the brief itself. Every
figure in this file is taken at `295ff04` or later and says which.

## 1. What is measured here

Nothing yet. Sections are filled as the measurements are taken.

