# Wave W0.3 — CP-00 ratification and integration

**Status:** in progress on `integration/W0.3`; stage one is independently accepted
and integrated through `a67ba31e7748c02974ae9ae93c7f30b6f141d417`.

## Objective

Turn the independently accepted W0.2 candidates into one internally consistent CP-00
contract set, close the mechanical `ID-01` and architecture-lint obligations, execute
independent cross-family and manual acceptance, and only then ratify and tag
`v0.0.0-architecture`.

## Entry dependencies

- W0.2 candidate contracts and golden baseline integrated at
  `cf7740474b1786163f54d93b013a0d526ef989e0`;
- W0.2 integration metadata recorded at
  `43a84d93fd544573226b82860ab24f924ed66d83`;
- all four W0.2 lanes independently accepted, including the narrow fifth ANA review;
- validation environment locked by `requirements/validation.lock` and available as
  `.venv/bootstrap`;
- canonical legacy oracle `32b9d903792b30506048a1d42b0e6b2d07aee403`
  and accepted inventory `667fb00fe3e45d1ce0bce7860725c1654b4cdeba`.

## Roles and authority

| Role | Assignment | Authority |
|---|---|---|
| Program integrator / freeze governor | primary agent `/root` | Integration order, candidate manifest, CP-00 report, main/tag operation. |
| Architecture-rule owner | assigned for `W0-ARC-02` | The two architecture-lint specification files only. |
| Validator owner | assigned for `W0-QA-03` | Validator CLI, its README and focused tests only. |
| Domain contract owner | assigned after QA-03 for `W0-DOM-02` | The three declared domain error-catalog paths only. |
| Event contract owner | assigned after DOM-02 for `W0-EVT-01` | `contracts/events/v1/**` only. |
| Independent cross-family QA | assigned for `W0-QA-01`; must not have authored the reviewed contracts | Consumer tests and review report only. |
| Manual CP-00 tester | independent of the contract authors and cross-family test author | Executes and records `docs/manual-tests/CP-00_architecture.md`. |
| Product/domain authority | repository owner/user | Only new or changed product semantics; none are planned by this wave. |

## Scope

- Specify the architecture lint rules required by `U-03` without implementing a
  linter.
- Complete `ID-01` in strict order: validator reads `contract_version`, domain removes
  its deprecated mirror, events replace `schema_version` with `contract_version`.
- Add independent consumer-style checks across architecture, domain, analysis,
  events and the golden baseline without changing those artifacts.
- Reconcile point-in-time statements in the CP-00 review with the accepted final ANA
  precondition and record the ratification act.
- Produce the CP-00 evidence bundle and run the complete manual acceptance script.
- Fast-forward the accepted integration branch to `main`, then create and push the
  annotated registry tag `v0.0.0-architecture`.

## Non-goals

- No production backend, frontend, worker, migration, composition or infrastructure.
- No new product behavior, owner decision, retention/tenant/IdP value, lease timing,
  retry number or cost threshold.
- No contract-family redesign beyond the already decided version-key alignment.
- No implementation of architecture lint enforcement; that remains `W1-ARC-01`.
- No project-optimization runtime or contract; `U-06` keeps it disabled until
  `W5-OPT-01`.

## Contract and freeze targets

| Artifact family | Input | W0.3 target | Owner |
|---|---|---|---|
| Domain | `1.0.0-draft.1`, candidate revision 4 | same contract version; deprecated `version` mirror absent; revision advanced | `W0-DOM-02` |
| Analysis | `1.0.0-draft.1` | byte-identical to accepted W0.2 candidate | read-only |
| Events | bootstrap `schema_version: 1` | `1.0.0-draft.1` with `contract_version` | `W0-EVT-01` |
| Golden baseline | selection schema 1; 95 assertions | byte-identical to accepted W0.2 candidate | read-only |
| Architecture | review candidate, `ratified = false` | reconciled CP-00 review and explicit ratification record | `W0-INT-01` only |

The freeze includes the complete 62-row legacy name-resolution map. A later remap to
another existing stage is semantic change even if referential-integrity gates pass.

## Frozen hotspots ownership

| Path / hotspot | W0.3 owner |
|---|---|
| `docs/architecture/ARCHITECTURE_LINT_RULES.{md,json}` | `W0-ARC-02` |
| `scripts/validate_bootstrap.py`, `scripts/README.md`, focused validator test | `W0-QA-03` |
| declared domain error-catalog paths | `W0-DOM-02`, only after QA-03 integration |
| `contracts/events/v1/**` | `W0-EVT-01`, only after DOM-02 integration |
| cross-family test/report paths | `W0-QA-01` |
| CP-00 review ratification, current/checkpoint state and checkpoint evidence | `W0-INT-01` |
| all other `contracts/**`, golden fixtures and dependency locks | read-only |
| migration head | none; no owner because no migration is allowed |
| root runtime locks, composition root, generated client and global styles | read-only / out of scope |

## Task graph

```text
W0-ARC-02 ───────────────────────────────┐
                                         ├─> W0-QA-01 ─> W0-INT-01
W0-QA-03 -> W0-DOM-02 -> W0-EVT-01 ─────┘
```

`W0-ARC-02` and `W0-QA-03` may run concurrently because their writable paths are
disjoint. Every later arrow is a hard integration dependency, not merely a scheduling
preference.

## Task table

| Task | Lane | Result | Allowed paths | Depends on |
|---|---|---|---|---|
| [W0-ARC-02](../tasks/W0-ARC-02.md) | ARC | Architecture lint rule specification | two lint-rule documents | accepted W0-ARC-01 |
| [W0-QA-03](../tasks/W0-QA-03.md) | QA | Validator reads `contract_version` | validator, README, focused tests | W0-QA-02, accepted W0-ARC-01 |
| [W0-DOM-02](../tasks/W0-DOM-02.md) | DOM | Remove deprecated domain `version` mirror | three domain error-catalog paths | integrated W0-QA-03, accepted W0-DOM-01 |
| [W0-EVT-01](../tasks/W0-EVT-01.md) | EVT | Event envelope uses `contract_version` | `contracts/events/v1/**` | integrated QA-03 and DOM-02; accepted DOM-01 and ANA-01 |
| [W0-QA-01](../tasks/W0-QA-01.md) | QA | Independent cross-family candidate verification | one test module and review report | integrated ARC-02 and EVT-01 |
| [W0-INT-01](../tasks/W0-INT-01.md) | INT | Ratification, CP-00 evidence, main integration and tag | integration/checkpoint paths only | accepted QA-01 and every prior W0.3 task |

## Execution status

| Task | State | Integration commit / next gate |
|---|---|---|
| `W0-ARC-02` | independently accepted and integrated | `a67ba31e7748c02974ae9ae93c7f30b6f141d417` |
| `W0-QA-03` | independently accepted and integrated | `23dddf99f833d12cd4cc22d11e224d4b278872bf` |
| `W0-DOM-02` | ready | remove both deprecated catalog keys and both schema declarations |
| `W0-EVT-01` | blocked | waits for accepted and integrated `W0-DOM-02` |
| `W0-QA-01` | blocked | waits for integrated `W0-EVT-01` |
| `W0-INT-01` | blocked | waits for an exact-convergence `W0-QA-01` ACCEPT |

## Integration order

1. **Completed:** start `W0-ARC-02` and `W0-QA-03` from the recorded W0.3 base on
   disjoint writable paths.
2. **Completed:** independently review and integrate each; the accepted convergence
   is `a67ba31e7748c02974ae9ae93c7f30b6f141d417`.
3. Re-pin and execute `W0-DOM-02`; integrate only after the validator accepts a
   catalog with no bare `version` key.
4. Re-pin and execute `W0-EVT-01`; integrate only after the recursive repository sweep
   finds neither bare `version` nor `schema_version` envelope declarations.
5. Run `W0-QA-01` against that exact convergence commit. Contract fixes discovered
   here reopen the owning task and invalidate the QA result.
6. Run `W0-INT-01`: prepare the manifest, execute manual CP-00 acceptance, reconcile
   and ratify the architecture review, and create the checkpoint evidence commit.
7. Verify `origin/main` has not diverged, fast-forward `main`, rerun the final gates,
   create the annotated `v0.0.0-architecture` tag at the accepted checkpoint commit,
   and push both `main` and the tag.

## Automated gates

- bootstrap validator prints standalone `PASS`; its complete real-library regression
  suite passes;
- every JSON file parses without duplicate keys; every owned schema passes the Draft
  2020-12 metaschema and its positive/negative examples behave as declared;
- the recursive version-key sweep is clean across `contracts/**`;
- domain error-envelope enum equals the 20-code catalog exactly;
- analysis Gates A–D pass, including all 293 immutable locators and the nine exact
  run-lifecycle `rule_id` bindings;
- golden selection remains five journeys and 95 unique assertions, and all 19 input
  checksums match;
- architecture review, ADR index and lint-rule IDs are complete and consistent;
- internal Markdown links and `git diff --check` pass;
- each task's diff is a subset of its `allowed_paths`.

## Manual/checkpoint impact

The manual tester executes all six cases in
`docs/manual-tests/CP-00_architecture.md` against one recorded candidate commit. Any
`FAIL` or unexplained `BLOCKED` stops ratification and prevents the tag. `U-04`,
`OQ-02` and `OQ-04` remain explicit deferred inputs with their recorded later
deadlines; no numeric value is invented to make the checklist green.

## Stop conditions / rollback

- Stop on a new semantic disagreement, unowned contract write, mixed contract version,
  non-reproducible documented gate, unresolved cross-family reference or manual
  acceptance failure.
- A defect before freeze is fixed in the owning task and re-reviewed; downstream
  commits are rebuilt from the corrected integration point.
- After ratification, any contract change follows the formal freeze-break procedure.
- A failed push never changes the local acceptance decision; reconcile the remote
  branch without force-pushing or rewriting accepted history.
