# Checkpoint evidence

This directory is intentionally documentation-only at bootstrap. During development, each checkpoint creates an evidence bundle under the chosen artifact path (default described in `CHECKPOINT_REGISTRY.md`). Do not fabricate evidence files before tests are actually executed.
