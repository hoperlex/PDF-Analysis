# Task P3-QA-01 — automated PC-01 journey, restart and idempotency evidence

> **Status: specified; not dispatchable.** Planned for P03, after all four slices are
> integrated. It tests slices and does not edit them.

## Outcome

One recorded-provider end-to-end suite proves the complete PC-01 journey, that restarting
the application and execution process loses no canonical state, that repeated commands
under one idempotency key create no duplicates, and that no S3 key reaches the browser.

## Depends on

- none complete at plan time

Planned predecessors and dispatch condition — this task is not dispatchable until each
is accepted and integrated:

  - `P3-WEB-01`, `P3-WEB-02`, `P3-WEB-03`, `P3-WEB-04` — the four P03 slices
  - `P1-QA-00` — the foundation suite
  - `P2-BHV-01` — the synthetic AR fixture and the recorded provider response

## Frozen inputs

- API contract and the web public seams at their accepted commits
- domain contract: the `audit_run` and `command_idempotency` machines and the
  `finding_current_verdict` projection
- golden assertions `GJ-02-EO-01`, `GJ-02-EO-10`, `GJ-03-EO-09`, `GJ-03-EO-10`
- acceptance fixture: the synthetic AR PDF with two seeded contradictions, one placeholder
  and clean controls
- migration head: the P02 head, read only
- base commit: the P03 convergence commit supplied at dispatch

## Allowed paths

- `tests/e2e/pc01/**`
- the PC-01 section of `tests/e2e/README.md`
- `docs/navigation/entries/p3-qa-01.json`
- `docs/program/tasks/P3-QA-01.md`

## Forbidden hotspots

- every `web/src/**` slice, web manifest and config
- `src/**`, `contracts/**`, `db/**`, `infra/**`, root locks and the `Makefile`
- `tests/integration/foundation/**`, `tests/contract/**`, `tests/checkpoint/**`, CP-00
  evidence and Git refs

## Non-goals

- No slice remediation: a defect is reported to its owner and never patched here.
- No live-provider assertion, no model-wording assertion, no performance or load target,
  no visual regression baseline, no cross-browser matrix beyond one pinned engine.

## Deliverables

- one end-to-end spec covering create, upload, run in recorded mode, progress, finding,
  page beside quotation, accept, reject, later comment, revoke, export and download
- a restart spec: stop and start the application and execution process mid-journey, then
  assert that run state, findings, ledger and export remain resolvable and that no run is
  displayed as still running
- an idempotency spec repeating upload, run start, decision append and export request
  under identical keys and asserting exactly one version, run, decision and export
- a negative spec set in which unsupported input, checksum failure, unavailable provider
  and ungrounded model output each render explicitly, with no fake success and no
  filesystem fallback
- a leakage spec asserting that no network URL, response body or DOM node contains the
  configured bucket name, the S3 endpoint host or an `s3://` prefix
- an anti-vacuity spec proving the suite fails when the API is stopped, so a green run
  cannot mean that nothing was exercised

## Required tests

- Precondition: one dedicated clean instance; no authoring lane uses its
  `FOUNDATION_INSTANCE`, ports, database or bucket concurrently.
- Command: `make foundation`
  Expected: exit `0`.
- Command: `npm --prefix web run e2e:pc01`
  Expected: exit `0`, with every spec reported as executed rather than skipped.
- Command: `npm --prefix web run e2e:pc01 -- --grep "anti-vacuity"` with the API stopped
  Expected: non-zero.
- Command: `.venv/bootstrap/bin/python scripts/validate_bootstrap.py`
  Expected: exit `0`, standalone `PASS`.
- Command: `git diff --check`
  Expected: exit `0`.

## Integration contract

The integrator may rely on one green recorded-mode journey command covering the accepted
slice commits. This task certifies those commits, not its own unreviewed changes, and
never edits a slice to make a spec pass.

## Failure/idempotency/security cases

- Specs fail if a stale `running` run survives restart, if a duplicate aggregate appears,
  or if any decision value is overwritten rather than appended.
- Cleanup is scoped to generated test projects, versions and exports and leaves the
  checkout unchanged.
- No credential or provider payload enters a test artifact or trace.

## Rollback / feature flag

Tests only. Revert the QA commit without touching product code or server state.

## Estimate

P50 1.5 days, P80 3 days.

## Handoff

- the exact certified slice commits and the fixture identity
- commands and results with measured elapsed time and the tree it was measured on
- any skipped spec and why
