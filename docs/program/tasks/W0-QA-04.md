# Task W0-QA-04 — a final-state contour that can distinguish a ratified checkpoint from an open round

## Outcome

An executable check contour that returns a truthful verdict on the **terminal** state of
CP-00, and that fails when given a tree whose records do not describe it. Today no such
contour exists: `artifacts/checkpoints/CP-00/check_state_records.py` models only "which
round is owed", so on the ratified tree it reports 14 findings that are a correct
description of a ratified checkpoint rather than defects, and the CP-00 suite passes 324
tests without noticing that `W0-INT-01`'s own ratification assertion fails.

## Depends on

- `W0-QA-01`, accepted and integrated at `3da104e5d6fafb2a581bda377a07911183af803f`
- `W0-INT-01`, executed at `39a3a6430bd97c38cb20bafc793fc9d077d0df8e`

## Frozen inputs

- domain contract: `contracts/domain/v1/**` at `1.0.0-draft.1`, candidate revision 5 — read only
- API contract: none exists at CP-00
- analysis/comparison/event contract: `contracts/analysis/v1/**`, `contracts/events/v1/**` at `1.0.0-draft.1` — read only
- migration head: `none`; `db/migrations/` holds only `README.md`
- base commit: `39a3a6430bd97c38cb20bafc793fc9d077d0df8e`
- immutable reviewed candidate: `92e13fa496a723ed6e4c3adbf138c4f4e1d7c368`
- local tag `v0.0.0-architecture`, annotated, on the base commit — **read only, never moved**

## Allowed paths

- `tests/checkpoint/**`
- `tests/contract/test_cp00_final_state.py`
- `tests/contract/test_cp00_candidate.py`
- `docs/program/reviews/W0-QA-04.md`

**Widened after the phase-A audit, and only this far.** `test_cp00_candidate.py:170`
pins `CHECKPOINT_TAG = "v0.0.0-architecture"`, consumed by two guards including one
that requires the W0.3 wave plan to carry that literal. The recovery ratifies as
`v0.0.1-architecture`, and no other task in the graph may write that file — so
`W0-INT-03`'s required suite run would be unreachable at the moment of ratification.
That is round nine's shape one file over: a final state no task is licensed to reach.
The same file holds the `review_status` fixtures at `:5722` and `:6086`. You are the
sole writer of the test tree in this batch; nothing else about the ownership changes.

That sentence used to name the test tree as a path-shaped code span, and an
independent reviewer showed the span was harvested as a declared glob — widening this
task's own licence to every file under it and making containment vacuous. The wording
is corrected here, but the wording is not the fix: a guard whose behaviour depends on
how a sentence is punctuated is not a guard. The harvest itself is narrowed in the
remediation.

## Forbidden hotspots

- `contracts/**`, `fixtures/**`, `scripts/**` — the immutable reviewed families
- `artifacts/checkpoints/CP-00/**` — every evidence record, the manifest and the existing
  checker; they are `W0-INT-02`'s and the historical bundle is immutable
- `docs/program/CURRENT_STATE.md`, `CHECKPOINT_REGISTRY.md`, `docs/program/waves/**`,
  every other task file — `W0-INT-02`'s
- `docs/architecture/**` — the reviewed architecture family
- root dependency and lock files, migration head, composition root, global styles
- the local tag `v0.0.0-architecture`: not moved, not deleted, not re-pointed

## Non-goals

- No reconciliation of live state records; no erratum; no manifest rewrite. Those are
  `W0-INT-02`.
- No ratification, no tag, no push.
- No edit to the existing `check_state_records.py`. Its round-accounting model is correct
  for an open round and is kept as history; the new contour is additional, not a rewrite.
- No decision on the canonical value of `review_status`. The contour must **test both
  sides against one another** and fail while they disagree; choosing the value is
  `W0-INT-02`'s, recorded as a disposition.

## Deliverables

1. A final-state contour covering, each with a positive case, a negative case and a
   mutation that turns the check red:
   - **terminal state**: a checkpoint is either open-round, closed-accepted-round, or
     terminal-ratified, and exactly one is true of a given tree;
   - **live vs historical**: a dated primary report may state a superseded fact; a live
     record may not. The distinction must be structural, not a path skip list;
   - **tag integrity**: the annotated tag exists, is annotated, points where the records
     say, and has not moved relative to its recorded target;
   - **file and hash accounting**: every file the checkpoint claims, with its own hash,
     and the aggregate each hash rolls into — no aggregate whose members are unlisted;
   - **cross-gate agreement**: `W0-INT-01`'s required assertions and the architecture
     review's own fields agree, and the contour fails while they do not.
2. `docs/program/reviews/W0-QA-04.md` — an evidence-backed report: commands, real output,
   measured exit codes, the mutation table with green-before/red-after for every check,
   and every limitation recorded plainly rather than asserted away.

## Required tests

- Command: `.venv/bootstrap/bin/python -m unittest discover -s tests/contract`.
  Expected: **the failure set is exactly the base commit's own failures, plus contour
  checks every one of whose findings names `W0-INT-02` as owner.** The count stated in
  the report matches what the runner collects.

  Exit `0` was the original expectation and is **withdrawn**, on an independent
  reviewer's reasoning that this task's contract could not satisfy both it and the
  clause requiring a tree whose two ratification gates disagree to turn a named check
  red. The gates do disagree, and choosing `review_status` is explicitly forbidden to
  this task. Exit `0` was already unreachable at dispatch for a second, unrelated and
  unowned reason: four recovery-planning files landed after the round-ten freeze,
  outside the delta ceiling, so the suite correctly reports the round void. Both are
  properties of `W0-INT-02`'s reconciliation, not of this deliverable. The clause that
  is **not** relaxed is the one requiring the check to be red while the gates disagree:
  a check that goes quiet on a contradiction is the defect this task exists to close.
- Command: `.venv/bootstrap/bin/python -m unittest discover -s tests/checkpoint`.
  Expected: exit `0`.
- Command: `.venv/bootstrap/bin/python scripts/validate_bootstrap.py`.
  Expected: exit `0`, standalone `PASS`.
- Command: `git status --porcelain -uall`.
  Expected: a **path set** that is a subset of the allowed paths above. Assert
  containment, never a count, and accept the empty result.
- Command: `git diff --check`.
  Expected: exit `0`, no output.
- Command: `git rev-list -n1 v0.0.0-architecture`.
  Expected: `39a3a6430bd97c38cb20bafc793fc9d077d0df8e`, unchanged from the base.

**Measurement rule, not optional.** Never measure in a linked worktree: `.git` is a file
there, the sandbox tests cannot copy the object database, and the suite reports 202 tests
with 19 environment errors instead of 324 `OK`. Measure in a clone or the main checkout,
and never while anything else clones this repository — the sandbox tests copy the working
tree and a concurrent clone corrupts the run.

## Integration contract

The integrator can rely on: running the two discover commands on any candidate tree
returns a verdict that is true of that tree's terminal state; a tree whose live records
contradict its manifest, whose tag has moved, whose claimed artifacts do not exist, or
whose two ratification gates disagree, turns at least one named check red; and every
check has a recorded mutation proving it can fail.

## Failure/idempotency/security cases

- The contour must be re-runnable on the same tree with the same verdict.
- A check that cannot fail is a defect, not a pass: every guard carries its mutation.
- The contour reads the repository; it writes nothing outside its allowed paths and runs
  no Git command that mutates refs, the index or the working tree.
- Any subprocess it spawns must not inherit the ambient Git environment; see `E-06` in
  `artifacts/checkpoints/CP-00/known-risks.md` for why.

## Rollback / feature flag

Not applicable: the deliverable is test tooling. Rollback is reverting the integration
commit, which removes the contour and restores the previous suite exactly.

## Handoff

- changed files: the exact list, proved a subset of allowed paths
- commands/results: every required test, verbatim, with the exit code of the measured
  process and not of a pipeline tail
- known limits: what the contour does not cover, stated plainly
- integration notes: the base commit measured against, and whether any finding belongs to
  `W0-INT-02` rather than to this task
