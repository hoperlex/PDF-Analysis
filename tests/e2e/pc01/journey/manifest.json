{
  "$comment": [
    "The PC-01 journey, written down once and read by two checkers.",
    "",
    "At RUN TIME `journey.mjs` walks these routes in order against a deployed origin, each",
    "in its own cold browser process, and asserts that what the browser actually requested",
    "matches `expects_api` here.",
    "",
    "At GATE TIME `tests/e2e/test_pc01_journey_conformance.py` reads the same file with no",
    "browser, no origin and no running stack, and asserts that every `page_module` exists,",
    "that every screen in `web/src/app` is covered by exactly one route here, that the",
    "dynamic segments of `path` match the `[param]` directories of `page_module`, and that",
    "every (method, path, operationId) triple is a real operation in",
    "`contracts/api/v1/openapi.json`.",
    "",
    "That pairing is the anti-rot mechanism. Neither checker asks the application to",
    "confirm itself: this file is an independent written claim, the gate checks it against",
    "the route tree and the contract, and the journey checks it against live traffic.",
    "",
    "`expects_api` paths are API paths -- the contract's own paths, WITHOUT the `/bff/v1`",
    "prefix the browser adds. Query strings are not declared and not matched: `?limit=50`",
    "is a pagination choice, not a seam."
  ],
  "api_prefix": "/bff/v1",
  "identifier_pattern": "[0-9A-HJKMNP-TV-Z]{26}",
  "session": {
    "$comment": [
      "The SIGN-IN. `D-92`, repaired by `W44-JOURNEY` 2026-09-24.",
      "",
      "The BFF answers `401` without a session cookie -- wave 34, by design -- and this",
      "journey was written for the application as it stood before that. It walked two routes",
      "of fifteen for nine waves, and a shorter walk looked like a walk.",
      "",
      "At RUN TIME `session.mjs` opens ONE cold browser at the address below, fills the two",
      "fields through the browser's own editing pipeline, presses the control by its own",
      "label, and carries the single `HttpOnly` cookie the exchange sets into every later",
      "cold browser -- identically, as a value, never as a shared jar. Each route therefore",
      "begins from an empty profile plus exactly one stated cookie, which `journey.mjs`",
      "reads back per route before it navigates. `D-16`'s cold-load property is measured",
      "rather than assumed.",
      "",
      "THERE IS NO CREDENTIAL HERE AND THERE MAY NEVER BE ONE. Each `fill` names the FIELD",
      "it fills -- `from`: `login` or `password` -- and the value comes from",
      "`E2E_PC01_LOGIN` / `E2E_PC01_PASSWORD` at run time, with no default and no file.",
      "`tests/e2e/test_pc01_journey_conformance.py` refuses a session action carrying a",
      "literal `value`, so the gate is what keeps a password out of this repository rather",
      "than this paragraph."
    ],
    "at": "/login",
    "page_module": "web/src/app/login/page.tsx",
    "control_module": "web/src/features/sign-in/ui/sign-in-form.tsx",
    "cookie": "am_session",
    "api": {
      "method": "POST",
      "path": "/session",
      "expect_status": 303,
      "$why": "the exchange is this tier's own and is never forwarded: the browser receives a 303 and an opaque cookie, and the minted credential never crosses the network. See web/src/app/bff/v1/[...path]/route.ts."
    },
    "actions": [
      {
        "do": "fill",
        "selector": "#sign-in-login",
        "from": "login"
      },
      {
        "do": "fill",
        "selector": "#sign-in-password",
        "from": "password"
      },
      {
        "do": "click",
        "selector": "form button[type=\"submit\"]",
        "text": "Войти"
      }
    ],
    "lands_on": "/projects",
    "bound_ms": 30000,
    "refusal_marker": "data-sign-in-refusal"
  },
  "viewport": {
    "$comment": [
      "The declared width every route is laid out at. `D-93`, `W44-JOURNEY` 2026-09-24.",
      "",
      "Wave 43 added four navigation links, `.am-app__bar` stopped fitting, and the overflow",
      "threshold moved from 482 px to 839 px -- so every screen in the product carried a",
      "horizontal scrollbar on a small laptop and on every tablet. 1085 frontend tests could",
      "not see it: the battery renders through `renderToStaticMarkup`, which has no layout,",
      "no box and no viewport. A judge driving a browser found it.",
      "",
      "780 is the width `W43-JUDGE-B` measured the regression at, and is deliberately the",
      "SAME number, so this assertion and that finding are comparable. It sits between the",
      "old threshold (482) and the new one (839), which is what makes it able to see the",
      "class: a bar that fits at 839 passes here, a bar that does not fails here.",
      "",
      "The verdict is `document.documentElement.scrollWidth <= window.innerWidth`, per route,",
      "after the page has settled. `innerWidth` rather than `clientWidth` on purpose: a",
      "vertical scrollbar makes `clientWidth` smaller than the viewport, and a page exactly",
      "filling the viewport would then redden for a scrollbar rather than for a layout. The",
      "looser of the two comparisons is the one that cannot produce a false red.",
      "",
      "Shown failing by `prove_the_width_assertion_can_fail.mjs`, which restores the",
      "`flex-wrap` this bar had before wave 43 repaired it and requires this assertion to",
      "redden."
    ],
    "width": 780,
    "height": 900,
    "measured_at": "W43-JUDGE-B, 2026-09-24, on the deployed stand"
  },
  "routes": [
    {
      "name": "root",
      "path": "/",
      "page_module": "web/src/app/page.tsx",
      "redirects_to": "/projects",
      "$comment": "A redirect is not a blank page: the browser lands on /projects and that screen lists. Measured -- declaring no call here made the route red, correctly.",
      "expects_api": [
        {
          "method": "GET",
          "path": "/projects",
          "operationId": "listProjects"
        }
      ],
      "follow": null
    },
    {
      "name": "projects",
      "path": "/projects",
      "page_module": "web/src/app/projects/page.tsx",
      "expects_api": [
        {
          "method": "GET",
          "path": "/projects",
          "operationId": "listProjects"
        }
      ],
      "follow": {
        "capture": "project_uid",
        "href_pattern": "^/projects/(prj_%ID%)$"
      }
    },
    {
      "name": "project",
      "path": "/projects/{project_uid}",
      "page_module": "web/src/app/projects/[project_uid]/page.tsx",
      "expects_api": [
        {
          "method": "GET",
          "path": "/projects/{project_uid}/documents",
          "operationId": "listDocuments"
        }
      ],
      "follow": {
        "capture": "document_uid",
        "href_pattern": "^/projects/prj_%ID%/documents/(doc_%ID%)$"
      }
    },
    {
      "name": "document",
      "path": "/projects/{project_uid}/documents/{document_uid}",
      "page_module": "web/src/app/projects/[project_uid]/documents/[document_uid]/page.tsx",
      "expects_api": [
        {
          "method": "GET",
          "path": "/documents/{document_uid}/versions",
          "operationId": "listVersions"
        }
      ],
      "follow": {
        "capture": "version_uid",
        "href_pattern": "^/projects/prj_%ID%/versions/(ver_%ID%)$"
      }
    },
    {
      "name": "version",
      "path": "/projects/{project_uid}/versions/{version_uid}",
      "page_module": "web/src/app/projects/[project_uid]/versions/[version_uid]/page.tsx",
      "expects_api": [
        {
          "method": "GET",
          "path": "/versions/{version_uid}/runs",
          "operationId": "listRuns"
        },
        {
          "method": "GET",
          "path": "/versions/{version_uid}",
          "operationId": "getDocumentVersion"
        }
      ],
      "follow": {
        "capture": "run_id",
        "href_pattern": "^/projects/prj_%ID%/runs/(run_%ID%)$"
      }
    },
    {
      "name": "comparison",
      "path": "/projects/{project_uid}/versions/{version_uid}/comparison",
      "page_module": "web/src/app/projects/[project_uid]/versions/[version_uid]/comparison/page.tsx",
      "$comment": [
        "Two runs of one published version, side by side. W43-COMPARE.",
        "",
        "PLACED AFTER `version` AND NOT BESIDE THE FOUR PREPARED SECTIONS, and the reason is",
        "this file's own follow-chain: the read walk captures identifiers in the order the",
        "routes appear, so a route naming {project_uid} and {version_uid} must come after the",
        "routes that captured them. The four prepared sections take no identifier at all and",
        "so sit at the end with `sign-in`'s neighbours.",
        "",
        "`expects_api` names one operation and that is a claim, not a floor. The screen calls",
        "`listRuns` on mount in EVERY state it has -- two runs, one run, and none -- so the",
        "assertion holds whatever the stand contains, which is what makes it worth asserting."
      ],
      "expects_api": [
        {
          "method": "GET",
          "path": "/versions/{version_uid}/runs",
          "operationId": "listRuns"
        }
      ],
      "follow": null
    },
    {
      "name": "run",
      "path": "/projects/{project_uid}/runs/{run_id}",
      "page_module": "web/src/app/projects/[project_uid]/runs/[run_id]/page.tsx",
      "expects_api": [
        {
          "method": "GET",
          "path": "/runs/{run_id}",
          "operationId": "getRunStatus"
        }
      ],
      "follow": null
    },
    {
      "name": "review",
      "path": "/projects/{project_uid}/runs/{run_id}/review",
      "page_module": "web/src/app/projects/[project_uid]/runs/[run_id]/review/page.tsx",
      "expects_api": [
        {
          "method": "GET",
          "path": "/runs/{run_id}",
          "operationId": "getRunStatus"
        },
        {
          "method": "GET",
          "path": "/runs/{run_id}/findings",
          "operationId": "listRunFindings"
        }
      ],
      "optional_api": [
        {
          "method": "GET",
          "path": "/findings/{finding_uid}",
          "operationId": "getFinding",
          "$why_optional": "the screen opens the first finding; a run with none makes no call"
        },
        {
          "method": "GET",
          "path": "/findings/{finding_uid}/decisions",
          "operationId": "listDecisionHistory",
          "$why_optional": "follows getFinding, so it is conditional on the same thing"
        },
        {
          "method": "GET",
          "path": "/versions/{version_uid}/content",
          "operationId": "streamDocumentVersionContent",
          "$why_optional": "the document pane fetches the PDF only once a finding is open"
        }
      ],
      "follow": null
    },
    {
      "name": "sign-in",
      "path": "/login",
      "page_module": "web/src/app/login/page.tsx",
      "$comment": [
        "The sign-in screen, added by W34-WEB because this manifest is where a new screen",
        "is declared: `test_the_journey_walks_every_screen_the_application_offers` reddens",
        "the moment `web/src/app` offers one the journey does not walk, which is the point",
        "of that check and not a side effect of it.",
        "",
        "`expects_api` is EMPTY and that is the claim, not an omission. The screen renders a",
        "`<form>` and asks the API nothing until it is submitted: the exchange is a POST the",
        "browser makes on navigation, to `/bff/v1/session`, which is this tier's own address",
        "and not an operation of `contracts/api/v1/openapi.json`. Declaring a contract",
        "operation here would name a call the browser does not make on load, and the walk",
        "asserts in both directions -- every declared call observed, and no undeclared one.",
        "",
        "It is LAST so the read walk's follow-chain is untouched: `root` hands `/projects`",
        "the first project uid and each route after it captures the next identifier. A route",
        "inserted before them walks a screen with nothing captured yet.",
        "",
        "What this route does NOT cover is the submitted exchange -- the password typed, the",
        "303, the cookie, the credential the forwarder then presents. That is a write step",
        "against a live API that can mint a token, and the API half of the exchange does not",
        "exist yet (W34-CONTRACT published the operation; no router implements it). The",
        "session report of W34-WEB names it as the open half rather than declaring a step",
        "here that no stack can satisfy."
      ],
      "expects_api": [],
      "follow": null
    },
    {
      "name": "knowledge-base",
      "path": "/knowledge-base",
      "page_module": "web/src/app/knowledge-base/page.tsx",
      "$comment": [
        "The knowledge base, added by the integrator because W38-KB could not: this manifest is",
        "named in that stream's forbidden_hotspots twice, deliberately, and it stopped rather",
        "than reach past the boundary. Its gate went red on exactly this screen, and the guard",
        "was right -- R-23 makes the screen required and the journey must walk every screen the",
        "application offers.",
        "`follow: null` because this screen captures no identity the next step needs: it is a",
        "projection over the whole decision journal and hangs off no parent."
      ],
      "expects_api": [
        {
          "method": "GET",
          "path": "/decisions",
          "operationId": "listDecisions"
        }
      ],
      "follow": null
    },
    {
      "name": "change-password",
      "path": "/account/password",
      "page_module": "web/src/app/account/password/page.tsx",
      "$comment": [
        "The password screen, R-26, added by W39-REVOKE -- which owns this file this wave,",
        "unlike W38-KB, which had it in its forbidden_hotspots and correctly stopped.",
        "",
        "`expects_api` is EMPTY and that is the claim, not an omission -- the same claim the",
        "sign-in entry above makes, for the same reason. The screen renders a `<form>` and",
        "asks the API nothing until it is submitted, and the submission is a browser",
        "navigation to `/bff/v1/session/password`, which is this tier's own reserved segment",
        "and not a contract path. The contract operation it reaches -- changePassword, POST",
        "/auth/password -- is called by the NODE process, never by the browser: the whole",
        "`auth` segment is refused to a browser precisely because that operation answers with",
        "a credential, and a credential in a page body is what JUDGE-SEC drove out of",
        "issueToken.",
        "",
        "So a declared call here would be false in the way this manifest is least able to",
        "tolerate: the run-time walk asserts what the BROWSER requested, and the browser",
        "requests nothing of the API on this screen.",
        "",
        "`follow: null` -- it captures no identity a later step needs, and it is deliberately",
        "not the last thing the write half does: succeeding here revokes every credential the",
        "account holds, so a journey that changed the password mid-walk would be walking with",
        "a dead one."
      ],
      "expects_api": [],
      "follow": null
    },
    {
      "name": "blocks",
      "path": "/blocks",
      "page_module": "web/src/app/blocks/page.tsx",
      "$comment": [
        "Блоки -- front-end preparation under R-23's addendum. W43-PREP.",
        "",
        "`expects_api` is EMPTY and that is the claim, not an omission: this screen is a",
        "`RoutePlaceholder` and calls nothing at all. The day its vertical lands, this",
        "entry gains operations and the assertion starts carrying weight."
      ],
      "expects_api": [],
      "follow": null
    },
    {
      "name": "optimisation",
      "path": "/optimisation",
      "page_module": "web/src/app/optimisation/page.tsx",
      "$comment": [
        "Оптимизация -- front-end preparation under R-23's addendum. W43-PREP.",
        "",
        "`expects_api` is EMPTY and that is the claim, not an omission: this screen is a",
        "`RoutePlaceholder` and calls nothing at all. The day its vertical lands, this",
        "entry gains operations and the assertion starts carrying weight."
      ],
      "expects_api": [],
      "follow": null
    },
    {
      "name": "logs",
      "path": "/logs",
      "page_module": "web/src/app/logs/page.tsx",
      "$comment": [
        "Логи -- front-end preparation under R-23's addendum. W43-PREP.",
        "",
        "`expects_api` is EMPTY and that is the claim, not an omission: this screen is a",
        "`RoutePlaceholder` and calls nothing at all. The day its vertical lands, this",
        "entry gains operations and the assertion starts carrying weight."
      ],
      "expects_api": [],
      "follow": null
    },
    {
      "name": "workers",
      "path": "/workers",
      "page_module": "web/src/app/workers/page.tsx",
      "$comment": [
        "Исполнители -- front-end preparation under R-23's addendum. W43-PREP.",
        "",
        "`expects_api` is EMPTY and that is the claim, not an omission: this screen is a",
        "`RoutePlaceholder` and calls nothing at all. The day its vertical lands, this",
        "entry gains operations and the assertion starts carrying weight."
      ],
      "expects_api": [],
      "follow": null
    }
  ],
  "write": {
    "$comment": [
      "The WRITE half of the journey. `D-30`, and before it `D-5`.",
      "",
      "`D-5` was `POST /api/v1/runs` -> 500, twice, through a browser, on 2026-09-16. The",
      "read walk above would not have caught it: it makes no POST. These three steps do what",
      "a user does -- name a project and press Create, choose the AR PDF and press Upload,",
      "press Start run -- through the app's own controls, on the public origin.",
      "",
      "Read by the same two checkers as `routes`. At RUN TIME `write.mjs` presses these",
      "controls and asserts the request, the status, the response body and what the screen",
      "rendered afterwards. At GATE TIME the conformance guard asserts that every",
      "`page_module` and `control_module` exists, that each step's `at` is a route the read",
      "walk also covers, that every (method, path, operationId) triple is a real contract",
      "operation, that the fixture is on disk, and that every selector, text and marker",
      "attribute named here still appears in `web/src`.",
      "",
      "That last check is new. `W21-E2E` stated the guard's cost as 'it checks addressing,",
      "not behaviour'. The write half's handles are not addressing, so leaving them unguarded",
      "would have meant a renamed id turns the journey red only when someone runs it. The",
      "check is still not behaviour -- it proves the handle exists in the source, not that",
      "pressing it does anything -- and that residue is stated rather than closed.",
      "",
      "`%STAMP%` is replaced with the run's UTC timestamp. The app mints its idempotency key",
      "from the typed name, so a fixed name would make every run the same command and the",
      "second run of the day would assert nothing about creation."
    ],
    "fixture": "fixtures/synthetic/ar/ar_baseline.pdf",
    "steps": [
      {
        "name": "create-project",
        "at": "/projects",
        "page_module": "web/src/app/projects/page.tsx",
        "control_module": "web/src/features/create-project/ui/create-project-form.tsx",
        "actions": [
          {
            "do": "fill",
            "selector": "#new-project-name",
            "value": "W22-E2E write half %STAMP%"
          },
          {
            "do": "click",
            "selector": "form button[type=\"submit\"]",
            "text": "Создать"
          }
        ],
        "expects_api": [
          {
            "method": "POST",
            "path": "/projects",
            "operationId": "createProject",
            "expect_status": 201
          }
        ],
        "expects_response": [
          {
            "of": "createProject",
            "json_path": "project_uid",
            "matches": "^prj_%ID%$"
          }
        ],
        "capture": {
          "kind": "attribute",
          "$why": "the screen states what it created, in its own role=status line",
          "selector": "[data-created-project]",
          "attribute": "data-created-project",
          "as": [
            "project_uid"
          ],
          "matches": "^(prj_%ID%)$",
          "bound_ms": 30000
        },
        "$why_no_expects_rendered": "D-61, W41-BLIND 2026-09-23. `expects_rendered` here declared \"Создан\", which this application renders on every project row as that project's created date -- with no step having run. It passed in the gate AND in the browser, because both test containment against text that is on the screen either way, so it was an assertion that could not fail. The evidence this step really has is `capture`: the `[data-created-project]` marker, matched against `^(prj_%ID%)$` under a 30s bound. A sentence is declared here again only when the outcome is the only thing that renders it.",
        "forbids_rendered": [
          "[role=\"alert\"]",
          "[data-create-problem]"
        ]
      },
      {
        "name": "upload-document",
        "at": "/projects/{project_uid}",
        "page_module": "web/src/app/projects/[project_uid]/page.tsx",
        "control_module": "web/src/features/upload-document/ui/upload-document-form.tsx",
        "actions": [
          {
            "do": "attach_file",
            "selector": "#upload-file",
            "fixture": true
          },
          {
            "do": "fill",
            "selector": "#upload-title",
            "value": "AR baseline %STAMP%"
          },
          {
            "do": "click",
            "selector": "form button[type=\"submit\"]",
            "text": "Загрузить"
          }
        ],
        "expects_api": [
          {
            "method": "POST",
            "path": "/projects/{project_uid}/documents",
            "operationId": "uploadDocument",
            "expect_status": 201
          }
        ],
        "expects_request": [
          {
            "of": "uploadDocument",
            "content_type_starts_with": "multipart/form-data",
            "body_contains": [
              "name=\"file\"",
              "ar_baseline.pdf",
              "application/pdf"
            ]
          }
        ],
        "expects_response": [
          {
            "of": "uploadDocument",
            "json_path": "version_uid",
            "matches": "^ver_%ID%$"
          },
          {
            "of": "uploadDocument",
            "json_path": "document_uid",
            "matches": "^doc_%ID%$"
          }
        ],
        "capture": {
          "kind": "location",
          "$why": "on success the screen navigates to the published version's own address",
          "pattern": "^/projects/(prj_%ID%)/versions/(ver_%ID%)$",
          "as": [
            "project_uid",
            "version_uid"
          ],
          "bound_ms": 30000
        },
        "$why_no_expects_rendered": "D-61, W41-BLIND 2026-09-23. `expects_rendered` here declared \"Эта версия\", which this application renders in the version panel's standing prose, on every version screen -- with no step having run. It passed in the gate AND in the browser, because both test containment against text that is on the screen either way, so it was an assertion that could not fail. The evidence this step really has is `capture`: the location match on the published version's own address. A sentence is declared here again only when the outcome is the only thing that renders it.",
        "forbids_rendered": [
          "[data-upload-failure]",
          "[data-precheck-problem]"
        ]
      },
      {
        "name": "start-run",
        "at": "/projects/{project_uid}/versions/{version_uid}",
        "page_module": "web/src/app/projects/[project_uid]/versions/[version_uid]/page.tsx",
        "control_module": "web/src/features/start-run/ui/start-run-control.tsx",
        "actions": [
          {
            "do": "click",
            "selector": "button",
            "text": "Запустить прогон"
          }
        ],
        "expects_api": [
          {
            "method": "POST",
            "path": "/runs",
            "operationId": "startRun",
            "expect_status": 202
          }
        ],
        "expects_response": [
          {
            "of": "startRun",
            "json_path": "run_id",
            "matches": "^run_%ID%$"
          },
          {
            "of": "startRun",
            "json_path": "state",
            "equals": "queued",
            "$why": "W20-EXEC moved execution off the request. 202 alone would not say the run was only accepted, not done."
          }
        ],
        "capture": {
          "kind": "location",
          "$why": "the 202 reading carries the run, and the screen moves to run progress",
          "pattern": "^/projects/(prj_%ID%)/runs/(run_%ID%)$",
          "as": [
            "project_uid",
            "run_id"
          ],
          "bound_ms": 30000
        },
        "$why_no_expects_rendered": "D-61, W41-BLIND 2026-09-23. `expects_rendered` here declared \"Прогон\", which this application renders in the start-run control's own note, before the button is pressed -- with no step having run. It passed in the gate AND in the browser, because both test containment against text that is on the screen either way, so it was an assertion that could not fail. The evidence this step really has is `capture`: the captured run id, `await_terminal`, and `expects_api_after_terminal`. A sentence is declared here again only when the outcome is the only thing that renders it.",
        "await_terminal": {
          "$comment": [
            "The run reaches its terminal asynchronously. This does NOT sleep: it reads",
            "the attribute the run screen's own poller writes, and that poller is the",
            "application's single `pollRunStatus` loop at its frozen 2s/x1.5/15s schedule.",
            "`bound_ms` is this journey's bound, not the app's -- `polling.ts` deliberately",
            "has no deadline. Reaching the bound is a finding with every reading attached,",
            "and exit 1. It is never a skip and never a pass."
          ],
          "outcome_selector": "[data-run-outcome]",
          "outcome_attribute": "data-run-outcome",
          "non_terminal_value": "in_flight",
          "accept": [
            "published",
            "partial"
          ],
          "activity_selector": "[data-run-activity]",
          "activity_attribute": "data-run-activity",
          "activity_when_final": "stopped",
          "bound_ms": 150000,
          "poll_ms": 500
        },
        "expects_api_after_terminal": [
          {
            "method": "GET",
            "path": "/runs/{run_id}",
            "operationId": "getRunStatus"
          }
        ],
        "forbids_rendered": [
          "[data-run-failure]"
        ]
      }
    ]
  },
  "refusals": {
    "$comment": [
      "The REFUSAL half. `D-44`, and before it `W27-REFUSE`'s six drives.",
      "",
      "`routes` and `write` both declare steps that SUCCEED. These six declare what happens",
      "when a person drops the wrong file into the upload control: where the refusal happened,",
      "what the screen said, and that the screen named the specific rule rather than a generic",
      "failure. `W27-REFUSE` drove all six through a browser on 2026-09-18 and all six held --",
      "and wrote the verdicts into `refusals.mjs`'s own source, where `make gate` cannot see",
      "them. That is what moving them here fixes.",
      "",
      "Read by the same two checkers as the other halves. At RUN TIME `refusals.mjs` attaches",
      "each fixture at the app's own control and asserts every field below against what the",
      "browser saw. At GATE TIME the conformance guard asserts, with no stack: the fixture is",
      "on disk; `refused_by` is `client` or `server` and the case declares the fields that go",
      "with it and none that do not; the operation triple is a real contract operation and the",
      "declared status is one the contract publishes FOR THAT OPERATION -- a `422` is as",
      "declarable as a `201`; every marker attribute and every marker VALUE named here still",
      "appears in `web/src`; every sentence in `expects_rendered` still appears in `web/src`;",
      "and no case declares a classification from `generic_kinds`.",
      "",
      "WHICH HALF DOES NOT MOVE, stated rather than quietly widened. The gate cannot see that",
      "the envelope really carries that `constraint`, that the screen really renders it, that",
      "`Upload` really goes unpressable, or that nothing was published. Those are behaviour and",
      "they stay with `refusals.mjs` against a running stack. `expects_rendered_from_envelope`",
      "is the part of the rendered text the SERVER supplies, so it is deliberately not checked",
      "against `web/src` -- it is checked against the `constraint` beside it instead, which is",
      "data and therefore guardable.",
      "",
      "`fixture` is relative to `fixture_dir`."
    ],
    "at": "/projects/{project_uid}",
    "page_module": "web/src/app/projects/[project_uid]/page.tsx",
    "control_module": "web/src/features/upload-document/ui/upload-document-form.tsx",
    "fixture_dir": "fixtures/synthetic/ar/negative",
    "api": {
      "method": "POST",
      "path": "/projects/{project_uid}/documents",
      "operationId": "uploadDocument",
      "$why": "the one call a server-side refusal makes. A client-side refusal makes none, which is half of what these cases assert."
    },
    "precheck_marker": "data-precheck-problem",
    "failure_marker": "data-upload-failure",
    "controls": {
      "$comment": [
        "The controls the six drives press and read. `W44-JOURNEY` 2026-09-24, and the",
        "reason they are here is `W28-GUARD`'s reason applied to the half it did not move.",
        "",
        "They were literals in `refusals.mjs`'s own source -- `Create`, `Upload`, `Retry`,",
        "`Chosen: ` -- and the gate cannot read that file. The application was translated",
        "and every one of them stopped matching: MEASURED 2026-09-24, `refusals.mjs` threw",
        "`click form button[type=\"submit\"] [text=\"Create\"]: no element matches it` before it",
        "drove a single fixture. So `D-83`'s \"outside the gate `refusals.mjs` drives them",
        "against a live stand\" was not true either: the eleven sentences were verified by",
        "nothing at all, in the gate or out of it.",
        "",
        "Declared here, they are handles like any other and",
        "`test_every_control_the_refusals_press_still_exists_in_the_application` reddens",
        "`make gate` the next time one is relabelled.",
        "",
        "The retry action is read by CLASS because `.am-state__action` is structure",
        "shared/ui owns whatever the action says. The chosen-file line is found by class",
        "and its visible prefix is asserted from `chosen_label`, so translating it cannot",
        "silently leave the live refusal drive green over stale prose."
      ],
      "file_input": "#upload-file",
      "title_input": "#upload-title",
      "submit_label": "Загрузить",
      "retry_action_selector": ".am-state__action button",
      "chosen_selector": ".am-form__chosen",
      "chosen_label": "Выбран файл:"
    },
    "generic_kinds": [
      "server_error",
      "unknown",
      "unrecognized",
      "transport"
    ],
    "cases": [
      {
        "fixture": "not_a_pdf.txt",
        "rule": "ENV-PDF",
        "refused_by": "client",
        "precheck_problem": "not_pdf",
        "expects_rendered": [
          "не является PDF",
          "Ничего не отправлено"
        ]
      },
      {
        "fixture": "companion_archive.zip",
        "rule": "ENV-PDF",
        "refused_by": "client",
        "precheck_problem": "not_pdf",
        "expects_rendered": [
          "не является PDF",
          "Ничего не отправлено"
        ]
      },
      {
        "fixture": "oversize.pdf",
        "rule": "ENV-SIZE",
        "refused_by": "client",
        "precheck_problem": "too_large",
        "expects_rendered": [
          "Этот файл больше, чем",
          "Ничего не отправлено"
        ],
        "$why_the_size_case_is_client_side": "`D-44`. It is client-side only while the pre-check limit stays strictly below nginx's `client_max_body_size`; tests/e2e/test_upload_limit_headroom.py is what keeps that true.",
        "$why_not_the_limit": "D-61, W41-BLIND 2026-09-23. This case declared \"25 MiB\" as well, and the upload panel prints `Не более 25 MiB.` before any file is chosen -- so that half of the assertion was satisfied by the envelope notice on an untouched screen. `Ничего не отправлено` is rendered only by the pre-check refusal panel and is the half that is evidence.",
        "$why_the_rule_specific_half_is_back": "W44-JOURNEY 2026-09-24. W41-BLIND removed \"25 MiB\" from this case because the upload panel prints `Не более 25 MiB.` before any file is chosen, so that half could not fail -- and left the case with only `Ничего не отправлено`, which says a refusal happened and not WHICH rule was broken. This is the rule-specific half in the form D-61 asks for: `Этот файл больше, чем` is authored in entities/document-version/model/upload-envelope.ts and rendered by the too_large pre-check and by nothing else. The size itself is interpolated, so the sentence stops where the template does -- declaring the number back would be declaring a value the source does not author."
      },
      {
        "fixture": "encrypted.pdf",
        "rule": "ENV-ENCRYPTED",
        "refused_by": "server",
        "expect_status": 422,
        "error_code": "validation_failed",
        "constraint": "not_encrypted",
        "failure_kind": "unsupported_input",
        "expects_rendered": [
          "выходит за допустимые ограничения"
        ],
        "expects_rendered_from_envelope": [
          "not_encrypted"
        ]
      },
      {
        "fixture": "image_only.pdf",
        "rule": "ENV-TEXT",
        "refused_by": "server",
        "expect_status": 422,
        "error_code": "validation_failed",
        "constraint": "every_page_has_extractable_text",
        "failure_kind": "unsupported_input",
        "expects_rendered": [
          "выходит за допустимые ограничения"
        ],
        "expects_rendered_from_envelope": [
          "every_page_has_extractable_text"
        ]
      },
      {
        "fixture": "too_many_pages.pdf",
        "rule": "ENV-PAGES",
        "refused_by": "server",
        "expect_status": 422,
        "error_code": "validation_failed",
        "constraint": "1 <= page_count <= 30",
        "failure_kind": "unsupported_input",
        "expects_rendered": [
          "выходит за допустимые ограничения"
        ],
        "expects_rendered_from_envelope": [
          "page_count"
        ]
      }
    ]
  }
}
